{
  "openapi": "3.1.1",
  "info": {
    "title": "Cyber Cloud",
    "version": "2026-08-01",
    "description": "The Cyber Cloud resource API at api-version 2026-08-01. Generated from the provider registry (docs/plan/02 § ADR-012); hand edits are overwritten by ./build.sh Generate and fail the Generated surfaces gate."
  },
  "servers": [
    {
      "url": "/",
      "description": "Relative to the gateway that serves this document."
    }
  ],
  "paths": {
    "/operations/{operationId}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/OperationId"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "Operations_Get",
        "summary": "Poll a long-running operation.",
        "description": "The target of the Azure-AsyncOperation header a 202 returns. Poll until status is terminal, then GET the resource — docs/plan/10 § Long-running operations, over HTTP.",
        "responses": {
          "200": {
            "description": "The operation's current state.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationStatus"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      }
    },
    "/tenants/{tenantId}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "Scopes_GetTenant",
        "summary": "Read a tenant.",
        "description": "A caller may read the tenant they hold a token for and no other, because the request's tenant is resolved from the token — docs/plan/10 § Request pipeline, stage 3. ⚠ There is no PUT. A tenant is created by IScopeManager.CreateTenantAsync, off the request pipeline entirely: the only tenant a request can address is one that already exists, so a create route could not authenticate. docs/plan/08 § The write path, end to end carries the argument and what was rejected.",
        "responses": {
          "200": {
            "description": "The scope.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      },
      "x-cybercloud-scope": "tenant",
      "x-cybercloud-scope-type": "CyberCloud.Resources/tenants",
      "x-cybercloud-display": {
        "name": "Tenant",
        "plural": "Tenants",
        "summary": "The isolation boundary. docs/plan/06 § The hierarchy."
      },
      "x-cybercloud-scope-read-only": true
    },
    "/tenants/{tenantId}/subscriptions": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "Scopes_ListSubscriptions",
        "summary": "List subscriptions.",
        "description": "Lists the subscriptions in the tenant the token names, one page at a time. The page holds the subscriptions the caller may read — any role on a subscription is enough, and no permission on the tenant itself is needed — so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      },
      "x-cybercloud-scope": "subscription",
      "x-cybercloud-scope-type": "CyberCloud.Resources/subscriptions",
      "x-cybercloud-scope-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "Scopes_GetSubscription",
        "summary": "Read a subscription.",
        "description": "Reads one subscription.",
        "responses": {
          "200": {
            "description": "The scope.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      },
      "x-cybercloud-scope": "subscription",
      "x-cybercloud-scope-type": "CyberCloud.Resources/subscriptions",
      "x-cybercloud-display": {
        "name": "Subscription",
        "plural": "Subscriptions",
        "summary": "The billing and quota boundary. docs/plan/06 § The hierarchy."
      },
      "put": {
        "operationId": "Scopes_PutSubscription",
        "summary": "Create a subscription.",
        "description": "Creates the subscription at this address, or returns the existing one unchanged. ⚠ 201 the first time and 200 on a repeat: that is what idempotent means and is why the verb is PUT. The id is the caller's to choose, so a retry addresses the same subscription rather than making a second one.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Scope.SubscriptionCreate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The scope already existed and is returned unchanged.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "201": {
            "description": "The scope was created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      }
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "Scopes_ListResourceGroups",
        "summary": "List resource groups.",
        "description": "Lists the resource groups in one subscription, one page at a time, filtered to the groups the caller may read. ⚠ 404 when the caller cannot read the subscription, which is the same answer as for a subscription that does not exist — docs/plan/07 § The enforcement seam. Stop when nextLink is absent.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      },
      "x-cybercloud-scope": "resourceGroup",
      "x-cybercloud-scope-type": "CyberCloud.Resources/subscriptions/resourceGroups",
      "x-cybercloud-scope-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "Scopes_GetResourceGroup",
        "summary": "Read a resource group.",
        "description": "Reads one resource group.",
        "responses": {
          "200": {
            "description": "The scope.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      },
      "x-cybercloud-scope": "resourceGroup",
      "x-cybercloud-scope-type": "CyberCloud.Resources/subscriptions/resourceGroups",
      "x-cybercloud-display": {
        "name": "Resource group",
        "plural": "Resource groups",
        "summary": "The lifecycle boundary — what a resource is created in. docs/plan/06 § The hierarchy."
      },
      "put": {
        "operationId": "Scopes_PutResourceGroup",
        "summary": "Create a resource group.",
        "description": "Creates the resource group at this address, or returns the existing one unchanged. ⚠ Every resource path names a resource group, so this is the call that has to succeed before any resource can be created at all.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Scope.ResourceGroupCreate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The scope already existed and is returned unchanged.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "201": {
            "description": "The scope was created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Scope"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        }
      }
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Analytics/clickhouseClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_List",
        "summary": "List the CyberCloud.Analytics/clickhouseClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Analytics/clickhouseClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Analytics/clickhouseClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_Get",
        "summary": "Read a CyberCloud.Analytics/clickhouseClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Analytics/clickhouseClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_Update",
        "summary": "Merge-patch a CyberCloud.Analytics/clickhouseClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_Delete",
        "summary": "Delete a CyberCloud.Analytics/clickhouseClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Analytics/clickhouseClusters",
      "x-cybercloud-display": {
        "name": "ClickHouse cluster",
        "plural": "ClickHouse clusters",
        "alias": "clickhouse",
        "summary": "A managed ClickHouse cluster on the Altinity operator, with declared shards and replicas and a ClickHouse Keeper quorum. The schema is the tenant's.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "shards × replicas × sizing.memory (from sizing.preset when unset) + keeperNodes × 512Mi, in GiB",
          "reads": [
            "/properties/shards",
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/memory",
            "/properties/keeperNodes"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "shards × replicas × storage.size + keeperNodes × 10Gi, in GiB",
          "reads": [
            "/properties/shards",
            "/properties/replicas",
            "/properties/storage/size",
            "/properties/keeperNodes"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "shards × replicas × sizing.cpu (from sizing.preset when unset) + keeperNodes × 250m, in cores",
          "reads": [
            "/properties/shards",
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/cpu",
            "/properties/keeperNodes"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Analytics/clickhouseClusters/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Analytics_clickhouseClusters_ListKeys",
        "summary": "listKeys a CyberCloud.Analytics/clickhouseClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Analytics/clickhouseClusters",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Cache/redis": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Cache_redis_List",
        "summary": "List the CyberCloud.Cache/redis in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Cache/redis",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Cache/redis/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Cache_redis_Get",
        "summary": "Read a CyberCloud.Cache/redis.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Cache_redis_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Cache/redis.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Cache.redis"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Cache_redis_Update",
        "summary": "Merge-patch a CyberCloud.Cache/redis.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Cache.redis"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Cache_redis_Delete",
        "summary": "Delete a CyberCloud.Cache/redis.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Cache/redis",
      "x-cybercloud-display": {
        "name": "Valkey cache",
        "plural": "Valkey caches",
        "alias": "valkey",
        "summary": "A managed Valkey cache with Sentinel failover, on spotahome/redis-operator. Valkey rather than Redis, per ADR-011; every Redis client works against it.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Cache/redis/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Cache_redis_ListKeys",
        "summary": "listKeys a CyberCloud.Cache/redis.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Cache.redis.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Cache/redis",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_List",
        "summary": "List the CyberCloud.Communication/services in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_Get",
        "summary": "Read a CyberCloud.Communication/services.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Communication_services_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Communication/services.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Communication_services_Update",
        "summary": "Merge-patch a CyberCloud.Communication/services.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Communication_services_Delete",
        "summary": "Delete a CyberCloud.Communication/services.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-display": {
        "name": "Communication service",
        "plural": "Communication services",
        "alias": "comms",
        "summary": "A sending service — SMS, WhatsApp, email, push and voice through the platform's carrier accounts or the tenant's own — with per-channel spend limits, versioned templates, a suppression list honoured before every dispatch, and delivery receipts per send.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{resourceName}/checkSuppression": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Communication_services_CheckSuppression",
        "summary": "checkSuppression a CyberCloud.Communication/services.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.CheckSuppressionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.CheckSuppressionResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-action": "checkSuppression"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{resourceName}/listSuppressions": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Communication_services_ListSuppressions",
        "summary": "listSuppressions a CyberCloud.Communication/services.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.ListSuppressionsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.ListSuppressionsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-action": "listSuppressions"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{resourceName}/send": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Communication_services_Send",
        "summary": "send a CyberCloud.Communication/services.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.SendRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.SendResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-action": "send"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{resourceName}/status": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Communication_services_Status",
        "summary": "status a CyberCloud.Communication/services.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.StatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.StatusResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services",
      "x-cybercloud-action": "status"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/channels": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_channels_List",
        "summary": "List the CyberCloud.Communication/services/channels in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.channels.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/channels",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/channels/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_channels_Get",
        "summary": "Read a CyberCloud.Communication/services/channels.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Communication_services_channels_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Communication/services/channels.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Communication_services_channels_Update",
        "summary": "Merge-patch a CyberCloud.Communication/services/channels.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Communication_services_channels_Delete",
        "summary": "Delete a CyberCloud.Communication/services/channels.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/channels",
      "x-cybercloud-display": {
        "name": "Communication channel",
        "plural": "Communication channels",
        "alias": "channel",
        "summary": "One channel a service sends on: which carrier, whose account pays, and what it may send and spend per day.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/suppressions": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_suppressions_List",
        "summary": "List the CyberCloud.Communication/services/suppressions in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/suppressions",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/suppressions/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_suppressions_Get",
        "summary": "Read a CyberCloud.Communication/services/suppressions.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Communication_services_suppressions_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Communication/services/suppressions.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Communication_services_suppressions_Update",
        "summary": "Merge-patch a CyberCloud.Communication/services/suppressions.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Communication_services_suppressions_Delete",
        "summary": "Delete a CyberCloud.Communication/services/suppressions.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/suppressions",
      "x-cybercloud-display": {
        "name": "Suppression",
        "plural": "Suppressions",
        "alias": "suppression",
        "summary": "An address a service must never send to, placed by the tenant. Bounces, complaints and opt-outs join the same list on their own and are not resources.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/templates": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_templates_List",
        "summary": "List the CyberCloud.Communication/services/templates in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/templates",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/templates/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Communication_services_templates_Get",
        "summary": "Read a CyberCloud.Communication/services/templates.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Communication_services_templates_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Communication/services/templates.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Communication_services_templates_Update",
        "summary": "Merge-patch a CyberCloud.Communication/services/templates.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Communication_services_templates_Delete",
        "summary": "Delete a CyberCloud.Communication/services/templates.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/templates",
      "x-cybercloud-display": {
        "name": "Message template",
        "plural": "Message templates",
        "alias": "template",
        "summary": "A named, versioned message body with typed variables. Every change appends a version; a send names the template and the version it wants.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Communication/services/{servicesName}/templates/{resourceName}/render": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "servicesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Communication_services_templates_Render",
        "summary": "render a CyberCloud.Communication/services/templates.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Communication.services.templates.RenderRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Communication.services.templates.RenderResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Communication/services/templates",
      "x-cybercloud-action": "render"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/feeds": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerRegistry_feeds_List",
        "summary": "List the CyberCloud.ContainerRegistry/feeds in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/feeds",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/feeds/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerRegistry_feeds_Get",
        "summary": "Read a CyberCloud.ContainerRegistry/feeds.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_ContainerRegistry_feeds_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.ContainerRegistry/feeds.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_ContainerRegistry_feeds_Update",
        "summary": "Merge-patch a CyberCloud.ContainerRegistry/feeds.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_ContainerRegistry_feeds_Delete",
        "summary": "Delete a CyberCloud.ContainerRegistry/feeds.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/feeds",
      "x-cybercloud-display": {
        "name": "Artifact feed",
        "plural": "Artifact feeds",
        "alias": "feed",
        "summary": "A NuGet, npm or Maven package feed served by the platform's feeds host, with artefacts on the platform's object storage.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/registries": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerRegistry_registries_List",
        "summary": "List the CyberCloud.ContainerRegistry/registries in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/registries",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/registries/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerRegistry_registries_Get",
        "summary": "Read a CyberCloud.ContainerRegistry/registries.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_ContainerRegistry_registries_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.ContainerRegistry/registries.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_ContainerRegistry_registries_Update",
        "summary": "Merge-patch a CyberCloud.ContainerRegistry/registries.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_ContainerRegistry_registries_Delete",
        "summary": "Delete a CyberCloud.ContainerRegistry/registries.",
        "description": "A deleted CyberCloud.ContainerRegistry/registries is recoverable for 7 day(s) — docs/plan/06 § Tags, locks. Its data plane is torn down, so nothing keeps running and nothing keeps being metered for compute; its stored state and its volumes are kept, so a restore re-applies exactly what was there. Its old address answers 404 for the whole window, its name is held, and its quota stays committed so that a restore cannot fail against an allowance spent since. Restore it, or purge it to end the window early — a purge needs 'purge', which is a separate right from 'delete'.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 7,
        "x-cybercloud-purge-permission": "purge"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/registries",
      "x-cybercloud-display": {
        "name": "Container registry",
        "plural": "Container registries",
        "alias": "registry",
        "summary": "A private OCI container registry on Harbor, with a web portal, an image store and a seven-day recovery window.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 7,
      "x-cybercloud-purge-permission": "purge",
      "x-cybercloud-purge-protection-pointer": "/properties/purgeProtection",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.memory (from sizing.preset when unset) + (3 × replicas + 2) × 512Mi, in GiB",
          "reads": [
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "storage.size + 10Gi for the metadata database + 1Gi for the job queue, in GiB",
          "reads": [
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.cpu (from sizing.preset when unset) + (3 × replicas + 2) × 250m, in cores",
          "reads": [
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/registries/{resourceName}/listCredentials": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerRegistry_registries_ListCredentials",
        "summary": "listCredentials a CyberCloud.ContainerRegistry/registries.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries.ListCredentialsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listCredentials",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/registries",
      "x-cybercloud-action": "listCredentials"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/registries/{resourceName}/purge": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerRegistry_registries_Purge",
        "summary": "purge a CyberCloud.ContainerRegistry/registries.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "purge",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/registries",
      "x-cybercloud-action": "purge"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerRegistry/registries/{resourceName}/restore": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerRegistry_registries_Restore",
        "summary": "restore a CyberCloud.ContainerRegistry/registries.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerRegistry/registries",
      "x-cybercloud-action": "restore"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/connectedClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_List",
        "summary": "List the CyberCloud.ContainerService/connectedClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/connectedClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/connectedClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_Get",
        "summary": "Read a CyberCloud.ContainerService/connectedClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.ContainerService/connectedClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_Update",
        "summary": "Merge-patch a CyberCloud.ContainerService/connectedClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_Delete",
        "summary": "Delete a CyberCloud.ContainerService/connectedClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/connectedClusters",
      "x-cybercloud-display": {
        "name": "Connected Kubernetes cluster",
        "plural": "Connected Kubernetes clusters",
        "alias": "byo",
        "summary": "A cluster you run yourself — on-prem, behind NAT, anywhere with outbound HTTPS — reached through an agent you install in it. Create it, run the install command it gives you, and place resources in it once it reports Succeeded.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Clusters",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/connectedClusters/{resourceName}/listInstallCommand": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerService_connectedClusters_ListInstallCommand",
        "summary": "listInstallCommand a CyberCloud.ContainerService/connectedClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters.ListInstallCommandResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listInstallCommand",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/connectedClusters",
      "x-cybercloud-action": "listInstallCommand"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_managedClusters_List",
        "summary": "List the CyberCloud.ContainerService/managedClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters/{managedClustersName}/agentPools": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "managedClustersName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_List",
        "summary": "List the CyberCloud.ContainerService/managedClusters/agentPools in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters/agentPools",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters/{managedClustersName}/agentPools/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "managedClustersName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_Get",
        "summary": "Read a CyberCloud.ContainerService/managedClusters/agentPools.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.ContainerService/managedClusters/agentPools.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_Update",
        "summary": "Merge-patch a CyberCloud.ContainerService/managedClusters/agentPools.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_Delete",
        "summary": "Delete a CyberCloud.ContainerService/managedClusters/agentPools.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters/agentPools",
      "x-cybercloud-display": {
        "name": "Node pool",
        "plural": "Node pools",
        "alias": "nodepool",
        "summary": "A group of identically sized worker virtual machines in a managed Kubernetes cluster, optionally under a cluster-autoscaler.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "(autoscale.enabled ? autoscale.maxCount : count) × the size preset's memory, in GiB",
          "reads": [
            "/properties/count",
            "/properties/size",
            "/properties/autoscale/enabled",
            "/properties/autoscale/maxCount"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "(autoscale.enabled ? autoscale.maxCount : count) × osDiskSize, in GiB",
          "reads": [
            "/properties/count",
            "/properties/osDiskSize",
            "/properties/autoscale/enabled",
            "/properties/autoscale/maxCount"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "(autoscale.enabled ? autoscale.maxCount : count) × the size preset's cpu, in cores",
          "reads": [
            "/properties/count",
            "/properties/size",
            "/properties/autoscale/enabled",
            "/properties/autoscale/maxCount"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters/{managedClustersName}/agentPools/{resourceName}/upgradeNodeImage": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "managedClustersName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerService_managedClusters_agentPools_UpgradeNodeImage",
        "summary": "upgradeNodeImage a CyberCloud.ContainerService/managedClusters/agentPools.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters/agentPools",
      "x-cybercloud-action": "upgradeNodeImage"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_ContainerService_managedClusters_Get",
        "summary": "Read a CyberCloud.ContainerService/managedClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_ContainerService_managedClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.ContainerService/managedClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_ContainerService_managedClusters_Update",
        "summary": "Merge-patch a CyberCloud.ContainerService/managedClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_ContainerService_managedClusters_Delete",
        "summary": "Delete a CyberCloud.ContainerService/managedClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters",
      "x-cybercloud-display": {
        "name": "Managed Kubernetes cluster",
        "plural": "Managed Kubernetes clusters",
        "alias": "aks",
        "summary": "A Kubernetes cluster whose control plane runs as pods in the management cluster and whose workers are isolated virtual machines. Node pools are a child resource.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Clusters",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "controlPlane.replicas × 3 containers × 1Gi, in GiB",
          "reads": [
            "/properties/controlPlane/replicas"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "controlPlane.replicas × 3 containers × 500m, in cores",
          "reads": [
            "/properties/controlPlane/replicas"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.ContainerService/managedClusters/{resourceName}/listCredentials": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_ContainerService_managedClusters_ListCredentials",
        "summary": "listCredentials a CyberCloud.ContainerService/managedClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.ListCredentialsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listCredentials",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.ContainerService/managedClusters",
      "x-cybercloud-action": "listCredentials"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforMySQL/servers": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_DBforMySQL_servers_List",
        "summary": "List the CyberCloud.DBforMySQL/servers in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforMySQL/servers",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforMySQL/servers/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_DBforMySQL_servers_Get",
        "summary": "Read a CyberCloud.DBforMySQL/servers.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_DBforMySQL_servers_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.DBforMySQL/servers.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_DBforMySQL_servers_Update",
        "summary": "Merge-patch a CyberCloud.DBforMySQL/servers.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_DBforMySQL_servers_Delete",
        "summary": "Delete a CyberCloud.DBforMySQL/servers.",
        "description": "A deleted CyberCloud.DBforMySQL/servers is recoverable for 7 day(s) — docs/plan/06 § Tags, locks. Its data plane is torn down, so nothing keeps running and nothing keeps being metered for compute; its stored state and its volumes are kept, so a restore re-applies exactly what was there. Its old address answers 404 for the whole window, its name is held, and its quota stays committed so that a restore cannot fail against an allowance spent since. Restore it, or purge it to end the window early — a purge needs 'purge', which is a separate right from 'delete'.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 7,
        "x-cybercloud-purge-permission": "purge"
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforMySQL/servers",
      "x-cybercloud-display": {
        "name": "MariaDB server",
        "plural": "MariaDB servers",
        "alias": "mariadb",
        "summary": "A managed MariaDB server on mariadb-operator, speaking the MySQL wire protocol, with Galera for high availability. MariaDB is MySQL-compatible on a documented subset and is not MySQL — see the supported-subset table before migrating.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 7,
      "x-cybercloud-purge-permission": "purge",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "instances × sizing.memory, in GiB, where instances is 3 under Galera and 1 otherwise, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/highAvailability",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "instances × storage.size, in GiB, where instances is 3 under Galera and 1 otherwise",
          "reads": [
            "/properties/highAvailability",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "instances × sizing.cpu, in cores, where instances is 3 under Galera and 1 otherwise, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/highAvailability",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforMySQL/servers/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforMySQL_servers_ListKeys",
        "summary": "listKeys a CyberCloud.DBforMySQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforMySQL/servers",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforMySQL/servers/{resourceName}/purge": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforMySQL_servers_Purge",
        "summary": "purge a CyberCloud.DBforMySQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "purge",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforMySQL/servers",
      "x-cybercloud-action": "purge"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforMySQL/servers/{resourceName}/restore": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforMySQL_servers_Restore",
        "summary": "restore a CyberCloud.DBforMySQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforMySQL/servers",
      "x-cybercloud-action": "restore"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforPostgreSQL/servers": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_List",
        "summary": "List the CyberCloud.DBforPostgreSQL/servers in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforPostgreSQL/servers",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforPostgreSQL/servers/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_Get",
        "summary": "Read a CyberCloud.DBforPostgreSQL/servers.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.DBforPostgreSQL/servers.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_Update",
        "summary": "Merge-patch a CyberCloud.DBforPostgreSQL/servers.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_Delete",
        "summary": "Delete a CyberCloud.DBforPostgreSQL/servers.",
        "description": "A deleted CyberCloud.DBforPostgreSQL/servers is recoverable for 7 day(s) — docs/plan/06 § Tags, locks. Its data plane is torn down, so nothing keeps running and nothing keeps being metered for compute; its stored state and its volumes are kept, so a restore re-applies exactly what was there. Its old address answers 404 for the whole window, its name is held, and its quota stays committed so that a restore cannot fail against an allowance spent since. Restore it, or purge it to end the window early — a purge needs 'purge', which is a separate right from 'delete'.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 7,
        "x-cybercloud-purge-permission": "purge"
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforPostgreSQL/servers",
      "x-cybercloud-display": {
        "name": "PostgreSQL server",
        "plural": "PostgreSQL servers",
        "alias": "postgres",
        "summary": "A managed PostgreSQL cluster on CloudNativePG, with replication, PgBouncer and backup to the tenant's object store.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 7,
      "x-cybercloud-purge-permission": "purge",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "replicas × sizing.memory, in GiB, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "replicas × (storage.size + storage.walSize), in GiB; walSize empty means one volume",
          "reads": [
            "/properties/replicas",
            "/properties/storage/size",
            "/properties/storage/walSize"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "replicas × sizing.cpu, in cores, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/replicas",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforPostgreSQL/servers/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_ListKeys",
        "summary": "listKeys a CyberCloud.DBforPostgreSQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforPostgreSQL/servers",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforPostgreSQL/servers/{resourceName}/purge": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_Purge",
        "summary": "purge a CyberCloud.DBforPostgreSQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "purge",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforPostgreSQL/servers",
      "x-cybercloud-action": "purge"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DBforPostgreSQL/servers/{resourceName}/restore": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DBforPostgreSQL_servers_Restore",
        "summary": "restore a CyberCloud.DBforPostgreSQL/servers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.DBforPostgreSQL/servers",
      "x-cybercloud-action": "restore"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DocumentDB/accounts": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_DocumentDB_accounts_List",
        "summary": "List the CyberCloud.DocumentDB/accounts in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.DocumentDB/accounts",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DocumentDB/accounts/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_DocumentDB_accounts_Get",
        "summary": "Read a CyberCloud.DocumentDB/accounts.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_DocumentDB_accounts_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.DocumentDB/accounts.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_DocumentDB_accounts_Update",
        "summary": "Merge-patch a CyberCloud.DocumentDB/accounts.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_DocumentDB_accounts_Delete",
        "summary": "Delete a CyberCloud.DocumentDB/accounts.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.DocumentDB/accounts",
      "x-cybercloud-display": {
        "name": "Document database account",
        "plural": "Document database accounts",
        "alias": "docdb",
        "summary": "MongoDB-compatible, not MongoDB: FerretDB over PostgreSQL, covering CRUD, indexes and the wire protocol. Multi-document transactions are not supported. Runs on a CloudNativePG cluster, so failover, backup and point-in-time recovery are the operator's.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "postgres.instances × sizing.memory (from sizing.preset when unset) + gateway.replicas × 512Mi, in GiB",
          "reads": [
            "/properties/postgres/instances",
            "/properties/sizing/preset",
            "/properties/sizing/memory",
            "/properties/gateway/replicas"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "postgres.instances × storage.size, in GiB",
          "reads": [
            "/properties/postgres/instances",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "postgres.instances × sizing.cpu (from sizing.preset when unset) + gateway.replicas × 250m, in cores",
          "reads": [
            "/properties/postgres/instances",
            "/properties/sizing/preset",
            "/properties/sizing/cpu",
            "/properties/gateway/replicas"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.DocumentDB/accounts/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_DocumentDB_accounts_ListKeys",
        "summary": "listKeys a CyberCloud.DocumentDB/accounts.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.DocumentDB/accounts",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Mail/domains": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Mail_domains_List",
        "summary": "List the CyberCloud.Mail/domains in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Mail.domains.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Mail/domains",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Mail/domains/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Mail_domains_Get",
        "summary": "Read a CyberCloud.Mail/domains.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Mail.domains"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Mail_domains_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Mail/domains.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Mail.domains"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Mail.domains"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Mail_domains_Update",
        "summary": "Merge-patch a CyberCloud.Mail/domains.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Mail.domains"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Mail.domains"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Mail_domains_Delete",
        "summary": "Delete a CyberCloud.Mail/domains.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Mail.domains"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Mail/domains",
      "x-cybercloud-display": {
        "name": "Mail domain",
        "plural": "Mail domains",
        "alias": "domain",
        "summary": "A managed mail domain on Dovecot, Postfix and Rspamd, with a per-tenant mail store, DKIM signing, and the SPF, DKIM, DMARC and MX records the domain must publish before the platform will send for it.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.memory, in GiB, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "storage.size, in GiB",
          "reads": [
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.cpu, in cores, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/kafkaClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_List",
        "summary": "List the CyberCloud.Messaging/kafkaClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/kafkaClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/kafkaClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_Get",
        "summary": "Read a CyberCloud.Messaging/kafkaClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Messaging/kafkaClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_Update",
        "summary": "Merge-patch a CyberCloud.Messaging/kafkaClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_Delete",
        "summary": "Delete a CyberCloud.Messaging/kafkaClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/kafkaClusters",
      "x-cybercloud-display": {
        "name": "Kafka cluster",
        "plural": "Kafka clusters",
        "alias": "kafka",
        "summary": "A managed Apache Kafka cluster on Strimzi in KRaft mode, with Cruise Control, configurable retention and an optional firewalled external listener.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/kafkaClusters/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Messaging_kafkaClusters_ListKeys",
        "summary": "listKeys a CyberCloud.Messaging/kafkaClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/kafkaClusters",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/natsClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_natsClusters_List",
        "summary": "List the CyberCloud.Messaging/natsClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/natsClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/natsClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_natsClusters_Get",
        "summary": "Read a CyberCloud.Messaging/natsClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Messaging_natsClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Messaging/natsClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Messaging_natsClusters_Update",
        "summary": "Merge-patch a CyberCloud.Messaging/natsClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Messaging_natsClusters_Delete",
        "summary": "Delete a CyberCloud.Messaging/natsClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/natsClusters",
      "x-cybercloud-display": {
        "name": "NATS cluster",
        "plural": "NATS clusters",
        "alias": "nats",
        "summary": "A managed NATS cluster with JetStream on file storage, optional leaf-node connectivity and an optional firewalled external listener.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "servers × sizing.memory, in GiB, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/servers",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "servers × storage.size, in GiB",
          "reads": [
            "/properties/servers",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "servers × sizing.cpu, in cores, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/servers",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/natsClusters/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Messaging_natsClusters_ListKeys",
        "summary": "listKeys a CyberCloud.Messaging/natsClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/natsClusters",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/rabbitmqClusters": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_List",
        "summary": "List the CyberCloud.Messaging/rabbitmqClusters in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/rabbitmqClusters",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/rabbitmqClusters/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_Get",
        "summary": "Read a CyberCloud.Messaging/rabbitmqClusters.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Messaging/rabbitmqClusters.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_Update",
        "summary": "Merge-patch a CyberCloud.Messaging/rabbitmqClusters.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_Delete",
        "summary": "Delete a CyberCloud.Messaging/rabbitmqClusters.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/rabbitmqClusters",
      "x-cybercloud-display": {
        "name": "RabbitMQ cluster",
        "plural": "RabbitMQ clusters",
        "alias": "rabbitmq",
        "summary": "A managed RabbitMQ cluster on the RabbitMQ Cluster Operator, with quorum queues as the default queue type and the management UI reachable only in-cluster.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "nodes × sizing.memory, in GiB, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/nodes",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "nodes × storage.size, in GiB",
          "reads": [
            "/properties/nodes",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "nodes × sizing.cpu, in cores, taking sizing.preset when the override is empty",
          "reads": [
            "/properties/nodes",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Messaging/rabbitmqClusters/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Messaging_rabbitmqClusters_ListKeys",
        "summary": "listKeys a CyberCloud.Messaging/rabbitmqClusters.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Messaging/rabbitmqClusters",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Monitor_workspaces_List",
        "summary": "List the CyberCloud.Monitor/workspaces in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Monitor_workspaces_Get",
        "summary": "Read a CyberCloud.Monitor/workspaces.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Monitor_workspaces_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Monitor/workspaces.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Monitor_workspaces_Update",
        "summary": "Merge-patch a CyberCloud.Monitor/workspaces.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Monitor_workspaces_Delete",
        "summary": "Delete a CyberCloud.Monitor/workspaces.",
        "description": "A deleted CyberCloud.Monitor/workspaces is recoverable for 7 day(s) — docs/plan/06 § Tags, locks. Its data plane is torn down, so nothing keeps running and nothing keeps being metered for compute; its stored state and its volumes are kept, so a restore re-applies exactly what was there. Its old address answers 404 for the whole window, its name is held, and its quota stays committed so that a restore cannot fail against an allowance spent since. Restore it, or purge it to end the window early — a purge needs 'purge', which is a separate right from 'delete'.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 7,
        "x-cybercloud-purge-permission": "purge"
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces",
      "x-cybercloud-display": {
        "name": "Monitor workspace",
        "plural": "Monitor workspaces",
        "alias": "workspace",
        "summary": "A tenancy in the platform's metrics, logs and traces stores, with its own retention, quota, ingest key and read-only datasource endpoints.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 7,
      "x-cybercloud-purge-permission": "purge",
      "x-cybercloud-purge-protection-pointer": "/properties/purgeProtection",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "the sum over metrics, logs and traces of (retention tier's days) × (that signal's quota in GiB/day), in GiB",
          "reads": [
            "/properties/retention/metrics",
            "/properties/retention/logs",
            "/properties/retention/traces",
            "/properties/quota/metricsGbPerDay",
            "/properties/quota/logsGbPerDay",
            "/properties/quota/tracesGbPerDay"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Monitor_workspaces_ListKeys",
        "summary": "listKeys a CyberCloud.Monitor/workspaces.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{resourceName}/purge": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Monitor_workspaces_Purge",
        "summary": "purge a CyberCloud.Monitor/workspaces.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "purge",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces",
      "x-cybercloud-action": "purge"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{resourceName}/restore": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Monitor_workspaces_Restore",
        "summary": "restore a CyberCloud.Monitor/workspaces.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces",
      "x-cybercloud-action": "restore"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{workspacesName}/alertRules": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "workspacesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_List",
        "summary": "List the CyberCloud.Monitor/workspaces/alertRules in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces/alertRules",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{workspacesName}/alertRules/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "workspacesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_Get",
        "summary": "Read a CyberCloud.Monitor/workspaces/alertRules.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Monitor/workspaces/alertRules.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_Update",
        "summary": "Merge-patch a CyberCloud.Monitor/workspaces/alertRules.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_Delete",
        "summary": "Delete a CyberCloud.Monitor/workspaces/alertRules.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces/alertRules",
      "x-cybercloud-display": {
        "name": "Alert rule",
        "plural": "Alert rules",
        "alias": "alert",
        "summary": "A condition over the workspace's metrics or logs, evaluated on a schedule; when it holds for long enough the action group is told through a Communication service, and again when it stops.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": false,
      "x-cybercloud-cluster-id-pointer": "",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Monitor/workspaces/{workspacesName}/alertRules/{resourceName}/listInstances": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "workspacesName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Monitor_workspaces_alertRules_ListInstances",
        "summary": "listInstances a CyberCloud.Monitor/workspaces/alertRules.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules.ListInstancesResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Monitor/workspaces/alertRules",
      "x-cybercloud-action": "listInstances"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/publicIpAddresses": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_publicIpAddresses_List",
        "summary": "List the CyberCloud.Network/publicIpAddresses in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/publicIpAddresses",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/publicIpAddresses/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_publicIpAddresses_Get",
        "summary": "Read a CyberCloud.Network/publicIpAddresses.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_publicIpAddresses_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/publicIpAddresses.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_publicIpAddresses_Update",
        "summary": "Merge-patch a CyberCloud.Network/publicIpAddresses.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_publicIpAddresses_Delete",
        "summary": "Delete a CyberCloud.Network/publicIpAddresses.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/publicIpAddresses",
      "x-cybercloud-display": {
        "name": "Public IP address",
        "plural": "Public IP addresses",
        "alias": "publicip",
        "summary": "A public address allocated from the region's pool, which a load balancer or a gateway can later be given. On its own it carries no traffic.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "PublicIps",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/publicIpAddresses/{resourceName}/showAllocation": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_publicIpAddresses_ShowAllocation",
        "summary": "showAllocation a CyberCloud.Network/publicIpAddresses.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses.ShowAllocationResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/publicIpAddresses",
      "x-cybercloud-action": "showAllocation"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_List",
        "summary": "List the CyberCloud.Network/virtualNetworks in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks",
      "x-cybercloud-display": {
        "name": "Virtual network",
        "plural": "Virtual networks",
        "alias": "vnet",
        "summary": "A private routing domain on Kube-OVN with its own address space. Network-layer tenant separation: a private routing domain with its own address space, on shared hardware.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{resourceName}/showIsolation": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_ShowIsolation",
        "summary": "showIsolation a CyberCloud.Network/virtualNetworks.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.ShowIsolationResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks",
      "x-cybercloud-action": "showIsolation"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/loadBalancers": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_List",
        "summary": "List the CyberCloud.Network/virtualNetworks/loadBalancers in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/loadBalancers",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/loadBalancers/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks/loadBalancers.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks/loadBalancers.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks/loadBalancers.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks/loadBalancers.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/loadBalancers",
      "x-cybercloud-display": {
        "name": "Load balancer",
        "plural": "Load balancers",
        "alias": "loadbalancer",
        "summary": "An L4 TCP proxy on an address inside a virtual network, spreading connections across a pool of workload addresses with health checks and a connection limit.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.preset's memory, in GiB",
          "reads": [
            "/properties/sizing/preset"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "sizing.preset's cpu, in cores",
          "reads": [
            "/properties/sizing/preset"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/loadBalancers/{resourceName}/showBackends": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_loadBalancers_ShowBackends",
        "summary": "showBackends a CyberCloud.Network/virtualNetworks/loadBalancers.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers.ShowBackendsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/loadBalancers",
      "x-cybercloud-action": "showBackends"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/natGateways": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_List",
        "summary": "List the CyberCloud.Network/virtualNetworks/natGateways in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/natGateways",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/natGateways/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks/natGateways.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks/natGateways.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks/natGateways.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks/natGateways.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/natGateways",
      "x-cybercloud-display": {
        "name": "NAT gateway",
        "plural": "NAT gateways",
        "alias": "natgateway",
        "summary": "Outbound-only internet access for one subnet of a virtual network, translated to a public IP address the tenant holds. Inbound traffic is not admitted.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/natGateways/{resourceName}/showEgress": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_natGateways_ShowEgress",
        "summary": "showEgress a CyberCloud.Network/virtualNetworks/natGateways.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways.ShowEgressResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/natGateways",
      "x-cybercloud-action": "showEgress"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/peerings": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_List",
        "summary": "List the CyberCloud.Network/virtualNetworks/peerings in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/peerings",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/peerings/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks/peerings.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks/peerings.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks/peerings.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks/peerings.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/peerings",
      "x-cybercloud-display": {
        "name": "Peering",
        "plural": "Peerings",
        "alias": "peering",
        "summary": "A route exchange between this virtual network and another in the same resource group, so workloads in either reach the other's range by private address. Both networks stay separately owned.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/peerings/{resourceName}/showRoutes": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_peerings_ShowRoutes",
        "summary": "showRoutes a CyberCloud.Network/virtualNetworks/peerings.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings.ShowRoutesResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/peerings",
      "x-cybercloud-action": "showRoutes"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/securityGroups": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_List",
        "summary": "List the CyberCloud.Network/virtualNetworks/securityGroups in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/securityGroups",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/securityGroups/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks/securityGroups.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks/securityGroups.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks/securityGroups.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks/securityGroups.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/securityGroups",
      "x-cybercloud-display": {
        "name": "Security group",
        "plural": "Security groups",
        "alias": "secgroup",
        "summary": "A deny-by-default set of allow rules that become OVN ACLs on the ports in a virtual network. A workload may carry several.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/securityGroups/{resourceName}/showEffectiveRules": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_securityGroups_ShowEffectiveRules",
        "summary": "showEffectiveRules a CyberCloud.Network/virtualNetworks/securityGroups.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups.ShowEffectiveRulesResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/securityGroups",
      "x-cybercloud-action": "showEffectiveRules"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/subnets": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_List",
        "summary": "List the CyberCloud.Network/virtualNetworks/subnets in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/subnets",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/subnets/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_Get",
        "summary": "Read a CyberCloud.Network/virtualNetworks/subnets.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Network/virtualNetworks/subnets.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_Update",
        "summary": "Merge-patch a CyberCloud.Network/virtualNetworks/subnets.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_Delete",
        "summary": "Delete a CyberCloud.Network/virtualNetworks/subnets.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/subnets",
      "x-cybercloud-display": {
        "name": "Subnet",
        "plural": "Subnets",
        "alias": "subnet",
        "summary": "A range inside a virtual network that workloads are given addresses from, with optional outbound NAT and optional isolation from other subnets.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Network/virtualNetworks/{virtualNetworksName}/subnets/{resourceName}/listAddressUsage": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "virtualNetworksName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Network_virtualNetworks_subnets_ListAddressUsage",
        "summary": "listAddressUsage a CyberCloud.Network/virtualNetworks/subnets.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets.ListAddressUsageResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Network/virtualNetworks/subnets",
      "x-cybercloud-action": "listAddressUsage"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Sample/widgets": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Sample_widgets_List",
        "summary": "List the CyberCloud.Sample/widgets in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Sample/widgets",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Sample/widgets/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Sample_widgets_Get",
        "summary": "Read a CyberCloud.Sample/widgets.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Sample_widgets_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Sample/widgets.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Sample_widgets_Update",
        "summary": "Merge-patch a CyberCloud.Sample/widgets.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Sample_widgets_Delete",
        "summary": "Delete a CyberCloud.Sample/widgets.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Sample/widgets",
      "x-cybercloud-display": {
        "name": "Widget",
        "plural": "Widgets",
        "alias": "widget",
        "summary": "A ConfigMap with two fields in it.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Sample/widgets/{resourceName}/ping": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Sample_widgets_Ping",
        "summary": "ping a CyberCloud.Sample/widgets.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Sample.widgets.PingRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Sample.widgets.PingResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Sample/widgets",
      "x-cybercloud-action": "ping"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Search/services": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Search_services_List",
        "summary": "List the CyberCloud.Search/services in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Search/services",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Search/services/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Search_services_Get",
        "summary": "Read a CyberCloud.Search/services.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Search_services_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Search/services.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Search.services"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Search_services_Update",
        "summary": "Merge-patch a CyberCloud.Search/services.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Search.services"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Search_services_Delete",
        "summary": "Delete a CyberCloud.Search/services.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Search/services",
      "x-cybercloud-display": {
        "name": "OpenSearch service",
        "plural": "OpenSearch services",
        "alias": "opensearch",
        "summary": "A managed OpenSearch cluster on the OpenSearch operator, with dedicated cluster-manager, data and optional coordinating node pools and operator-generated transport and HTTP TLS.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "(dataNodes + coordinatingNodes) × sizing.memory (from sizing.preset when unset) + masterNodes × 2Gi, in GiB",
          "reads": [
            "/properties/dataNodes",
            "/properties/coordinatingNodes",
            "/properties/masterNodes",
            "/properties/sizing/preset",
            "/properties/sizing/memory"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "dataNodes × storage.size + (masterNodes + coordinatingNodes) × 10Gi, in GiB",
          "reads": [
            "/properties/dataNodes",
            "/properties/masterNodes",
            "/properties/coordinatingNodes",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "(dataNodes + coordinatingNodes) × sizing.cpu (from sizing.preset when unset) + masterNodes × 500m, in cores",
          "reads": [
            "/properties/dataNodes",
            "/properties/coordinatingNodes",
            "/properties/masterNodes",
            "/properties/sizing/preset",
            "/properties/sizing/cpu"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Search/services/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Search_services_ListKeys",
        "summary": "listKeys a CyberCloud.Search/services.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Search.services.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Search/services",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_List",
        "summary": "List the CyberCloud.Storage/accounts in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/buckets": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_buckets_List",
        "summary": "List the CyberCloud.Storage/accounts/buckets in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/buckets",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/buckets/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_buckets_Get",
        "summary": "Read a CyberCloud.Storage/accounts/buckets.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Storage_accounts_buckets_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Storage/accounts/buckets.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Storage_accounts_buckets_Update",
        "summary": "Merge-patch a CyberCloud.Storage/accounts/buckets.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Storage_accounts_buckets_Delete",
        "summary": "Delete a CyberCloud.Storage/accounts/buckets.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/buckets",
      "x-cybercloud-display": {
        "name": "Bucket",
        "plural": "Buckets",
        "alias": "bucket",
        "summary": "An S3 bucket inside a managed object-storage account, with an optional size limit and optional object versioning.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/buckets/{resourceName}/stats": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Storage_accounts_buckets_Stats",
        "summary": "stats a CyberCloud.Storage/accounts/buckets.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets.StatsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/buckets",
      "x-cybercloud-action": "stats"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/fileShares": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_List",
        "summary": "List the CyberCloud.Storage/accounts/fileShares in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/fileShares",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/fileShares/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_Get",
        "summary": "Read a CyberCloud.Storage/accounts/fileShares.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Storage/accounts/fileShares.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_Update",
        "summary": "Merge-patch a CyberCloud.Storage/accounts/fileShares.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_Delete",
        "summary": "Delete a CyberCloud.Storage/accounts/fileShares.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/fileShares",
      "x-cybercloud-display": {
        "name": "File share",
        "plural": "File shares",
        "alias": "fileshare",
        "summary": "A ReadWriteMany file share on a managed object-storage account's filer, mounted into pods through the SeaweedFS CSI driver with an enforced size.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{accountsName}/fileShares/{resourceName}/listMountTargets": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "name": "accountsName",
          "in": "path",
          "required": true,
          "description": "The name of the parent resource this one lives inside. A child is addressed through its parent — docs/plan/12 § Child resources.",
          "schema": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "minLength": 1,
            "maxLength": 63
          }
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Storage_accounts_fileShares_ListMountTargets",
        "summary": "listMountTargets a CyberCloud.Storage/accounts/fileShares.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares.ListMountTargetsResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts/fileShares",
      "x-cybercloud-action": "listMountTargets"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Storage_accounts_Get",
        "summary": "Read a CyberCloud.Storage/accounts.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Storage_accounts_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Storage/accounts.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Storage_accounts_Update",
        "summary": "Merge-patch a CyberCloud.Storage/accounts.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Storage_accounts_Delete",
        "summary": "Delete a CyberCloud.Storage/accounts.",
        "description": "A deleted CyberCloud.Storage/accounts is recoverable for 7 day(s) — docs/plan/06 § Tags, locks. Its data plane is torn down, so nothing keeps running and nothing keeps being metered for compute; its stored state and its volumes are kept, so a restore re-applies exactly what was there. Its old address answers 404 for the whole window, its name is held, and its quota stays committed so that a restore cannot fail against an allowance spent since. Restore it, or purge it to end the window early — a purge needs 'purge', which is a separate right from 'delete'.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 7,
        "x-cybercloud-purge-permission": "purge"
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts",
      "x-cybercloud-display": {
        "name": "Storage account",
        "plural": "Storage accounts",
        "alias": "objectstore",
        "summary": "A managed S3-compatible object store on SeaweedFS, with replicated volume servers, a filer and a scalable S3 gateway.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 7,
      "x-cybercloud-purge-permission": "purge",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "MemoryGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "volumeServers × sizing.memory (from sizing.preset when unset) + (masters + 1 + gateway.replicas) × 512Mi, in GiB",
          "reads": [
            "/properties/volumeServers",
            "/properties/sizing/preset",
            "/properties/sizing/memory",
            "/properties/masters",
            "/properties/gateway/replicas"
          ]
        },
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "volumeServers × storage.size + 10Gi for the filer, in GiB",
          "reads": [
            "/properties/volumeServers",
            "/properties/storage/size"
          ]
        },
        {
          "meter": "Vcpu",
          "amountPointer": "",
          "fallback": null,
          "expression": "volumeServers × sizing.cpu (from sizing.preset when unset) + (masters + 1 + gateway.replicas) × 250m, in cores",
          "reads": [
            "/properties/volumeServers",
            "/properties/sizing/preset",
            "/properties/sizing/cpu",
            "/properties/masters",
            "/properties/gateway/replicas"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{resourceName}/listKeys": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Storage_accounts_ListKeys",
        "summary": "listKeys a CyberCloud.Storage/accounts.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. ⚠ The response carries secret material. It is always audited and is never cached — docs/plan/08 § The provider registry.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts.ListKeysResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "listKeys",
        "x-cybercloud-secret": true,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts",
      "x-cybercloud-action": "listKeys"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{resourceName}/purge": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Storage_accounts_Purge",
        "summary": "purge a CyberCloud.Storage/accounts.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "purge",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts",
      "x-cybercloud-action": "purge"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Storage/accounts/{resourceName}/restore": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Storage_accounts_Restore",
        "summary": "restore a CyberCloud.Storage/accounts.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end. This action is long-running: it answers 202 and an operation to poll, like every other write.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": true
      },
      "x-cybercloud-resource-type": "CyberCloud.Storage/accounts",
      "x-cybercloud-action": "restore"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Terminal/consoles": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        },
        {
          "name": "$top",
          "in": "query",
          "required": false,
          "description": "How many resources to examine. ⚠ A cap the platform clamps and not a promise: the filter runs once per member, so this bounds the work and not the number of results.",
          "schema": {
            "type": "integer",
            "minimum": 1
          }
        },
        {
          "name": "$skipToken",
          "in": "query",
          "required": false,
          "description": "Where to resume. Take it from the previous page's nextLink rather than constructing one.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "operationId": "CyberCloud_Terminal_consoles_List",
        "summary": "List the CyberCloud.Terminal/consoles in a resource group.",
        "description": "Returns one page of the resources of this type in the resource group the path names, including any that are being deleted — docs/plan/06 § Two-phase create keeps a resource whose teardown has not converged visible with that state, because its pods still run and its meter still ticks.",
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles.List"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "x-cybercloud-resource-type": "CyberCloud.Terminal/consoles",
      "x-cybercloud-collection": true
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Terminal/consoles/{resourceName}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "get": {
        "operationId": "CyberCloud_Terminal_consoles_Get",
        "summary": "Read a CyberCloud.Terminal/consoles.",
        "description": "The body is this api-version's projection of the resource: docs/plan/08 § The provider registry keeps grain state as a superset and a read at an older version drops what that version did not declare.",
        "responses": {
          "200": {
            "description": "The resource: the Resource envelope's five members, then the body, then tags.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "read"
      },
      "put": {
        "operationId": "CyberCloud_Terminal_consoles_CreateOrUpdate",
        "summary": "Create or replace a CyberCloud.Terminal/consoles.",
        "description": "A full replacement. Every property the schema marks required must be present; a read-only property is refused rather than ignored — docs/plan/08 § The write path, end to end.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "patch": {
        "operationId": "CyberCloud_Terminal_consoles_Update",
        "summary": "Merge-patch a CyberCloud.Terminal/consoles.",
        "description": "A merge patch. The patch itself may omit required properties; the merged result is what is validated — see ResourceSchema.Validate.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "write"
      },
      "delete": {
        "operationId": "CyberCloud_Terminal_consoles_Delete",
        "summary": "Delete a CyberCloud.Terminal/consoles.",
        "description": "The delete is permanent: this type declares no soft-delete window.",
        "responses": {
          "202": {
            "description": "Accepted. The body is the resource as the manager holds it now that the write is accepted — provisioningState says which write is in flight: Creating, Updating or Deleting after a PUT, PATCH or DELETE; unchanged by a long-running action, which serves the read that followed its start. Poll the Azure-AsyncOperation target until the status is terminal.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
                }
              }
            },
            "headers": {
              "Azure-AsyncOperation": {
                "description": "The absolute URL of the operation to poll.",
                "required": true,
                "schema": {
                  "type": "string",
                  "format": "uri"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before the first poll.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "delete",
        "x-cybercloud-soft-delete-days": 0,
        "x-cybercloud-purge-permission": ""
      },
      "x-cybercloud-resource-type": "CyberCloud.Terminal/consoles",
      "x-cybercloud-display": {
        "name": "Cloud terminal",
        "plural": "Cloud terminals",
        "alias": "shell",
        "summary": "A browser shell running in this subscription's own cluster, holding a managed identity, with a persistent home directory and no stored credential.",
        "declared": true
      },
      "x-cybercloud-supports-tags": true,
      "x-cybercloud-requires-cluster": true,
      "x-cybercloud-cluster-id-pointer": "/properties/clusterId",
      "x-cybercloud-soft-delete-days": 0,
      "x-cybercloud-purge-permission": "",
      "x-cybercloud-purge-protection-pointer": "",
      "x-cybercloud-meters": [
        {
          "meter": "Resources",
          "amountPointer": "",
          "fallback": 1,
          "expression": "1",
          "reads": []
        },
        {
          "meter": "StorageGb",
          "amountPointer": "",
          "fallback": null,
          "expression": "home.size, in GiB",
          "reads": [
            "/properties/home/size"
          ]
        }
      ]
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Terminal/consoles/{resourceName}/connect": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Terminal_consoles_Connect",
        "summary": "connect a CyberCloud.Terminal/consoles.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles.ConnectResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "connect",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Terminal/consoles",
      "x-cybercloud-action": "connect"
    },
    "/tenants/{tenantId}/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/CyberCloud.Terminal/consoles/{resourceName}/terminate": {
      "parameters": [
        {
          "$ref": "#/components/parameters/TenantId"
        },
        {
          "$ref": "#/components/parameters/SubscriptionId"
        },
        {
          "$ref": "#/components/parameters/ResourceGroupName"
        },
        {
          "$ref": "#/components/parameters/ResourceName"
        },
        {
          "$ref": "#/components/parameters/ApiVersion"
        }
      ],
      "post": {
        "operationId": "CyberCloud_Terminal_consoles_Terminate",
        "summary": "terminate a CyberCloud.Terminal/consoles.",
        "description": "An action never creates: a POST to a name that does not exist is a 404 — docs/plan/08 § The write path, end to end.",
        "responses": {
          "200": {
            "description": "The action's result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CyberCloud.Terminal.consoles.TerminateResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "412": {
            "$ref": "#/components/responses/PreconditionFailed"
          },
          "429": {
            "$ref": "#/components/responses/TooManyRequests"
          },
          "500": {
            "$ref": "#/components/responses/InternalServerError"
          },
          "default": {
            "$ref": "#/components/responses/InternalServerError"
          }
        },
        "x-cybercloud-permission": "connect",
        "x-cybercloud-secret": false,
        "x-cybercloud-long-running": false
      },
      "x-cybercloud-resource-type": "CyberCloud.Terminal/consoles",
      "x-cybercloud-action": "terminate"
    }
  },
  "components": {
    "parameters": {
      "ApiVersion": {
        "name": "api-version",
        "in": "query",
        "required": true,
        "description": "Required on every request. Missing is a 400 naming the current version. There is no 'latest' and no '-preview': a version is a date and is immutable — docs/plan/10 § API versioning.",
        "schema": {
          "type": "string",
          "enum": [
            "2026-08-01"
          ]
        }
      },
      "OperationId": {
        "name": "operationId",
        "in": "path",
        "required": true,
        "description": "The operation, from the Azure-AsyncOperation header of a 202.",
        "schema": {
          "type": "string",
          "format": "uuid"
        }
      },
      "ResourceGroupName": {
        "name": "resourceGroupName",
        "in": "path",
        "required": true,
        "description": "The resource group — the lifecycle boundary. docs/plan/06 § The hierarchy.",
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 63,
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$"
        }
      },
      "ResourceName": {
        "name": "resourceName",
        "in": "path",
        "required": true,
        "description": "The resource's name within its group.",
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 63,
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$"
        }
      },
      "SubscriptionId": {
        "name": "subscriptionId",
        "in": "path",
        "required": true,
        "description": "The subscription — the billing and quota boundary. docs/plan/06 § The hierarchy. Hyphenated 'D' form; braced, parenthesised and bare-hex forms are rejected so that one resource has exactly one path.",
        "schema": {
          "type": "string",
          "format": "uuid"
        }
      },
      "TenantId": {
        "name": "tenantId",
        "in": "path",
        "required": true,
        "description": "The owning tenant. docs/plan/06 § The hierarchy. Hyphenated 'D' form; braced, parenthesised and bare-hex forms are rejected so that one resource has exactly one path.",
        "schema": {
          "type": "string",
          "format": "uuid"
        }
      }
    },
    "responses": {
      "BadRequest": {
        "description": "The request is malformed: an invalid body, an unknown property, a missing or unparseable api-version.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "InvalidApiVersion",
          "InvalidGrainKey",
          "InvalidRequestBody",
          "InvalidResourceId",
          "InvalidResourceName",
          "InvalidResourceType"
        ]
      },
      "Forbidden": {
        "description": "The caller can read the resource but may not perform this action — docs/plan/07 § The enforcement seam.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "AuthorizationFailed",
          "PolicyViolation",
          "TenantSuspended"
        ]
      },
      "NotFound": {
        "description": "The resource does not exist, or the caller may not read it. ⚠ The two are deliberately indistinguishable — docs/plan/07 § The enforcement seam.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "ResourceGroupNotFound",
          "ResourceNotFound",
          "SchemaInvalid",
          "SubscriptionNotFound",
          "TenantNotFound"
        ]
      },
      "Conflict": {
        "description": "The write conflicts: the name is taken, an operation is already running, or the scope is locked.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "Conflict",
          "OperationCanceled",
          "OperationInProgress",
          "ResourceAlreadyExists",
          "ResourceHasChildren",
          "ScopeLocked"
        ]
      },
      "PreconditionFailed": {
        "description": "An If-Match etag did not match — docs/plan/06 § Tags, locks. A conditional retry keys on this rather than on the 409.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "PreconditionFailed"
        ]
      },
      "TooManyRequests": {
        "description": "The caller is being rate limited, or a quota meter would be exceeded — docs/plan/10 § Rate limiting and docs/plan/06 § Quota.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "QuotaExceeded"
        ]
      },
      "InternalServerError": {
        "description": "An internal error. ⚠ Never carries exception detail: the correlation id is in the response header and the detail is in the trace — docs/plan/08 § Errors.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorResponse"
            }
          }
        },
        "x-cybercloud-error-codes": [
          "InternalError",
          "OperationTimeout",
          "ProvisioningFailed"
        ]
      }
    },
    "schemas": {
      "CyberCloud.Analytics.clickhouseClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Analytics/clickhouseClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cluster's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the ClickHouse cluster.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "keeperNodes": {
                "default": 3,
                "description": "Number of ClickHouse Keeper nodes. Keeper is a Raft quorum, so three is the smallest count that survives losing one and an even count tolerates no more failures than the odd count below it. One is offered for development and has no quorum at all.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether ClickHouse's own Prometheus endpoint is served on port 9363. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". ⚠ It makes the metrics exist; the object that scrapes them is not built — see conformance.yaml § owed.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "replicas": {
                "default": 2,
                "description": "Number of replicas per shard. This is the availability axis, and it only applies to tables the tenant creates as Replicated — the resource does not manage tables. Total server count is shards times replicas.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "shards": {
                "default": 1,
                "description": "Number of shards. This is the capacity and parallelism axis: a table's data is split across shards and a query fans out to all of them. ⚠ Resharding an existing table is not something the operator or this resource does, so growing this moves new data only.",
                "maximum": 10,
                "minimum": 1,
                "type": "integer"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per ClickHouse server, either by preset or explicitly. The Keeper nodes are sized by the platform and are not affected.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 8Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "m1.small",
                    "description": "A sizing preset from docs/plan/12. Analytics uses the m1 family, which is 1 vCPU to 8 GiB.",
                    "enum": [
                      "m1.2xlarge",
                      "m1.4xlarge",
                      "m1.large",
                      "m1.medium",
                      "m1.micro",
                      "m1.nano",
                      "m1.small",
                      "m1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume, per ClickHouse server.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name for the ClickHouse servers and the Keeper nodes. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "100Gi",
                    "description": "Data volume size per ClickHouse server, in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "25.3",
                "description": "ClickHouse version, which is also the ClickHouse Keeper version — the two share a release train and a wire protocol. Only long-term-support lines are offered; a new api-version is what adds a third.",
                "enum": [
                  "24.8",
                  "25.3"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "keeperNodes",
              "replicas",
              "shards",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Analytics/clickhouseClusters",
        "type": "object"
      },
      "CyberCloud.Analytics.clickhouseClusters.List": {
        "type": "object",
        "title": "CyberCloud.Analytics.clickhouseClusters.List",
        "description": "One page of CyberCloud.Analytics/clickhouseClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Analytics.clickhouseClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Analytics.clickhouseClusters.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Analytics/clickhouseClusters/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "clusterName": {
            "description": "The name to write in ON CLUSTER and in a Distributed table's first argument. Returned rather than left for the caller to guess, because it is the one platform-chosen string that reaches the tenant's SQL.",
            "type": "string"
          },
          "httpEndpoint": {
            "description": "The in-cluster HTTP endpoint, http://host:port. ⚠ No external address is returned, because there is none — see the cluster's own documentation on exposure.",
            "type": "string"
          },
          "nativeEndpoint": {
            "description": "The in-cluster native-protocol endpoint, host:port, for clickhouse-client and every driver that speaks the binary protocol.",
            "type": "string"
          },
          "password": {
            "description": "The password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "username": {
            "description": "The user the credential below belongs to.",
            "type": "string"
          }
        },
        "required": [
          "clusterName",
          "httpEndpoint",
          "nativeEndpoint",
          "password",
          "username"
        ],
        "title": "CyberCloud.Analytics.clickhouseClusters.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Cache.redis": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Cache/redis: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cache is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cache's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the RedisFailover.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "maxmemoryPolicy": {
                "default": "noeviction",
                "description": "What happens when the cache reaches its memory limit. noeviction returns an error to the writer rather than dropping a key somebody is relying on.",
                "enum": [
                  "noeviction",
                  "allkeys-lru",
                  "allkeys-lfu",
                  "allkeys-random",
                  "volatile-lru",
                  "volatile-lfu",
                  "volatile-random",
                  "volatile-ttl"
                ],
                "type": "string"
              },
              "mode": {
                "default": "Sentinel",
                "description": "Replication topology. Sentinel is the only one this operator implements, and a client written against it is not portable to a sharded deployment, so it may not change after create.",
                "enum": [
                  "Sentinel"
                ],
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the operator runs a metrics exporter beside every Valkey and every Sentinel pod.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "persistence": {
                "additionalProperties": false,
                "description": "What survives a restart. None of the three settings makes a cache a database.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "fsync": {
                    "default": "everysec",
                    "description": "How often the append-only file reaches the disk. Read only when the mode is AOF; everysec can lose the last second of writes.",
                    "enum": [
                      "always",
                      "everysec",
                      "no"
                    ],
                    "type": "string"
                  },
                  "mode": {
                    "default": "AOF",
                    "description": "None keeps nothing, RDB snapshots periodically, AOF appends every write.",
                    "enum": [
                      "None",
                      "RDB",
                      "AOF"
                    ],
                    "type": "string"
                  },
                  "size": {
                    "default": "8Gi",
                    "description": "Persistent volume size in Kubernetes quantity form. Unused when the mode is None, which keeps the data directory in memory.",
                    "example": "8Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "replicas": {
                "default": 3,
                "description": "Number of Valkey instances, including the primary. One is a single point of failure and is offered for development only.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset. It also sets maxmemory, so a cache evicts or refuses before the kernel kills the pod.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "m1.small",
                    "description": "A sizing preset from docs/plan/12. Caches use the m1 family, which is 1 vCPU to 8 GiB.",
                    "enum": [
                      "m1.nano",
                      "m1.micro",
                      "m1.small",
                      "m1.medium",
                      "m1.large",
                      "m1.xlarge",
                      "m1.2xlarge",
                      "m1.4xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "version": {
                "default": "8",
                "description": "Major Valkey version. Minor upgrades are applied automatically in the maintenance window.",
                "enum": [
                  "7",
                  "8"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "replicas",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Cache/redis",
        "type": "object"
      },
      "CyberCloud.Cache.redis.List": {
        "type": "object",
        "title": "CyberCloud.Cache.redis.List",
        "description": "One page of CyberCloud.Cache/redis. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Cache.redis"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Cache.redis.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Cache/redis/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "host": {
            "description": "The in-cluster DNS name of the Sentinel service. ⚠ Not the primary's own address — a client asks Sentinel which member is the primary, because that answer changes at every failover.",
            "type": "string"
          },
          "masterName": {
            "description": "The Sentinel master group to ask for. Every client library takes this as a separate argument from the host.",
            "type": "string"
          },
          "password": {
            "description": "The requirepass value, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "port": {
            "description": "The Sentinel TCP port.",
            "maximum": 65535,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "host",
          "masterName",
          "password",
          "port"
        ],
        "title": "CyberCloud.Cache.redis.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Communication.services": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Communication/services: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the service is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The service's own settings.",
            "properties": {
              "defaultLocale": {
                "default": "",
                "description": "The locale a send is rendered in when the request names none — a BCP 47 tag such as cs-CZ. Empty falls back to en, then to whichever body the template has first.",
                "example": "en-GB",
                "maxLength": 35,
                "pattern": "^(?:([a-z]{2,3}(-[A-Za-z0-9]{2,8})*)?)$",
                "type": "string"
              }
            },
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Communication/services",
        "type": "object"
      },
      "CyberCloud.Communication.services.CheckSuppressionRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Communication/services/checkSuppression.",
        "properties": {
          "channel": {
            "description": "The channel the address would be sent on.",
            "enum": [
              "sms",
              "whatsapp",
              "email",
              "push",
              "voice"
            ],
            "type": "string"
          },
          "destination": {
            "description": "The address, in any spelling.",
            "maxLength": 320,
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "channel",
          "destination"
        ],
        "title": "CyberCloud.Communication.services.CheckSuppressionRequest",
        "type": "object"
      },
      "CyberCloud.Communication.services.CheckSuppressionResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Communication/services/checkSuppression returns.",
        "properties": {
          "note": {
            "description": "The carrier's, the recipient's or the operator's words.",
            "type": "string"
          },
          "reason": {
            "description": "Why, when it is: hardBounce, complaint, optOut or manualBlock.",
            "type": "string"
          },
          "suppressed": {
            "description": "Whether a send to it would be refused.",
            "type": "boolean"
          },
          "suppressedAt": {
            "description": "When it was suppressed.",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "suppressed"
        ],
        "title": "CyberCloud.Communication.services.CheckSuppressionResponse",
        "type": "object"
      },
      "CyberCloud.Communication.services.List": {
        "type": "object",
        "title": "CyberCloud.Communication.services.List",
        "description": "One page of CyberCloud.Communication/services. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Communication.services"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Communication.services.ListSuppressionsRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Communication/services/listSuppressions.",
        "properties": {
          "channel": {
            "default": "",
            "description": "One of sms, whatsapp, email, push or voice, or empty for every channel.",
            "maxLength": 16,
            "type": "string"
          }
        },
        "title": "CyberCloud.Communication.services.ListSuppressionsRequest",
        "type": "object"
      },
      "CyberCloud.Communication.services.ListSuppressionsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Communication/services/listSuppressions returns.",
        "properties": {
          "count": {
            "description": "How many entries are on the list.",
            "type": "integer"
          },
          "entries": {
            "description": "Every entry, oldest first, one line each: '{channel} {destination} {reason} {suppressedAt}: {note}'.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "count",
          "entries"
        ],
        "title": "CyberCloud.Communication.services.ListSuppressionsResponse",
        "type": "object"
      },
      "CyberCloud.Communication.services.SendRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Communication/services/send.",
        "properties": {
          "arguments": {
            "description": "The template's arguments, one name=value per element.",
            "example": [
              "code=482913"
            ],
            "items": {
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*=[\\s\\S]*)$",
              "type": "string"
            },
            "type": "array"
          },
          "body": {
            "default": "",
            "description": "The message text, when no template is named.",
            "maxLength": 65536,
            "type": "string"
          },
          "channel": {
            "description": "Which channel to send on.",
            "enum": [
              "sms",
              "whatsapp",
              "email",
              "push",
              "voice"
            ],
            "type": "string"
          },
          "idempotencyKey": {
            "description": "The caller's key for this message. A retry carrying the same key returns the message already sent and calls no carrier; derive it from the thing being notified about, never from the attempt.",
            "example": "invoice-2026-0042-issued",
            "maxLength": 200,
            "minLength": 1,
            "type": "string"
          },
          "locale": {
            "default": "",
            "description": "The locale to render in, or empty for the service's default.",
            "maxLength": 35,
            "pattern": "^(?:([a-z]{2,3}(-[A-Za-z0-9]{2,8})*)?)$",
            "type": "string"
          },
          "template": {
            "default": "",
            "description": "The name of a template under this service to render, or empty to send body as written. WhatsApp requires one.",
            "maxLength": 63,
            "type": "string"
          },
          "templateVersion": {
            "default": 0,
            "description": "Which version of the template, or 0 for the newest one a send may use.",
            "minimum": 0,
            "type": "integer"
          },
          "to": {
            "description": "The recipient — an E.164 number for sms, whatsapp and voice, an address for email, a device token for push.",
            "example": "+420777123456",
            "maxLength": 320,
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "channel",
          "idempotencyKey",
          "to"
        ],
        "title": "CyberCloud.Communication.services.SendRequest",
        "type": "object"
      },
      "CyberCloud.Communication.services.SendResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Communication/services/send returns.",
        "properties": {
          "channel": {
            "description": "The channel it went on.",
            "enum": [
              "sms",
              "whatsapp",
              "email",
              "push",
              "voice"
            ],
            "type": "string"
          },
          "cost": {
            "description": "What the carrier charged, once it said.",
            "type": "number"
          },
          "currency": {
            "description": "The currency of cost.",
            "type": "string"
          },
          "detail": {
            "description": "The last thing the platform or the carrier said about it.",
            "type": "string"
          },
          "dispatchedAt": {
            "description": "When a carrier accepted it. Absent until then.",
            "format": "date-time",
            "type": "string"
          },
          "messageId": {
            "description": "The platform's id for the message.",
            "format": "uuid",
            "type": "string"
          },
          "provider": {
            "description": "Which carrier implementation served it.",
            "type": "string"
          },
          "providerMessageId": {
            "description": "The carrier's own id, once it has one.",
            "type": "string"
          },
          "queuedAt": {
            "description": "When the platform accepted it.",
            "format": "date-time",
            "type": "string"
          },
          "receiptCount": {
            "description": "How many delivery receipts have arrived.",
            "type": "integer"
          },
          "receipts": {
            "description": "Every delivery receipt, oldest first, one line each: '{occurredAt} {status} {providerStatus}: {detail}'.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "settledAt": {
            "description": "When it was delivered, failed or refused. Absent until then.",
            "format": "date-time",
            "type": "string"
          },
          "status": {
            "description": "Where the message is in its life.",
            "enum": [
              "queued",
              "dispatched",
              "delivered",
              "failed",
              "refused"
            ],
            "type": "string"
          },
          "to": {
            "description": "The recipient, normalized.",
            "type": "string"
          }
        },
        "required": [
          "channel",
          "messageId",
          "queuedAt",
          "receiptCount",
          "status",
          "to"
        ],
        "title": "CyberCloud.Communication.services.SendResponse",
        "type": "object"
      },
      "CyberCloud.Communication.services.StatusRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Communication/services/status.",
        "properties": {
          "idempotencyKey": {
            "description": "The key the message was sent under.",
            "maxLength": 200,
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "idempotencyKey"
        ],
        "title": "CyberCloud.Communication.services.StatusRequest",
        "type": "object"
      },
      "CyberCloud.Communication.services.StatusResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Communication/services/status returns.",
        "properties": {
          "channel": {
            "description": "The channel it went on.",
            "enum": [
              "sms",
              "whatsapp",
              "email",
              "push",
              "voice"
            ],
            "type": "string"
          },
          "cost": {
            "description": "What the carrier charged, once it said.",
            "type": "number"
          },
          "currency": {
            "description": "The currency of cost.",
            "type": "string"
          },
          "detail": {
            "description": "The last thing the platform or the carrier said about it.",
            "type": "string"
          },
          "dispatchedAt": {
            "description": "When a carrier accepted it. Absent until then.",
            "format": "date-time",
            "type": "string"
          },
          "messageId": {
            "description": "The platform's id for the message.",
            "format": "uuid",
            "type": "string"
          },
          "provider": {
            "description": "Which carrier implementation served it.",
            "type": "string"
          },
          "providerMessageId": {
            "description": "The carrier's own id, once it has one.",
            "type": "string"
          },
          "queuedAt": {
            "description": "When the platform accepted it.",
            "format": "date-time",
            "type": "string"
          },
          "receiptCount": {
            "description": "How many delivery receipts have arrived.",
            "type": "integer"
          },
          "receipts": {
            "description": "Every delivery receipt, oldest first, one line each: '{occurredAt} {status} {providerStatus}: {detail}'.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "settledAt": {
            "description": "When it was delivered, failed or refused. Absent until then.",
            "format": "date-time",
            "type": "string"
          },
          "status": {
            "description": "Where the message is in its life.",
            "enum": [
              "queued",
              "dispatched",
              "delivered",
              "failed",
              "refused"
            ],
            "type": "string"
          },
          "to": {
            "description": "The recipient, normalized.",
            "type": "string"
          }
        },
        "required": [
          "channel",
          "messageId",
          "queuedAt",
          "receiptCount",
          "status",
          "to"
        ],
        "title": "CyberCloud.Communication.services.StatusResponse",
        "type": "object"
      },
      "CyberCloud.Communication.services.channels": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Communication/services/channels: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the channel is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The channel's own settings.",
            "properties": {
              "account": {
                "default": "platform",
                "description": "Whose carrier account pays. platform is marked up and needs no setup; tenant is the tenant's own contract, reached through accountRef and authRef.",
                "enum": [
                  "platform",
                  "tenant"
                ],
                "type": "string"
              },
              "accountRef": {
                "default": "",
                "description": "For account: tenant — the vault handle of the account identifier (a Twilio account SID, a Meta business account id), as path#field.",
                "maxLength": 512,
                "pattern": "^(?:([^#@\\s]+#[^#@\\s]+(@[^#@\\s]+)?)?)$",
                "type": "string",
                "x-cybercloud-widget": "secret-ref"
              },
              "authRef": {
                "default": "",
                "description": "For account: tenant — the vault handle of the authenticating value (an auth token, a bearer, an access secret), as path#field.",
                "maxLength": 512,
                "pattern": "^(?:([^#@\\s]+#[^#@\\s]+(@[^#@\\s]+)?)?)$",
                "type": "string",
                "x-cybercloud-widget": "secret-ref"
              },
              "enabled": {
                "default": true,
                "description": "Whether sending on this channel is allowed at all. The kill switch: turning it off keeps the configuration and refuses every send.",
                "type": "boolean"
              },
              "estimatedUnitCost": {
                "default": 0,
                "description": "What one message is expected to cost, in currency, reserved before dispatch. Set it to the most expensive destination the channel sends to; an estimate that is too low turns the spend limit into a suggestion.",
                "example": 0.08,
                "minimum": 0,
                "type": "number"
              },
              "kind": {
                "description": "Which channel this configures. One service holds one configuration per kind.",
                "enum": [
                  "sms",
                  "whatsapp",
                  "email",
                  "push",
                  "voice"
                ],
                "example": "email",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "limits": {
                "additionalProperties": false,
                "description": "What the channel may send and spend per UTC day.",
                "properties": {
                  "currency": {
                    "default": "EUR",
                    "description": "ISO 4217, for the spend limit and the refusal that names it.",
                    "pattern": "^(?:[A-Z]{3})$",
                    "type": "string"
                  },
                  "maxMessagesPerDay": {
                    "default": 0,
                    "description": "The most messages this channel dispatches in one UTC day. Zero — the default — means none: a channel with no limit is a channel that cannot send, on purpose.",
                    "example": 1000,
                    "minimum": 0,
                    "type": "integer"
                  },
                  "maxSpendPerDay": {
                    "default": 0,
                    "description": "The most this channel spends in one UTC day, in currency. Zero means none.",
                    "example": 50,
                    "minimum": 0,
                    "type": "number"
                  }
                },
                "type": "object"
              },
              "provider": {
                "default": "",
                "description": "Which carrier implementation serves it — twilio, meta-cloud, ses — or empty for the one the platform registers for the kind.",
                "maxLength": 64,
                "pattern": "^(?:([a-z0-9]([a-z0-9-]{0,62}[a-z0-9])?)?)$",
                "type": "string"
              },
              "signingRef": {
                "default": "",
                "description": "The vault handle of the carrier's webhook-signing value, when it signs its callbacks. A receipt that cannot be verified is data from the internet.",
                "maxLength": 512,
                "pattern": "^(?:([^#@\\s]+#[^#@\\s]+(@[^#@\\s]+)?)?)$",
                "type": "string",
                "x-cybercloud-widget": "secret-ref"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Communication/services/channels",
        "type": "object"
      },
      "CyberCloud.Communication.services.channels.List": {
        "type": "object",
        "title": "CyberCloud.Communication.services.channels.List",
        "description": "One page of CyberCloud.Communication/services/channels. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Communication.services.channels"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Communication.services.suppressions": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Communication/services/suppressions: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the suppression is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The suppression's own settings.",
            "properties": {
              "channel": {
                "description": "The channel the address is blocked on. Suppression is per channel: an email bounce says nothing about a phone number.",
                "enum": [
                  "sms",
                  "whatsapp",
                  "email",
                  "push",
                  "voice"
                ],
                "example": "email",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "destination": {
                "description": "The address, in any spelling. Normalized before it is stored, so two spellings of one address are one entry.",
                "example": "someone@example.com",
                "maxLength": 320,
                "minLength": 1,
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "note": {
                "default": "",
                "description": "Why, in the tenant's words. What a support case reads.",
                "example": "Asked us to stop by phone, 2026-09-01",
                "maxLength": 512,
                "type": "string"
              }
            },
            "required": [
              "channel",
              "destination"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Communication/services/suppressions",
        "type": "object"
      },
      "CyberCloud.Communication.services.suppressions.List": {
        "type": "object",
        "title": "CyberCloud.Communication.services.suppressions.List",
        "description": "One page of CyberCloud.Communication/services/suppressions. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Communication.services.suppressions"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Communication.services.templates": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Communication/services/templates: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the template is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The template's own settings.",
            "properties": {
              "body": {
                "description": "The message text. A {name} is replaced by the argument of that name, left to right, and a substituted value is never re-scanned.",
                "example": "Your verification code is {code}. It expires in {minutes} minutes.",
                "maxLength": 65536,
                "minLength": 1,
                "type": "string"
              },
              "channel": {
                "description": "Which channel the body is written for. A WhatsApp body is not an email body, and the channel decides whether carrier pre-approval is consulted at all.",
                "enum": [
                  "sms",
                  "whatsapp",
                  "email",
                  "push",
                  "voice"
                ],
                "example": "email",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "locale": {
                "default": "en",
                "description": "The BCP 47 tag the body is written in. One locale per template; a second language is a second template.",
                "maxLength": 35,
                "pattern": "^(?:[a-z]{2,3}(-[A-Za-z0-9]{2,8})*)$",
                "type": "string"
              },
              "optionalVariables": {
                "description": "The placeholders a send may supply. An unsupplied one is left as written, so a tenant testing the template sees it.",
                "example": [
                  "minutes"
                ],
                "items": {
                  "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*)$",
                  "type": "string"
                },
                "type": "array"
              },
              "subject": {
                "default": "",
                "description": "The subject line, for channels that have one. Placeholders are substituted here too.",
                "example": "Your code is {code}",
                "maxLength": 998,
                "type": "string"
              },
              "variables": {
                "description": "The placeholders a send must supply. A send missing one is refused before any carrier is called.",
                "example": [
                  "code"
                ],
                "items": {
                  "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*)$",
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "body",
              "channel"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Communication/services/templates",
        "type": "object"
      },
      "CyberCloud.Communication.services.templates.List": {
        "type": "object",
        "title": "CyberCloud.Communication.services.templates.List",
        "description": "One page of CyberCloud.Communication/services/templates. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Communication.services.templates"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Communication.services.templates.RenderRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Communication/services/templates/render.",
        "properties": {
          "arguments": {
            "description": "The arguments, one name=value per element. A missing required variable is refused, naming every missing one at once.",
            "example": [
              "code=482913"
            ],
            "items": {
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*=[\\s\\S]*)$",
              "type": "string"
            },
            "type": "array"
          }
        },
        "title": "CyberCloud.Communication.services.templates.RenderRequest",
        "type": "object"
      },
      "CyberCloud.Communication.services.templates.RenderResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Communication/services/templates/render returns.",
        "properties": {
          "body": {
            "description": "The body, substituted.",
            "type": "string"
          },
          "locale": {
            "description": "The locale it was rendered in.",
            "type": "string"
          },
          "subject": {
            "description": "The subject, substituted.",
            "type": "string"
          }
        },
        "required": [
          "body",
          "locale",
          "subject"
        ],
        "title": "CyberCloud.Communication.services.templates.RenderResponse",
        "type": "object"
      },
      "CyberCloud.ContainerRegistry.feeds": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.ContainerRegistry/feeds: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the feed is billed in and served from.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The feed's own settings.",
            "properties": {
              "description": {
                "description": "What the feed is for, shown in the portal beside its name.",
                "maxLength": 256,
                "type": "string"
              },
              "kind": {
                "default": "nuget",
                "description": "Which protocol the feed speaks: nuget (the v3 API), npm (the registry API) or maven (the repository layout). Immutable, because the three have three versioning models.",
                "enum": [
                  "nuget",
                  "npm",
                  "maven"
                ],
                "example": "nuget",
                "type": "string",
                "x-cybercloud-immutable": true
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.ContainerRegistry/feeds",
        "type": "object"
      },
      "CyberCloud.ContainerRegistry.feeds.List": {
        "type": "object",
        "title": "CyberCloud.ContainerRegistry.feeds.List",
        "description": "One page of CyberCloud.ContainerRegistry/feeds. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.feeds"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.ContainerRegistry.registries": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.ContainerRegistry/registries: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the registry is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The registry's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the registry.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether Harbor's core exports Prometheus metrics and a PodMonitor selects them. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". Turning it off removes the metrics port as well as the scrape, so nothing is left listening on an unscraped address.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "purgeProtection": {
                "default": false,
                "description": "Whether this registry may be destroyed before its seven-day recovery window is out. Once true it stays true for the rest of the registry's life, and a purge is refused while it is set — a flag whose holder can clear it and then purge is one round-trip of protection.",
                "type": "boolean"
              },
              "replicas": {
                "default": 2,
                "description": "How many replicas of each stateless component — the API core, the web portal and the job service — run. Two is the smallest count that survives a node drain. The registry itself, the database and Redis each own a volume and run one replica whatever this says.",
                "maximum": 10,
                "minimum": 1,
                "type": "integer"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory for the registry pod, either by preset or explicitly. The other five components are sized by the platform and are not affected.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "s1.small",
                    "description": "A sizing preset from docs/plan/12. The registry uses the s1 family, which is 1 vCPU to 4 GiB.",
                    "enum": [
                      "s1.2xlarge",
                      "s1.4xlarge",
                      "s1.large",
                      "s1.medium",
                      "s1.micro",
                      "s1.nano",
                      "s1.small",
                      "s1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "Where the image layers live.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name for the image volume. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "100Gi",
                    "description": "Image storage, in Kubernetes quantity form. Grows online; never shrinks. ⚠ This is a filesystem volume rather than the tenant's object-storage bucket, which is what docs/plan/13 asks for and what the platform cannot yet give it — see the registry's own documentation.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "2.15",
                "description": "Harbor minor version. The platform pins the patch, because Harbor publishes image tags per patch and a bare minor resolves to nothing. A minor leaving support is a portal notice and a 120-day window — docs/plan/12 § Cross-cutting decisions.",
                "enum": [
                  "2.14",
                  "2.15"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "replicas",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.ContainerRegistry/registries",
        "type": "object"
      },
      "CyberCloud.ContainerRegistry.registries.List": {
        "type": "object",
        "title": "CyberCloud.ContainerRegistry.registries.List",
        "description": "One page of CyberCloud.ContainerRegistry/registries. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.ContainerRegistry.registries"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.ContainerRegistry.registries.ListCredentialsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.ContainerRegistry/registries/listCredentials returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "endpoint": {
            "description": "The in-cluster registry endpoint — what docker login takes. ⚠ http rather than https, and no external address is returned, because there is none.",
            "type": "string"
          },
          "password": {
            "description": "The administrator's password, read from the tenant's Vault for this call only. Minted at create; never derived from the resource id.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "portalUrl": {
            "description": "Harbor's web UI, which is a different Service from the API endpoint because this row renders no single front door.",
            "type": "string"
          },
          "username": {
            "description": "The administrator's username. Always admin — Harbor seeds that row and nothing renames it.",
            "type": "string"
          }
        },
        "required": [
          "endpoint",
          "password",
          "portalUrl",
          "username"
        ],
        "title": "CyberCloud.ContainerRegistry.registries.ListCredentialsResponse",
        "type": "object"
      },
      "CyberCloud.ContainerService.connectedClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.ContainerService/connectedClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in. ⚠ Where the cluster physically is is the tenant's business; this is the region whose gateway the agent dials and whose silos hold the connection.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The connected cluster's own settings.",
            "properties": {
              "distribution": {
                "default": "other",
                "description": "What the cluster runs — k3s, kubeadm, OpenShift, a hosted service. Informational: the agent works against any conformant API server and nothing here changes what it does.",
                "example": "k3s",
                "maxLength": 32,
                "minLength": 1,
                "type": "string"
              },
              "heartbeatSeconds": {
                "default": 15,
                "description": "How often the agent reports in. Read when listInstallCommand is called: the command passes it to the chart and the platform repeats it in the welcome the agent adopts on every connection. A change after that takes effect on the next listInstallCommand — no re-install; the running agent adopts it on its next connection. ⚠ The platform calls a cluster Degraded after ninety seconds without a heartbeat (docs/plan/09 § Cluster connections), so a value above thirty leaves fewer than three chances for a packet to arrive.",
                "maximum": 60,
                "minimum": 5,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.ContainerService/connectedClusters",
        "type": "object"
      },
      "CyberCloud.ContainerService.connectedClusters.List": {
        "type": "object",
        "title": "CyberCloud.ContainerService.connectedClusters.List",
        "description": "One page of CyberCloud.ContainerService/connectedClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.ContainerService.connectedClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.ContainerService.connectedClusters.ListInstallCommandResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.ContainerService/connectedClusters/listInstallCommand returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "chart": {
            "description": "The chart reference the command installs: the OCI reference this deployment publishes the agent chart under, or charts/agent — the path in a checkout of the CyberCloud repository — when it has not published one, in which case run the command from that checkout.",
            "type": "string"
          },
          "command": {
            "description": "The helm command to run against the cluster being connected, with the one-time token inline. Run it from a workstation with cluster-admin on that cluster; the platform needs nothing from the cluster's side.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "expiresAt": {
            "description": "When the token stops being accepted, RFC 3339. Twenty-four hours from the call; ask again for a fresh one.",
            "format": "date-time",
            "type": "string"
          },
          "token": {
            "description": "The one-time enrollment token, separately, for an install that does not use helm. It admits exactly one agent connection and is spent by it.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "tunnelEndpoint": {
            "description": "The WebSocket URL the agent dials — wss://{gateway}/agent/v1/tunnel. The cluster needs outbound HTTPS to it and nothing inbound.",
            "format": "uri",
            "type": "string"
          }
        },
        "required": [
          "chart",
          "command",
          "expiresAt",
          "token",
          "tunnelEndpoint"
        ],
        "title": "CyberCloud.ContainerService.connectedClusters.ListInstallCommandResponse",
        "type": "object"
      },
      "CyberCloud.ContainerService.managedClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.ContainerService/managedClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cluster's own settings.",
            "properties": {
              "clusterId": {
                "description": "The management cluster the control plane runs in. ⚠ This is not the cluster being created: it is the cluster whose API server accepts the Cluster API objects that create one.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "controlPlane": {
                "additionalProperties": false,
                "description": "The control plane, which runs as pods in the management cluster.",
                "properties": {
                  "replicas": {
                    "default": 2,
                    "description": "How many copies of the API server, controller manager and scheduler to run. Two survives a node failure; one is offered for development. ⚠ Unlike an etcd quorum this is a plain replica count — the datastore is separate and is shared — so an even number is not the mistake it would be on a Raft member set.",
                    "maximum": 5,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "replicas"
                ],
                "type": "object"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the control plane's own metrics endpoints are scraped. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". ⚠ It covers the control plane, which runs in the management cluster. Nothing scrapes inside the cluster being created; that needs an agent in the bundle.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "network": {
                "additionalProperties": false,
                "description": "The address space of the cluster being created.",
                "properties": {
                  "podCidr": {
                    "default": "10.244.0.0/16",
                    "description": "The CIDR block pods are addressed from. ⚠ It must not overlap the management cluster's own pod or service range, and nothing checks that — an overlap produces a cluster whose nodes route the platform's addresses to themselves.",
                    "example": "10.244.0.0/16",
                    "pattern": "^(?:((25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]?\\d)\\.){3}(25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]?\\d)/([0-9]|[12]\\d|3[0-2]))$",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "cidr"
                  },
                  "serviceCidr": {
                    "default": "10.96.0.0/12",
                    "description": "The CIDR block Service cluster IPs are allocated from. The same overlap warning applies.",
                    "example": "10.96.0.0/12",
                    "pattern": "^(?:((25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]?\\d)\\.){3}(25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]?\\d)/([0-9]|[12]\\d|3[0-2]))$",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "cidr"
                  }
                },
                "required": [
                  "podCidr",
                  "serviceCidr"
                ],
                "type": "object"
              },
              "version": {
                "default": "1.33",
                "description": "The Kubernetes minor version of the control plane. The patch level is the platform's. ⚠ Upgrade the control plane before the node pools and by at most one minor at a time; a node pool may run up to three minors behind and may never run ahead. Neither rule is enforced by this API today — an illegal pair is refused by the cluster's own admission, after the create was accepted.",
                "enum": [
                  "1.32",
                  "1.33"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.ContainerService/managedClusters",
        "type": "object"
      },
      "CyberCloud.ContainerService.managedClusters.List": {
        "type": "object",
        "title": "CyberCloud.ContainerService.managedClusters.List",
        "description": "One page of CyberCloud.ContainerService/managedClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.ContainerService.managedClusters.ListCredentialsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.ContainerService/managedClusters/listCredentials returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "apiServerEndpoint": {
            "description": "The URL the kubeconfig points at, https://host:port. ⚠ It is an address inside the management cluster and is not routable from anywhere else — the control plane is deliberately not exposed, because there is no upstream field to attach a CIDR allow-list to. Returned separately so that a caller does not have to parse YAML to discover that.",
            "format": "uri",
            "type": "string"
          },
          "expiresAt": {
            "description": "When the credential stops working, RFC 3339. ⚠ Returned because a credential with no stated expiry is one every caller will paste into CI and never rotate — docs/plan/13 makes \"a kubectl credential that expires\" part of what a tenant is buying.",
            "format": "date-time",
            "type": "string"
          },
          "kubeconfig": {
            "description": "A complete kubeconfig for the cluster, YAML. ⚠ docs/plan/13 requires this to be short-lived and scoped rather than the cluster's admin credential; what Cluster API generates is the admin one, and narrowing it needs a certificate request against the cluster this platform cannot yet reach.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          }
        },
        "required": [
          "apiServerEndpoint",
          "expiresAt",
          "kubeconfig"
        ],
        "title": "CyberCloud.ContainerService.managedClusters.ListCredentialsResponse",
        "type": "object"
      },
      "CyberCloud.ContainerService.managedClusters.agentPools": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.ContainerService/managedClusters/agentPools: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the pool is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The pool's own settings.",
            "properties": {
              "autoscale": {
                "additionalProperties": false,
                "description": "Whether a cluster-autoscaler may resize the pool.",
                "properties": {
                  "enabled": {
                    "default": false,
                    "description": "Whether the pool carries autoscaler bounds. Off by default. ⚠ Turning it on changes what the pool costs: quota is then reserved against maxCount, because a pool that may grow to twenty machines has to have twenty machines' worth of headroom to grow into.",
                    "type": "boolean"
                  },
                  "maxCount": {
                    "default": 3,
                    "description": "The largest the autoscaler may grow the pool to, and what quota is reserved against while autoscaling is on. ⚠ Nothing checks that it is at least minCount — that is a relation between two properties of one body, which the schema validates nothing about.",
                    "maximum": 100,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "minCount": {
                    "default": 1,
                    "description": "The smallest the autoscaler may shrink the pool to. Ignored when autoscaling is off.",
                    "maximum": 100,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The management cluster the machine objects are applied to. Must be the one the cluster is in — nothing checks that, and a pool placed elsewhere produces a MachineDeployment naming a Cluster that is not there, which Cluster API accepts and never reconciles.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "count": {
                "default": 3,
                "description": "How many worker VMs the pool runs. ⚠ When autoscaling is on this is the starting size and the autoscaler moves it; quota is reserved against the maximum in that case, not against this.",
                "maximum": 100,
                "minimum": 1,
                "type": "integer"
              },
              "osDiskSize": {
                "default": "60Gi",
                "description": "The root volume of each VM, in Kubernetes quantity form. It holds the operating system, the container images and every writable layer, so a pool running large images needs more of it than a pool running small ones.",
                "example": "60Gi",
                "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "size": {
                "default": "s1.small",
                "description": "The VM size, from the platform's sizing catalogue. Kubernetes nodes use the s1 family, which is 1 vCPU to 4 GiB. ⚠ Immutable: a Cluster API machine template cannot be resized in place, so changing this would mean replacing every VM in the pool, which is a different operation from the one a PUT looks like.",
                "enum": [
                  "s1.2xlarge",
                  "s1.4xlarge",
                  "s1.large",
                  "s1.medium",
                  "s1.micro",
                  "s1.nano",
                  "s1.small",
                  "s1.xlarge"
                ],
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cozy-preset"
              },
              "upgrade": {
                "additionalProperties": false,
                "description": "How machines are replaced when the pool changes.",
                "properties": {
                  "maxSurge": {
                    "default": 1,
                    "description": "How many extra machines may exist during a rolling replacement. One means a new VM boots and joins before an old one is removed, which is why it is the default: it costs one machine's capacity and loses none.",
                    "maximum": 10,
                    "minimum": 0,
                    "type": "integer"
                  },
                  "maxUnavailable": {
                    "default": 0,
                    "description": "How many machines may be missing during a rolling replacement. Zero with a surge of one is the safe pair; raising it is faster and reduces the pool's capacity while it runs. ⚠ Both being zero would make an upgrade unable to start, and nothing refuses that pair.",
                    "maximum": 10,
                    "minimum": 0,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "version": {
                "default": "1.33",
                "description": "The Kubernetes minor version the nodes run. ⚠ It may be up to three minors behind the cluster's control plane and may never be ahead of it. This API does not check that — the cluster's version is a different resource — so an illegal pair is accepted here and produces nodes that join and then misbehave.",
                "enum": [
                  "1.32",
                  "1.33"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "count",
              "osDiskSize",
              "size",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.ContainerService/managedClusters/agentPools",
        "type": "object"
      },
      "CyberCloud.ContainerService.managedClusters.agentPools.List": {
        "type": "object",
        "title": "CyberCloud.ContainerService.managedClusters.agentPools.List",
        "description": "One page of CyberCloud.ContainerService/managedClusters/agentPools. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.ContainerService.managedClusters.agentPools"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.DBforMySQL.servers": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.DBforMySQL/servers: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the server is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The server's own settings.",
            "properties": {
              "bootstrap": {
                "additionalProperties": false,
                "description": "What exists in the database the moment it comes up.",
                "properties": {
                  "database": {
                    "default": "app",
                    "description": "Name of the application database created on first start. Lower case only, because on Linux a database is a directory and its name is therefore case-sensitive.",
                    "maxLength": 64,
                    "minLength": 1,
                    "pattern": "^(?:[a-z_][a-z0-9_]*)$",
                    "type": "string"
                  },
                  "username": {
                    "default": "app",
                    "description": "Account granted every privilege on the application database. Capped at MySQL's 32 characters rather than MariaDB's longer limit, because this row is sold as MySQL-compatible.",
                    "maxLength": 32,
                    "minLength": 1,
                    "pattern": "^(?:[a-z_][a-z0-9_]*)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the MariaDB objects.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "highAvailability": {
                "default": "Galera",
                "description": "Replication topology. Galera is three synchronous instances; None is a single instance and a single point of failure, offered for development only.",
                "enum": [
                  "None",
                  "Galera"
                ],
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the operator runs a mysqld-exporter beside the server.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "s1.small",
                    "description": "A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.",
                    "enum": [
                      "s1.nano",
                      "s1.micro",
                      "s1.small",
                      "s1.medium",
                      "s1.large",
                      "s1.xlarge",
                      "s1.2xlarge",
                      "s1.4xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume. Every instance gets its own.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "20Gi",
                    "description": "Data volume size in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "20Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "11.4",
                "description": "Major MariaDB version, by LTS series. Minor upgrades are applied automatically in the maintenance window.",
                "enum": [
                  "10.11",
                  "11.4",
                  "11.8"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.DBforMySQL/servers",
        "type": "object"
      },
      "CyberCloud.DBforMySQL.servers.List": {
        "type": "object",
        "title": "CyberCloud.DBforMySQL.servers.List",
        "description": "One page of CyberCloud.DBforMySQL/servers. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.DBforMySQL.servers"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.DBforMySQL.servers.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.DBforMySQL/servers/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "authenticationPlugin": {
            "description": "The authentication plugin a client must be configured for. ⚠ Not MySQL 8's caching_sha2_password default — see the supported-subset table. Returned rather than documented because a wrong default here is a connection that fails with a message about a plugin nobody chose.",
            "type": "string"
          },
          "database": {
            "description": "The application database.",
            "type": "string"
          },
          "host": {
            "description": "The in-cluster DNS name to connect to. ⚠ The primary Service's when high availability is on, so the topology is part of the connection string rather than hidden behind it.",
            "type": "string"
          },
          "password": {
            "description": "The application account's password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "port": {
            "description": "The TCP port.",
            "maximum": 65535,
            "minimum": 1,
            "type": "integer"
          },
          "username": {
            "description": "The application account.",
            "type": "string"
          }
        },
        "required": [
          "authenticationPlugin",
          "database",
          "host",
          "password",
          "port",
          "username"
        ],
        "title": "CyberCloud.DBforMySQL.servers.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.DBforPostgreSQL.servers": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.DBforPostgreSQL/servers: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the server is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The server's own settings.",
            "properties": {
              "backup": {
                "additionalProperties": false,
                "description": "Backup to the tenant's object store, using CloudNativePG's barman-cloud.",
                "properties": {
                  "destinationPath": {
                    "default": "",
                    "description": "Object-store URL for base backups and WAL, for example s3://tenant-bucket/postgres. Empty means the platform fills it in from the tenant's default bucket.",
                    "example": "s3://tenant-bucket/postgres",
                    "pattern": "^(?:(s3://[a-z0-9][a-z0-9.\\-]*[a-z0-9](/[^\\s]*)?)?)$",
                    "type": "string"
                  },
                  "enabled": {
                    "default": true,
                    "description": "Whether continuous backup and WAL archiving run.",
                    "type": "boolean"
                  },
                  "retentionDays": {
                    "default": 14,
                    "description": "How long base backups and WAL are kept. The point-in-time-recovery window is this number of days.",
                    "maximum": 365,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "bootstrap": {
                "additionalProperties": false,
                "description": "What exists in the database the moment it comes up.",
                "properties": {
                  "database": {
                    "default": "app",
                    "description": "Name of the application database created on first start.",
                    "maxLength": 63,
                    "minLength": 1,
                    "pattern": "^(?:[a-z_][a-z0-9_]*)$",
                    "type": "string"
                  },
                  "owner": {
                    "default": "app",
                    "description": "Role that owns the application database.",
                    "maxLength": 63,
                    "minLength": 1,
                    "pattern": "^(?:[a-z_][a-z0-9_]*)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the CloudNativePG objects.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "extensions": {
                "default": [],
                "description": "Extensions to install, from the platform allow-list. An arbitrary-extension escape hatch is a code-execution surface and is not offered.",
                "example": [
                  "pgvector"
                ],
                "items": {
                  "enum": [
                    "pgvector",
                    "postgis",
                    "pg_stat_statements",
                    "timescaledb"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether CloudNativePG emits a PodMonitor for the platform's metrics stack.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "pooling": {
                "additionalProperties": false,
                "description": "PgBouncer in front of the cluster.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether to run a connection pooler. On by default — a managed Postgres without one fails at the first serverless workload, and adding it later changes the connection string.",
                    "type": "boolean"
                  },
                  "instances": {
                    "default": 2,
                    "description": "Number of pooler pods.",
                    "maximum": 8,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "mode": {
                    "default": "transaction",
                    "description": "PgBouncer pooling mode. Transaction pooling is the useful one and breaks session-scoped features such as prepared statements and advisory locks.",
                    "enum": [
                      "session",
                      "transaction",
                      "statement"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "replicas": {
                "default": 2,
                "description": "Number of instances, including the primary. One is a single point of failure and is offered for development only.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "s1.small",
                    "description": "A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.",
                    "enum": [
                      "s1.nano",
                      "s1.micro",
                      "s1.small",
                      "s1.medium",
                      "s1.large",
                      "s1.xlarge",
                      "s1.2xlarge",
                      "s1.4xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "20Gi",
                    "description": "Data volume size in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "20Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  },
                  "walSize": {
                    "default": "",
                    "description": "Size of the separate write-ahead-log volume. Empty means the WAL shares the data volume.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "synchronousReplication": {
                "default": false,
                "description": "Whether commits wait for a replica. Costs write latency and removes the window in which a failover loses transactions.",
                "type": "boolean"
              },
              "version": {
                "default": "17",
                "description": "Major PostgreSQL version. Minor upgrades are applied automatically in the maintenance window.",
                "enum": [
                  "16",
                  "17",
                  "18"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "replicas",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.DBforPostgreSQL/servers",
        "type": "object"
      },
      "CyberCloud.DBforPostgreSQL.servers.List": {
        "type": "object",
        "title": "CyberCloud.DBforPostgreSQL.servers.List",
        "description": "One page of CyberCloud.DBforPostgreSQL/servers. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.DBforPostgreSQL.servers"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.DBforPostgreSQL.servers.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.DBforPostgreSQL/servers/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "database": {
            "description": "The application database.",
            "type": "string"
          },
          "host": {
            "description": "The in-cluster DNS name to connect to. ⚠ The pooler's when pooling is on, so turning pooling off later is a visible change to this value rather than a silent one.",
            "type": "string"
          },
          "password": {
            "description": "The owning role's password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "port": {
            "description": "The TCP port.",
            "maximum": 65535,
            "minimum": 1,
            "type": "integer"
          },
          "username": {
            "description": "The owning role.",
            "type": "string"
          }
        },
        "required": [
          "database",
          "host",
          "password",
          "port",
          "username"
        ],
        "title": "CyberCloud.DBforPostgreSQL.servers.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.DocumentDB.accounts": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.DocumentDB/accounts: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the account is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The account's own settings.",
            "properties": {
              "backup": {
                "additionalProperties": false,
                "description": "Backup to an object store, using CloudNativePG's barman-cloud. docs/plan/12: because it is PostgreSQL underneath, backup and point-in-time recovery are the operator's.",
                "properties": {
                  "destinationPath": {
                    "default": "",
                    "description": "Object-store URL for base backups and WAL, for example s3://tenant-bucket/documentdb. Empty means no backup configuration is rendered, whatever enabled says.",
                    "example": "s3://tenant-bucket/documentdb",
                    "pattern": "^(?:(s3://[a-z0-9][a-z0-9.\\-]*[a-z0-9](/[^\\s]*)?)?)$",
                    "type": "string"
                  },
                  "enabled": {
                    "default": false,
                    "description": "Whether continuous backup and WAL archiving run. ⚠ Off by default, and only because there is no destination to default to: turning it on without a destinationPath below renders no backup configuration at all rather than an empty one, so the two properties have to be set together.",
                    "type": "boolean"
                  },
                  "retentionDays": {
                    "default": 14,
                    "description": "How long base backups and WAL are kept. The point-in-time-recovery window is this number of days.",
                    "maximum": 365,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the PostgreSQL cluster and the FerretDB gateway.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "gateway": {
                "additionalProperties": false,
                "description": "The FerretDB gateway — what speaks the MongoDB wire protocol.",
                "properties": {
                  "replicas": {
                    "default": 2,
                    "description": "Number of FerretDB pods. The gateway is stateless — it translates and forwards, and every byte is in PostgreSQL — so this is a throughput and availability setting rather than a topology one.",
                    "maximum": 10,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "replicas"
                ],
                "type": "object"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether both halves of this service are scraped: CloudNativePG is asked for a PodMonitor over the PostgreSQL pods, and the platform writes one over the FerretDB pods because FerretDB has no operator to ask. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\".",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "postgres": {
                "additionalProperties": false,
                "description": "The CloudNativePG cluster the documents actually live in.",
                "properties": {
                  "instances": {
                    "default": 2,
                    "description": "Number of PostgreSQL instances, including the primary. One is a single point of failure and is offered for development only. Failover, replication and point-in-time recovery are CloudNativePG's, which is why this row costs 1.2 engineer-months rather than a rebuild of them.",
                    "maximum": 5,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "instances"
                ],
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per PostgreSQL instance, either by preset or explicitly. The FerretDB gateway pods are sized by the platform and are not affected.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "s1.small",
                    "description": "A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.",
                    "enum": [
                      "s1.2xlarge",
                      "s1.4xlarge",
                      "s1.large",
                      "s1.medium",
                      "s1.micro",
                      "s1.nano",
                      "s1.small",
                      "s1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume, per PostgreSQL instance.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name for the PostgreSQL volumes. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "20Gi",
                    "description": "Data volume size per PostgreSQL instance, in Kubernetes quantity form. Every instance carries a full copy, so raw consumption is this times the instance count. Grows online; never shrinks.",
                    "example": "20Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "2.7",
                "description": "FerretDB minor version. ⚠ One property moves two images: the DocumentDB PostgreSQL extension and the FerretDB gateway are released as a matched pair and a mismatched pair is a proxy talking to an extension it does not know. FerretDB maintains no long-term branch, so the two values here are the two most recent releases and a third is a new api-version.",
                "enum": [
                  "2.5",
                  "2.7"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.DocumentDB/accounts",
        "type": "object"
      },
      "CyberCloud.DocumentDB.accounts.List": {
        "type": "object",
        "title": "CyberCloud.DocumentDB.accounts.List",
        "description": "One page of CyberCloud.DocumentDB/accounts. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.DocumentDB.accounts"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.DocumentDB.accounts.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.DocumentDB/accounts/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "database": {
            "description": "The database the DocumentDB extension is installed in, which is the one a driver authenticates against.",
            "type": "string"
          },
          "endpoint": {
            "description": "The in-cluster MongoDB endpoint, mongodb://host:port/. ⚠ No external address is returned, because there is none — see the account's own documentation on exposure.",
            "type": "string"
          },
          "password": {
            "description": "That role's password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "username": {
            "description": "The PostgreSQL role a client authenticates as. Not secret on its own; useless without the password below.",
            "type": "string"
          }
        },
        "required": [
          "database",
          "endpoint",
          "password",
          "username"
        ],
        "title": "CyberCloud.DocumentDB.accounts.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Mail.domains": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Mail/domains: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the mail domain is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The domain's own settings.",
            "properties": {
              "catchAll": {
                "default": "",
                "description": "The local part that receives mail addressed to no known mailbox. Empty means unrouted mail is rejected at RCPT TO, which is the default and the better answer for deliverability: a catch-all accepts every dictionary attack and turns the domain into a backscatter source.",
                "pattern": "^(?:^([a-z0-9]([a-z0-9._-]{0,62}[a-z0-9])?)?$)$",
                "type": "string"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the mail back end.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "dedicatedIp": {
                "default": false,
                "description": "Request a dedicated outbound IP with a warm-up schedule, rather than sharing the platform's warmed pool. Subject to a volume threshold and to approval; requesting it here does not by itself allocate one.",
                "type": "boolean"
              },
              "domain": {
                "default": "example.com",
                "description": "The mail domain this resource hosts, for example example.com. Immutable: the DKIM record, the SPF record and the MX all name it, so changing it would invalidate every record the tenant has published.",
                "example": "example.com",
                "maxLength": 253,
                "pattern": "^(?:^([A-Za-z0-9]([A-Za-z0-9-]{0,61}[A-Za-z0-9])?\\.)+[A-Za-z]{2,63}$)$",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "filtering": {
                "additionalProperties": false,
                "description": "Spam and virus filtering.",
                "properties": {
                  "antivirus": {
                    "default": true,
                    "description": "Scan attachments with ClamAV through Rspamd. Adds roughly 1 GiB of resident memory for the signature database.",
                    "type": "boolean"
                  },
                  "rejectThreshold": {
                    "default": 15,
                    "description": "The Rspamd score at or above which a message is rejected outright rather than filed as junk.",
                    "maximum": 30,
                    "minimum": 5,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "relayHosts": {
                "default": [],
                "description": "Smart hosts to relay outbound mail through instead of delivering it directly. Empty means the platform's own outbound pool.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "sieve": {
                "default": true,
                "description": "Server-side rules through Dovecot's Pigeonhole, editable over ManageSieve.",
                "type": "boolean"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory for the back end, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "c1.micro",
                    "description": "A sizing preset from docs/plan/12. Mail back ends use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.",
                    "enum": [
                      "c1.large",
                      "c1.medium",
                      "c1.micro",
                      "c1.nano",
                      "c1.small",
                      "c1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The mail store.",
                "properties": {
                  "mailboxQuota": {
                    "default": "1Gi",
                    "description": "The default per-mailbox quota, in Kubernetes quantity form. A mailbox may override it. Enforced by Dovecot, not by the volume.",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  },
                  "size": {
                    "default": "20Gi",
                    "description": "The mail volume size, in Kubernetes quantity form. Holds every mailbox in the domain. Grows online; never shrinks.",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "2.4",
                "description": "Dovecot version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.",
                "enum": [
                  "2.3",
                  "2.4"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "domain",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Mail/domains",
        "type": "object"
      },
      "CyberCloud.Mail.domains.List": {
        "type": "object",
        "title": "CyberCloud.Mail.domains.List",
        "description": "One page of CyberCloud.Mail/domains. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Mail.domains"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Messaging.kafkaClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Messaging/kafkaClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cluster's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the Strimzi objects.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "cruiseControl": {
                "additionalProperties": false,
                "description": "Cruise Control — partition rebalancing and broker-load reporting.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether Cruise Control runs. On by default: docs/plan/12 puts it in the chart rather than in a follow-up, because rebalancing a Kafka cluster by hand is the operational cost that makes this the most demanding service in the catalogue.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "external": {
                "additionalProperties": false,
                "description": "Exposure outside the cluster, via a load balancer and a firewall allow-list.",
                "properties": {
                  "allowedCidrs": {
                    "default": [],
                    "description": "Source ranges permitted to reach the external listener. Required in substance rather than in schema: an empty list with external exposure on renders a load balancer that accepts nothing, which is the safe reading of an unfinished configuration.",
                    "example": [
                      "203.0.113.0/24"
                    ],
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "enabled": {
                    "default": false,
                    "description": "Whether the cluster is reachable from outside the Kubernetes cluster. Off by default — docs/plan/12 § Cross-cutting decisions makes external exposure never the default, because a managed broker on a public IP with a weak password is the most common cloud breach there is.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "listener": {
                "additionalProperties": false,
                "description": "The in-cluster listener every client reaches.",
                "properties": {
                  "tls": {
                    "default": true,
                    "description": "Whether the in-cluster listener requires TLS. On by default; turning it off is a plaintext broker on the pod network.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the operator runs a Kafka Exporter alongside the cluster, exposing consumer-lag and topic metrics to the platform's metrics stack.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "nodes": {
                "default": 3,
                "description": "Number of Kafka nodes, each acting as both a KRaft controller and a broker. Use an odd number: a quorum of two tolerates no failures, and one is a single point of failure offered for development only.",
                "maximum": 9,
                "minimum": 1,
                "type": "integer"
              },
              "retention": {
                "additionalProperties": false,
                "description": "How long the cluster keeps messages by default. docs/plan/12 calls retention sizing an ongoing operational concern rather than a one-time setting.",
                "properties": {
                  "hours": {
                    "default": 168,
                    "description": "Default retention in hours. A week by default.",
                    "maximum": 8760,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "size": {
                    "default": "",
                    "description": "Default retention by partition size, in Kubernetes quantity form. Empty means retention is by time alone.",
                    "example": "50Gi",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per node, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "c1.small",
                    "description": "A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.",
                    "enum": [
                      "c1.nano",
                      "c1.micro",
                      "c1.small",
                      "c1.medium",
                      "c1.large",
                      "c1.xlarge",
                      "c1.2xlarge",
                      "c1.4xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The log volume, per node.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "deleteClaim": {
                    "default": false,
                    "description": "Whether deleting the resource also deletes the log volumes. Off by default, so a mistaken delete leaves the data recoverable by hand.",
                    "type": "boolean"
                  },
                  "size": {
                    "default": "100Gi",
                    "description": "Log volume size per node, in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "topics": {
                "additionalProperties": false,
                "description": "Defaults applied to topics the cluster creates automatically. A topic created explicitly overrides them.",
                "properties": {
                  "minInSyncReplicas": {
                    "default": 2,
                    "description": "How many replicas must acknowledge a write before it is committed. Must be below the replication factor, or the cluster cannot tolerate one broker restart.",
                    "maximum": 9,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "partitions": {
                    "default": 3,
                    "description": "Default partition count for a new topic.",
                    "maximum": 200,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "replicationFactor": {
                    "default": 3,
                    "description": "Default replication factor for a new topic. Must not exceed the node count, or every produce to a new topic fails.",
                    "maximum": 9,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "version": {
                "default": "3.9",
                "description": "Apache Kafka version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.",
                "enum": [
                  "3.8",
                  "3.9"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "nodes",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Messaging/kafkaClusters",
        "type": "object"
      },
      "CyberCloud.Messaging.kafkaClusters.List": {
        "type": "object",
        "title": "CyberCloud.Messaging.kafkaClusters.List",
        "description": "One page of CyberCloud.Messaging/kafkaClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Messaging.kafkaClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Messaging.kafkaClusters.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Messaging/kafkaClusters/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "bootstrapServers": {
            "description": "The in-cluster bootstrap address, host:port. ⚠ The external listener's address is not returned here — an address a client cannot reach from where it is running is worse than an absent one.",
            "type": "string"
          },
          "password": {
            "description": "The SCRAM user's password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "securityProtocol": {
            "description": "What a client must configure: SASL_SSL when the listener requires TLS, SASL_PLAINTEXT when it does not.",
            "enum": [
              "SASL_SSL",
              "SASL_PLAINTEXT"
            ],
            "type": "string"
          },
          "username": {
            "description": "The SCRAM user.",
            "type": "string"
          }
        },
        "required": [
          "bootstrapServers",
          "password",
          "securityProtocol",
          "username"
        ],
        "title": "CyberCloud.Messaging.kafkaClusters.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Messaging.natsClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Messaging/natsClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cluster's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the NATS servers.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "external": {
                "additionalProperties": false,
                "description": "Exposure outside the cluster, via a load balancer and a firewall allow-list.",
                "properties": {
                  "allowedCidrs": {
                    "default": [],
                    "description": "Source ranges permitted to reach the external listener. Required in substance rather than in schema: an empty list with external exposure on renders a load balancer that accepts nothing, which is the safe reading of an unfinished configuration.",
                    "example": [
                      "203.0.113.0/24"
                    ],
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "enabled": {
                    "default": false,
                    "description": "Whether the cluster is reachable from outside the Kubernetes cluster. Off by default — docs/plan/12 § Cross-cutting decisions makes external exposure never the default, because a managed broker on a public IP with a weak password is the most common cloud breach there is.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "jetstream": {
                "additionalProperties": false,
                "description": "JetStream — the persistence layer streams and consumers live in. docs/plan/12 has it on unconditionally, so there is no switch to turn it off.",
                "properties": {
                  "maxMemoryStore": {
                    "default": "",
                    "description": "Ceiling for memory-backed streams, in Kubernetes quantity form. Empty means no memory store at all, so every stream is file-backed — which is what docs/plan/12 asks for and what the volume above is sized for. A value here must leave room for the server itself inside the container's memory limit.",
                    "example": "256Mi",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "leafNodes": {
                "additionalProperties": false,
                "description": "Leaf-node connectivity — docs/plan/12: \"so a tenant's edge can attach\".",
                "properties": {
                  "enabled": {
                    "default": false,
                    "description": "Whether the servers accept leaf-node connections. Off by default: a leaf node joins the cluster's subject space, so it is a topology change rather than a client.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "limits": {
                "additionalProperties": false,
                "description": "Server limits a client can run into.",
                "properties": {
                  "maxConnections": {
                    "default": 65536,
                    "description": "Largest number of client connections one server accepts.",
                    "maximum": 65536,
                    "minimum": 16,
                    "type": "integer"
                  },
                  "maxPayload": {
                    "default": 1048576,
                    "description": "Largest message a client may publish, in bytes. Raising it costs memory on every server, because a server buffers a whole message before routing it.",
                    "maximum": 67108864,
                    "minimum": 1024,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether a PodMonitor selects these servers' monitoring endpoint. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". The endpoint itself is always served; this decides whether anything scrapes it.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "servers": {
                "default": 3,
                "description": "Number of NATS servers. docs/plan/12 says three or five: JetStream replicates through a Raft group, so an even count buys no extra fault tolerance over the odd count below it. One is offered for development only and has no quorum at all.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per server, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "c1.small",
                    "description": "A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.",
                    "enum": [
                      "c1.2xlarge",
                      "c1.4xlarge",
                      "c1.large",
                      "c1.medium",
                      "c1.micro",
                      "c1.nano",
                      "c1.small",
                      "c1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The JetStream file store, per server.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "10Gi",
                    "description": "JetStream file-store volume size per server, in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "10Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "2.11",
                "description": "NATS server version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.",
                "enum": [
                  "2.10",
                  "2.11"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "servers",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Messaging/natsClusters",
        "type": "object"
      },
      "CyberCloud.Messaging.natsClusters.List": {
        "type": "object",
        "title": "CyberCloud.Messaging.natsClusters.List",
        "description": "One page of CyberCloud.Messaging/natsClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Messaging.natsClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Messaging.natsClusters.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Messaging/natsClusters/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "monitoringUrl": {
            "description": "The in-cluster HTTP monitoring endpoint, http://host:port — /varz and /jsz. Not secret, and returned here because a caller holding the client URL is the caller who needs it.",
            "type": "string"
          },
          "password": {
            "description": "The client user's password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "url": {
            "description": "The in-cluster client URL, nats://host:port. ⚠ The external listener's address is not returned here — an address a client cannot reach from where it is running is worse than an absent one.",
            "type": "string"
          },
          "user": {
            "description": "The client user.",
            "type": "string"
          }
        },
        "required": [
          "monitoringUrl",
          "password",
          "url",
          "user"
        ],
        "title": "CyberCloud.Messaging.natsClusters.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Messaging.rabbitmqClusters": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Messaging/rabbitmqClusters: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the cluster is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The cluster's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the RabbitmqCluster.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "limits": {
                "additionalProperties": false,
                "description": "Broker limits a client can run into.",
                "properties": {
                  "maxMessageSize": {
                    "default": 134217728,
                    "description": "Largest message a client may publish, in bytes. Raising it costs memory on every node that holds a copy, which for a quorum queue is all of them.",
                    "maximum": 536870912,
                    "minimum": 65536,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "nodes": {
                "default": 3,
                "description": "Number of RabbitMQ nodes. Use an odd number of at least three: a quorum queue is a Raft group, so a group of two tolerates no failures and an even count buys nothing over the odd count below it. One is offered for development only and replicates nothing.",
                "maximum": 7,
                "minimum": 1,
                "type": "integer"
              },
              "plugins": {
                "additionalProperties": false,
                "description": "Protocols and features beyond AMQP.",
                "properties": {
                  "additional": {
                    "default": [],
                    "description": "Plugins to enable on top of the three the operator always enables — peer discovery, the management UI and the Prometheus endpoint. Each recognised plugin adds its own port to the cluster's in-cluster Service.",
                    "example": [
                      "rabbitmq_stream"
                    ],
                    "items": {
                      "enum": [
                        "rabbitmq_federation",
                        "rabbitmq_mqtt",
                        "rabbitmq_shovel",
                        "rabbitmq_stomp",
                        "rabbitmq_stream",
                        "rabbitmq_web_mqtt",
                        "rabbitmq_web_stomp"
                      ],
                      "type": "string"
                    },
                    "type": "array"
                  }
                },
                "type": "object"
              },
              "queues": {
                "additionalProperties": false,
                "description": "How queues behave when a client does not say.",
                "properties": {
                  "defaultType": {
                    "default": "quorum",
                    "description": "The queue type a client gets when it declares a queue without asking for one. Quorum by default — docs/plan/12: a quorum queue is replicated through Raft across the nodes above, and on 4.x a classic queue is not replicated at all, so leaving this unset would put a single-node queue on a cluster the tenant paid three nodes for. This is a node-wide fallback: a vhost created with its own default queue type overrides it.",
                    "enum": [
                      "classic",
                      "quorum",
                      "stream"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per node, either by preset or explicitly.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "c1.small",
                    "description": "A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.",
                    "enum": [
                      "c1.2xlarge",
                      "c1.4xlarge",
                      "c1.large",
                      "c1.medium",
                      "c1.micro",
                      "c1.nano",
                      "c1.small",
                      "c1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The message store, per node.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "20Gi",
                    "description": "Message-store volume size per node, in Kubernetes quantity form. Grows online; never shrinks. A quorum queue keeps its whole Raft log on every member, so this is the same figure on every node rather than a share of one.",
                    "example": "20Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "4.1",
                "description": "RabbitMQ version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field. Only 4.x is offered: classic queue mirroring was removed in 4.0, so on every version here the replicated queue type is the quorum queue.",
                "enum": [
                  "4.0",
                  "4.1"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "nodes",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Messaging/rabbitmqClusters",
        "type": "object"
      },
      "CyberCloud.Messaging.rabbitmqClusters.List": {
        "type": "object",
        "title": "CyberCloud.Messaging.rabbitmqClusters.List",
        "description": "One page of CyberCloud.Messaging/rabbitmqClusters. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Messaging.rabbitmqClusters"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Messaging.rabbitmqClusters.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Messaging/rabbitmqClusters/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "managementUrl": {
            "description": "The in-cluster management UI and HTTP API, http://host:port. docs/plan/12 fronts this with the portal's authenticated proxy; it is never a public route.",
            "type": "string"
          },
          "password": {
            "description": "The generated user's password, read from the cluster's default-user Secret for this call only. ⚠ Neither this value nor the user name is chosen by the platform: the operator generates both at first boot and RabbitMQ's own guest/guest account is never created.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "url": {
            "description": "The in-cluster AMQP URL, amqp://host:port. ⚠ There is no external address to return — this type declares no external listener at all, for the reason its schema gives.",
            "type": "string"
          },
          "user": {
            "description": "The generated broker user.",
            "type": "string"
          }
        },
        "required": [
          "managementUrl",
          "password",
          "url",
          "user"
        ],
        "title": "CyberCloud.Messaging.rabbitmqClusters.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Monitor.workspaces": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Monitor/workspaces: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the workspace is billed in, and the region whose telemetry stores hold its data.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The workspace's own settings.",
            "properties": {
              "clusterId": {
                "description": "The regional cluster whose telemetry data plane serves this workspace. ⚠ Nothing is deployed into it: a workspace is a tenancy in stores the platform already runs, and this is where its ingest routing is published.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "purgeProtection": {
                "default": false,
                "description": "Whether this workspace may be destroyed before its seven-day recovery window is out. Once true it stays true for the rest of the workspace's life, and a purge is refused while it is set.",
                "type": "boolean"
              },
              "quota": {
                "additionalProperties": false,
                "description": "What the workspace may take in per day, and what happens when it takes in more.",
                "properties": {
                  "cardinalityCap": {
                    "default": 20000,
                    "description": "Distinct values one metric label may take before the series carrying it are rejected. The rejection names the offending label — docs/plan/16 § Ingest — because a rejection nobody can diagnose is one the client just retries.",
                    "maximum": 1000000,
                    "minimum": 100,
                    "type": "integer"
                  },
                  "logsGbPerDay": {
                    "default": 10,
                    "description": "Log lines accepted per day, in gibibytes.",
                    "maximum": 10000,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "metricsGbPerDay": {
                    "default": 5,
                    "description": "Metric samples accepted per day, in gibibytes. This is the first factor of what the workspace draws against the subscription's storage quota; the second is the retention tier.",
                    "maximum": 10000,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "overQuotaSampleRate": {
                    "default": 10,
                    "description": "The percentage of data still accepted once a signal is over its daily allowance. ⚠ It cannot be zero: docs/plan/16 requires that going over quota samples at a visible rate rather than dropping silently, and zero is a silent drop spelled as a rate.",
                    "maximum": 100,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "seriesCap": {
                    "default": 1000000,
                    "description": "Active metric series the workspace may hold at once. One tenant putting a request id in a metric label is how a shared time-series database dies, so this is a ceiling rather than a guideline.",
                    "maximum": 50000000,
                    "minimum": 1000,
                    "type": "integer"
                  },
                  "tracesGbPerDay": {
                    "default": 5,
                    "description": "Spans accepted per day, in gibibytes.",
                    "maximum": 10000,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "cardinalityCap",
                  "logsGbPerDay",
                  "metricsGbPerDay",
                  "overQuotaSampleRate",
                  "seriesCap",
                  "tracesGbPerDay"
                ],
                "type": "object"
              },
              "retention": {
                "additionalProperties": false,
                "description": "How long each signal is kept. Priced per tier — docs/plan/16 § Cost and retention honesty.",
                "properties": {
                  "logs": {
                    "default": "short",
                    "description": "Logs retention tier: short is 7 days, standard 30, extended 90. ⚠ Shortening this under an existing workspace destroys the lines already outside the new window, and the reconciler refuses the change rather than applying it.",
                    "enum": [
                      "short",
                      "standard",
                      "extended"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "sku"
                  },
                  "metrics": {
                    "default": "short",
                    "description": "Metrics retention tier: short is 15 days, standard 90, extended 400. ⚠ Shortening this under an existing workspace destroys the samples already outside the new window, and the reconciler refuses the change rather than applying it.",
                    "enum": [
                      "short",
                      "standard",
                      "extended"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "sku"
                  },
                  "traces": {
                    "default": "short",
                    "description": "Traces retention tier: short is 3 days, standard 14, extended 30. ⚠ Shortening this under an existing workspace destroys the spans already outside the new window, and the reconciler refuses the change rather than applying it.",
                    "enum": [
                      "short",
                      "standard",
                      "extended"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "sku"
                  }
                },
                "required": [
                  "logs",
                  "metrics",
                  "traces"
                ],
                "type": "object"
              }
            },
            "required": [
              "clusterId",
              "purgeProtection"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Monitor/workspaces",
        "type": "object"
      },
      "CyberCloud.Monitor.workspaces.List": {
        "type": "object",
        "title": "CyberCloud.Monitor.workspaces.List",
        "description": "One page of CyberCloud.Monitor/workspaces. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Monitor.workspaces.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Monitor/workspaces/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "accountId": {
            "description": "The VictoriaMetrics tenant this workspace's metrics live under. It appears in every metrics URL below and is returned rather than left to be guessed at.",
            "type": "string"
          },
          "database": {
            "description": "The ClickHouse database this workspace's logs, traces and events live in. It is the one platform-chosen string that reaches the tenant's SQL.",
            "type": "string"
          },
          "ingestKey": {
            "description": "The ingest key, read from the tenant's vault for this call only. It authenticates writes on every endpoint above.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "otlpEndpoint": {
            "description": "Where OTLP over gRPC is accepted, host:port.",
            "type": "string"
          },
          "promqlEndpoint": {
            "description": "The read-only PromQL/MetricsQL datasource URL, for the tenant's own Grafana or an external one.",
            "type": "string"
          },
          "remoteWriteEndpoint": {
            "description": "Where Prometheus remote-write is accepted.",
            "type": "string"
          },
          "sqlEndpoint": {
            "description": "The read-only SQL datasource URL for logs, traces and events.",
            "type": "string"
          }
        },
        "required": [
          "accountId",
          "database",
          "ingestKey",
          "otlpEndpoint",
          "promqlEndpoint",
          "remoteWriteEndpoint",
          "sqlEndpoint"
        ],
        "title": "CyberCloud.Monitor.workspaces.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Monitor.workspaces.alertRules": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Monitor/workspaces/alertRules: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the rule is evaluated in — the workspace's.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The rule's own settings.",
            "properties": {
              "actionGroup": {
                "additionalProperties": false,
                "description": "Who is told, and how.",
                "properties": {
                  "channel": {
                    "description": "Which of that service's channels carries it. The service must have the channel configured and enabled, or every notification is refused by name.",
                    "enum": [
                      "sms",
                      "whatsapp",
                      "email",
                      "push",
                      "voice"
                    ],
                    "example": "email",
                    "type": "string"
                  },
                  "notifyOnResolve": {
                    "default": true,
                    "description": "Whether the recipients are told when the condition stops holding, as well as when it starts.",
                    "type": "boolean"
                  },
                  "recipients": {
                    "description": "Where it goes — addresses or E.164 numbers, one send each, every one checked against the service's suppression list before dispatch. At least one and at most 20; a list outside that is refused when the rule is reconciled.",
                    "example": [
                      "oncall@example.com"
                    ],
                    "items": {
                      "maxLength": 320,
                      "minLength": 1,
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "service": {
                    "description": "The CyberCloud.Communication/services resource the notification is sent through, as its full resource id path. It must be in this tenant.",
                    "example": "/tenants/11111111-1111-4111-8111-111111111111/subscriptions/33333333-3333-4333-8333-333333333333/resourceGroups/prod/providers/CyberCloud.Communication/services/alerts",
                    "format": "cybercloud-resource-id",
                    "maxLength": 512,
                    "type": "string"
                  }
                },
                "required": [
                  "channel",
                  "recipients",
                  "service"
                ],
                "type": "object"
              },
              "condition": {
                "additionalProperties": false,
                "description": "What is asked, and what answer counts.",
                "properties": {
                  "lookbackSeconds": {
                    "default": 300,
                    "description": "How far back the query may read, in seconds. Capped at a day: the look-back is the query's cost, and the cap is the platform's, not the tenant's.",
                    "maximum": 86400,
                    "minimum": 60,
                    "type": "integer"
                  },
                  "operator": {
                    "description": "How a value is compared with the threshold.",
                    "enum": [
                      "greaterThan",
                      "greaterOrEqual",
                      "lessThan",
                      "lessOrEqual",
                      "equal",
                      "notEqual"
                    ],
                    "example": "greaterThan",
                    "type": "string"
                  },
                  "query": {
                    "description": "The query, verbatim. It must produce numbers; the rule fires when any of them satisfies the operator against the threshold.",
                    "example": "max(rate(http_requests_errors_total[5m]))",
                    "maxLength": 4096,
                    "minLength": 1,
                    "type": "string"
                  },
                  "signal": {
                    "description": "Which store the query runs against: metrics is MetricsQL over the workspace's VictoriaMetrics account, logs is SQL over its ClickHouse database.",
                    "enum": [
                      "metrics",
                      "logs"
                    ],
                    "example": "metrics",
                    "type": "string"
                  },
                  "threshold": {
                    "description": "The number the value is compared with.",
                    "example": 5,
                    "type": "number"
                  }
                },
                "required": [
                  "operator",
                  "query",
                  "signal",
                  "threshold"
                ],
                "type": "object"
              },
              "enabled": {
                "default": true,
                "description": "Whether the rule is evaluated. Off keeps the rule and its history and stops the clock; an open alert is resolved without a notification.",
                "type": "boolean"
              },
              "evaluation": {
                "additionalProperties": false,
                "description": "How often, and how long before it counts.",
                "properties": {
                  "forSeconds": {
                    "default": 0,
                    "description": "How long the condition must hold before the rule fires, in seconds. Zero fires on the first evaluation that meets it; 300 ignores anything shorter than five minutes.",
                    "maximum": 86400,
                    "minimum": 0,
                    "type": "integer"
                  },
                  "intervalSeconds": {
                    "default": 60,
                    "description": "How often the condition is evaluated, in seconds. A multiple of 60: the evaluator ticks once a minute and a rule at 300 is evaluated on every fifth tick. Anything else is refused when the rule is reconciled.",
                    "maximum": 3600,
                    "minimum": 60,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "severity": {
                "description": "How loud: critical pages somebody, error is looked at today, warning this week, informational is worth knowing.",
                "enum": [
                  "critical",
                  "error",
                  "warning",
                  "informational"
                ],
                "example": "warning",
                "type": "string"
              }
            },
            "required": [
              "severity"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Monitor/workspaces/alertRules",
        "type": "object"
      },
      "CyberCloud.Monitor.workspaces.alertRules.List": {
        "type": "object",
        "title": "CyberCloud.Monitor.workspaces.alertRules.List",
        "description": "One page of CyberCloud.Monitor/workspaces/alertRules. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Monitor.workspaces.alertRules"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Monitor.workspaces.alertRules.ListInstancesResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Monitor/workspaces/alertRules/listInstances returns.",
        "properties": {
          "count": {
            "description": "How many firings the rule keeps.",
            "type": "integer"
          },
          "instances": {
            "description": "Every firing, oldest first, one line each: '{state} {severity} fired {firedAt} resolved {resolvedAt} value {value}: {summary} | {notification}'.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "open": {
            "description": "How many of them are still firing.",
            "type": "integer"
          },
          "state": {
            "description": "Where the rule is now: ok, pending or firing.",
            "type": "string"
          }
        },
        "required": [
          "count",
          "instances",
          "open",
          "state"
        ],
        "title": "CyberCloud.Monitor.workspaces.alertRules.ListInstancesResponse",
        "type": "object"
      },
      "CyberCloud.Network.publicIpAddresses": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/publicIpAddresses: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the address is allocated in. ⚠ It must be a region whose operator has an external pool — nothing checks that, and an address in a region with none never becomes ready.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The address's own settings.",
            "properties": {
              "address": {
                "additionalProperties": false,
                "description": "A particular address to ask for, rather than whichever one is free.",
                "properties": {
                  "v4": {
                    "default": "",
                    "description": "The IPv4 address to reclaim, or empty to be given whichever one is free. ⚠ A bare address and not a prefix — 10.100.0.7, never 10.100.0.7/32. An address that is already taken is refused by the fabric rather than by the API.",
                    "example": "10.100.0.7",
                    "pattern": "^(?:((\\d{1,3}\\.){3}\\d{1,3})?)$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  },
                  "v6": {
                    "default": "",
                    "description": "The IPv6 address to reclaim, or empty to be given whichever one is free. ⚠ Lower case only — the fabric refuses an address with an upper-case letter in it. Whether an address has a v6 half at all is decided by the pool it comes from, not here.",
                    "example": "fd00:ff::7",
                    "pattern": "^(?:([0-9A-Fa-f:.]+)?)$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose external pool the address is allocated from. ⚠ An address is only reachable from the fabric that announces it, so a load balancer in another cluster cannot use it.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/publicIpAddresses",
        "type": "object"
      },
      "CyberCloud.Network.publicIpAddresses.List": {
        "type": "object",
        "title": "CyberCloud.Network.publicIpAddresses.List",
        "description": "One page of CyberCloud.Network/publicIpAddresses. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.publicIpAddresses"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.publicIpAddresses.ShowAllocationResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/publicIpAddresses/showAllocation returns.",
        "properties": {
          "attachedTo": {
            "description": "The NAT rule currently using this address, or empty. ⚠ Empty means the address is allocated and carries no traffic. The only thing that can attach one is a natGateways resource, which puts a subnet's outbound traffic on it; nothing can yet publish anything inbound on an address.",
            "type": "string"
          },
          "macAddress": {
            "description": "The MAC address the fabric bound to it. ⚠ Reported because it is what an operator needs to find this address in an ARP table when it is unreachable.",
            "type": "string"
          },
          "ready": {
            "description": "Whether the fabric has finished announcing the address. ⚠ False with an address already reported is the ordinary state for a few seconds after create.",
            "type": "boolean"
          },
          "sampledAt": {
            "description": "When the platform read the object, RFC 3339. ⚠ The read time rather than the time the fabric wrote the figures, because the object carries no timestamp on them.",
            "format": "date-time",
            "type": "string"
          },
          "v4": {
            "description": "The IPv4 address the fabric allocated, or empty when it has not allocated one yet.",
            "type": "string"
          },
          "v6": {
            "description": "The IPv6 address the fabric allocated, or empty for an IPv4-only pool.",
            "type": "string"
          }
        },
        "required": [
          "ready",
          "sampledAt",
          "v4"
        ],
        "title": "CyberCloud.Network.publicIpAddresses.ShowAllocationResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the virtual network lives in. ⚠ It selects which reserved ranges the address space is checked against — a region's underlay is part of that list.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The virtual network's own settings.",
            "properties": {
              "addressSpace": {
                "additionalProperties": false,
                "description": "The address range the network plans for. ⚠ Declarative: it is checked against the region's reserved ranges and is not rendered into the fabric, because a Kube-OVN Vpc carries no CIDR — the subnets do.",
                "properties": {
                  "v4": {
                    "default": "10.20.0.0/16",
                    "description": "The IPv4 range the network plans for, in CIDR form. It may overlap another of your own virtual networks — that is what a VPC is for — and it may not overlap a range the platform reserves, which is refused with the conflicting range named.",
                    "example": "10.20.0.0/16",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  },
                  "v6": {
                    "default": "",
                    "description": "The IPv6 range the network plans for, or empty for an IPv4-only network. ⚠ docs/plan/14 § IPv6 asks for dual-stack from day one rather than as a retrofit, which is why this is here at the first api-version even though a v4-only network is the ordinary case.",
                    "example": "fd00:20::/48",
                    "pattern": "^(?:([0-9A-Fa-f:.]+/[0-9]{1,3})?)$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  }
                },
                "required": [
                  "v4"
                ],
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose fabric carries the network.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "enableExternal": {
                "default": false,
                "description": "Whether the network's router is attached to the external network. Off by default: a network that reaches the outside without being asked is a network whose owner did not choose that. A natGateways child needs it on: without the attachment its translation is programmed and its packets are dropped. ⚠ Turning it on requires the cluster to have an external subnet configured; without one the Vpc is accepted and the attachment never completes.",
                "type": "boolean"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.List",
        "description": "One page of CyberCloud.Network/virtualNetworks. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.ShowIsolationResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/showIsolation returns.",
        "properties": {
          "claim": {
            "description": "What tenant isolation this network provides, in one sentence. It is the same sentence for every virtual network on the platform, and it is deliberately narrower than 'isolated'.",
            "type": "string"
          },
          "limits": {
            "description": "What this network does NOT guarantee, one entry per limit, each naming the guarantee, why the substrate does not deliver it, and what to ask for instead. ⚠ Read this before deciding a virtual network satisfies a compliance requirement.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "substrate": {
            "description": "The technology enforcing the separation, named so that a tenant's own security review has something to review.",
            "example": "Kube-OVN (Open vSwitch)",
            "type": "string"
          }
        },
        "required": [
          "claim",
          "limits",
          "substrate"
        ],
        "title": "CyberCloud.Network.virtualNetworks.ShowIsolationResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.loadBalancers": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks/loadBalancers: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the load balancer is billed in. ⚠ It must be the region its virtual network is in — nothing checks that, because the network's own region is not readable from here.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The load balancer's own settings.",
            "properties": {
              "backend": {
                "additionalProperties": false,
                "description": "Where the connections go.",
                "properties": {
                  "addresses": {
                    "default": "10.20.1.11",
                    "description": "The workload addresses to balance across, comma separated — for example 10.20.1.11,10.20.1.12. ⚠ Addresses and not resource ids: there is no service discovery and no DNS inside a virtual network, so an address is what a tenant has for their own workloads. At most 32.",
                    "example": "10.20.1.11,10.20.1.12",
                    "maxLength": 1024,
                    "pattern": "^(?:[0-9A-Fa-f:.]+(,[0-9A-Fa-f:.]+)*)$",
                    "type": "string"
                  },
                  "port": {
                    "default": 8080,
                    "description": "The TCP port every backend address is reached on. ⚠ One port for the whole pool: a pool whose members listen on different ports is two pools.",
                    "maximum": 65535,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "addresses",
                  "port"
                ],
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster the proxy runs in. ⚠ It must be the cluster the virtual network was created in: a proxy in another cluster has no route into this network at all.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "frontend": {
                "additionalProperties": false,
                "description": "The address and port workloads connect to.",
                "properties": {
                  "port": {
                    "default": 80,
                    "description": "The TCP port the proxy listens on. ⚠ There is no protocol setting: HAProxy does not proxy UDP in any version, so every rule here is TCP.",
                    "maximum": 65535,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "v4": {
                    "default": "10.20.1.10",
                    "description": "The IPv4 address the proxy answers on, inside the subnet's range. ⚠ Required, and it is the one thing about this resource a tenant must choose: there is no DNS inside a virtual network, so an address nobody picked is an address nothing can be pointed at. A bare address and never a prefix.",
                    "example": "10.20.1.10",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3})$",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "cidr"
                  },
                  "v6": {
                    "default": "",
                    "description": "The IPv6 address the proxy also answers on, or empty. ⚠ Lower case only, for the fabric's reason. It must be inside the subnet's IPv6 range, which means the subnet has to have one.",
                    "example": "fd00:20:1::10",
                    "pattern": "^(?:([0-9A-Fa-f:.]+)?)$",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "cidr"
                  }
                },
                "required": [
                  "port",
                  "v4"
                ],
                "type": "object"
              },
              "health": {
                "additionalProperties": false,
                "description": "How a backend is decided to be up. ⚠ Checking cannot be turned off — a proxy that sends connections to a dead server is worse than no proxy.",
                "properties": {
                  "healthyAfter": {
                    "default": 2,
                    "description": "How many successful probes put a backend back into the pool.",
                    "maximum": 10,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "intervalSeconds": {
                    "default": 5,
                    "description": "How often each backend is probed with a TCP connection.",
                    "maximum": 60,
                    "minimum": 2,
                    "type": "integer"
                  },
                  "unhealthyAfter": {
                    "default": 3,
                    "description": "How many failed probes take a backend out of the pool.",
                    "maximum": 10,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "limits": {
                "additionalProperties": false,
                "description": "What the proxy refuses rather than passes on.",
                "properties": {
                  "maxConnections": {
                    "default": 2000,
                    "description": "How many connections the frontend accepts at once. ⚠ Further connections wait in the kernel's accept queue rather than being refused, so this is a back-pressure setting rather than a firewall. It is also applied per backend server.",
                    "maximum": 100000,
                    "minimum": 10,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory for the proxy.",
                "properties": {
                  "preset": {
                    "default": "c1.small",
                    "description": "How much the proxy pod gets. An L4 proxy is mostly kernel work, so the small row carries far more than its size suggests; the larger rows are for many long-lived connections.",
                    "enum": [
                      "c1.large",
                      "c1.medium",
                      "c1.small"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "subnet": {
                "default": "web",
                "description": "The subnet of this virtual network the proxy sits on. ⚠ The proxy gets an address from it, so the frontend address below must be inside its range. A name that is not a subnet of this network is refused by the fabric rather than by the API, and the proxy pod never schedules.",
                "example": "web",
                "maxLength": 63,
                "pattern": "^(?:[a-z0-9]([-a-z0-9]*[a-z0-9])?)$",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "subnet"
              },
              "version": {
                "default": "3.2",
                "description": "Which HAProxy line to run. 3.2 is the long-term-support line and is the default; 3.4 is the current one. ⚠ A change here restarts the proxy and drops open connections.",
                "enum": [
                  "3.2",
                  "3.4"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "subnet"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks/loadBalancers",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.loadBalancers.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.loadBalancers.List",
        "description": "One page of CyberCloud.Network/virtualNetworks/loadBalancers. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.loadBalancers"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.loadBalancers.ShowBackendsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/loadBalancers/showBackends returns.",
        "properties": {
          "frontend": {
            "description": "The address and port the proxy answers on.",
            "type": "string"
          },
          "note": {
            "description": "What the answer is and is not — in particular that the servers are what the platform configured rather than what the proxy currently believes is healthy.",
            "type": "string"
          },
          "readyReplicas": {
            "description": "How many proxy pods are running and passing their readiness probe. ⚠ 0 with servers listed is a load balancer that is configured and carrying no traffic, which is the state this action exists to make visible.",
            "type": "integer"
          },
          "sampledAt": {
            "description": "When the platform read the Deployment, RFC 3339.",
            "format": "date-time",
            "type": "string"
          },
          "serverCount": {
            "description": "How many servers the pool carries.",
            "type": "integer"
          },
          "servers": {
            "description": "One line per backend server, in the order the proxy is given them.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "frontend",
          "note",
          "readyReplicas",
          "sampledAt",
          "serverCount",
          "servers"
        ],
        "title": "CyberCloud.Network.virtualNetworks.loadBalancers.ShowBackendsResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.natGateways": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks/natGateways: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the NAT gateway is billed in. ⚠ It must be the region its virtual network is in — nothing checks that, because the network's own region is not readable from here.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The NAT gateway's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose fabric holds the network. ⚠ It must be the cluster the virtual network and the public address were created in: a rule in another cluster names a subnet and an address that do not exist there.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "publicIpAddress": {
                "default": "egress",
                "description": "The name of a publicIpAddresses resource in the same resource group whose address the subnet's traffic leaves with. ⚠ A name, not a resource id: the address must be in this subscription and resource group, and one in another cannot be named. An address that does not exist is refused by the fabric rather than by the API.",
                "example": "egress",
                "maxLength": 63,
                "pattern": "^(?:[a-z0-9]([-a-z0-9]*[a-z0-9])?)$",
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "subnet": {
                "default": "web",
                "description": "The subnet of this virtual network whose workloads egress through the address. One subnet per NAT gateway; a network with several private subnets creates one per subnet, and they may share the address. ⚠ A name that is not a subnet of this network is refused by the fabric rather than by the API, and the gateway never becomes ready.",
                "example": "web",
                "maxLength": 63,
                "pattern": "^(?:[a-z0-9]([-a-z0-9]*[a-z0-9])?)$",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "subnet"
              }
            },
            "required": [
              "clusterId",
              "publicIpAddress",
              "subnet"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks/natGateways",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.natGateways.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.natGateways.List",
        "description": "One page of CyberCloud.Network/virtualNetworks/natGateways. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.natGateways"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.natGateways.ShowEgressResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/natGateways/showEgress returns.",
        "properties": {
          "publicV4": {
            "description": "The IPv4 address the subnet's traffic leaves with, or empty until the fabric has resolved the named public address.",
            "type": "string"
          },
          "publicV6": {
            "description": "The IPv6 address the subnet's traffic leaves with, or empty for an IPv4-only pool.",
            "type": "string"
          },
          "ready": {
            "description": "Whether the fabric has programmed the translation. ⚠ False with every address empty is a rule whose subnet or public address the fabric cannot find — check both names.",
            "type": "boolean"
          },
          "sampledAt": {
            "description": "When the platform read the object, RFC 3339.",
            "format": "date-time",
            "type": "string"
          },
          "sourceV4": {
            "description": "The IPv4 range being translated — the subnet's prefix as the fabric resolved it, or empty until it has.",
            "type": "string"
          },
          "sourceV6": {
            "description": "The IPv6 range being translated, or empty for an IPv4-only subnet.",
            "type": "string"
          }
        },
        "required": [
          "publicV4",
          "ready",
          "sampledAt",
          "sourceV4"
        ],
        "title": "CyberCloud.Network.virtualNetworks.natGateways.ShowEgressResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.peerings": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks/peerings: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the peering is billed in. ⚠ It must be the region both virtual networks are in — nothing checks that, because neither network's own region is readable from here.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The peering's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose fabric holds both networks. ⚠ Two networks in two clusters cannot be peered: a Kube-OVN peering is two ports on one OVN northbound database.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "link": {
                "additionalProperties": false,
                "description": "The point-to-point range the two routers address each other on.",
                "properties": {
                  "v4": {
                    "default": "10.255.255.0/30",
                    "description": "A small IPv4 range, /30 or wider, that is in neither network. This network's peer port takes its first host address and the remote's takes the second. ⚠ It is checked against the platform's reserved ranges like any other, so link-local space cannot be used.",
                    "example": "10.255.255.0/30",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  }
                },
                "required": [
                  "v4"
                ],
                "type": "object"
              },
              "localAddressSpace": {
                "additionalProperties": false,
                "description": "The range this network advertises to the remote — the remote's router gets one static route to it.",
                "properties": {
                  "v4": {
                    "default": "10.20.0.0/16",
                    "description": "This network's IPv4 range, in CIDR form — normally its address space. ⚠ It may not overlap the remote range or the link, and the refusal names both.",
                    "example": "10.20.0.0/16",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  }
                },
                "required": [
                  "v4"
                ],
                "type": "object"
              },
              "remoteAddressSpace": {
                "additionalProperties": false,
                "description": "The range the remote advertises to this network — this network's router gets one static route to it.",
                "properties": {
                  "v4": {
                    "default": "10.30.0.0/16",
                    "description": "The remote network's IPv4 range, in CIDR form — normally its address space. ⚠ Two networks with overlapping ranges cannot be peered, which is the one place this platform's 'overlapping your own networks is fine' stops applying: a route to a range you also hold has nowhere to go.",
                    "example": "10.30.0.0/16",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  }
                },
                "required": [
                  "v4"
                ],
                "type": "object"
              },
              "remoteNetwork": {
                "default": "spoke",
                "description": "The name of the virtualNetworks resource in the same resource group to peer this network with. ⚠ A name, not a resource id: the remote must be in this subscription and resource group, and a network in another cannot be named — write on the peering has to imply write on both networks. A remote that does not exist keeps the peering in progress until it does. Cannot be changed once created; delete the peering and create another.",
                "example": "spoke",
                "maxLength": 63,
                "pattern": "^(?:[a-z0-9]([-a-z0-9]*[a-z0-9])?)$",
                "type": "string",
                "x-cybercloud-immutable": true
              }
            },
            "required": [
              "clusterId",
              "remoteNetwork"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks/peerings",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.peerings.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.peerings.List",
        "description": "One page of CyberCloud.Network/virtualNetworks/peerings. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.peerings"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.peerings.ShowRoutesResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/peerings/showRoutes returns.",
        "properties": {
          "localConnectIP": {
            "description": "The address, with the link's prefix, this network's peer port carries.",
            "type": "string"
          },
          "localConnected": {
            "description": "Whether the fabric lists the remote in the local Vpc's status.vpcPeerings — the peer port exists. False on a cluster without the Kube-OVN controller.",
            "type": "boolean"
          },
          "localVpc": {
            "description": "The local network's Vpc object name.",
            "type": "string"
          },
          "localWritten": {
            "description": "Whether the local Vpc carries this peering's entry and route.",
            "type": "boolean"
          },
          "remoteConnectIP": {
            "description": "The address, with the link's prefix, the remote's peer port carries.",
            "type": "string"
          },
          "remoteConnected": {
            "description": "The same, read off the remote Vpc.",
            "type": "boolean"
          },
          "remoteVpc": {
            "description": "The remote network's Vpc object name.",
            "type": "string"
          },
          "remoteWritten": {
            "description": "Whether the remote Vpc carries this peering's entry and route. ⚠ False with localWritten true is a remote network that was deleted or never existed.",
            "type": "boolean"
          },
          "sampledAt": {
            "description": "When the platform read the objects, RFC 3339.",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "localConnectIP",
          "localConnected",
          "localVpc",
          "localWritten",
          "remoteConnectIP",
          "remoteConnected",
          "remoteVpc",
          "remoteWritten",
          "sampledAt"
        ],
        "title": "CyberCloud.Network.virtualNetworks.peerings.ShowRoutesResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.securityGroups": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks/securityGroups: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the security group lives in. It must be the network's own region — nothing checks that.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The security group's own settings.",
            "properties": {
              "allowSameGroupTraffic": {
                "default": false,
                "description": "Whether workloads that carry this same security group may reach each other without a rule. Off by default: it is the one setting here that permits traffic nobody wrote a rule for.",
                "type": "boolean"
              },
              "clusterId": {
                "description": "The cluster whose fabric carries the security group. Must be the cluster the network is in — nothing checks that.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "egress": {
                "additionalProperties": false,
                "description": "What workloads carrying this group may reach. ⚠ Same default-deny: an empty section permits no outbound traffic at all, which for most workloads means no DNS and no package downloads.",
                "properties": {
                  "allowIcmp": {
                    "default": false,
                    "description": "Whether outbound ICMP is allowed to the remotes above. Off by default.",
                    "type": "boolean"
                  },
                  "remoteV4": {
                    "default": "",
                    "description": "The IPv4 range outbound traffic may reach, or empty for no IPv4 rules at all.",
                    "example": "0.0.0.0/0",
                    "pattern": "^(?:((\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})?)$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  },
                  "remoteV6": {
                    "default": "",
                    "description": "The IPv6 range outbound traffic may reach, or empty for no IPv6 rules at all.",
                    "example": "::/0",
                    "pattern": "^(?:([0-9A-Fa-f:.]+/[0-9]{1,3})?)$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  },
                  "tcpPorts": {
                    "default": "",
                    "description": "TCP ports outbound traffic may reach, in the same form as the inbound list. Empty means no outbound TCP.",
                    "example": "443",
                    "pattern": "^(?:((6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?(,(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?)*)?)$",
                    "type": "string"
                  },
                  "udpPorts": {
                    "default": "",
                    "description": "UDP ports outbound traffic may reach. Empty means no outbound UDP — which includes DNS on port 53.",
                    "example": "53",
                    "pattern": "^(?:((6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?(,(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?)*)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "ingress": {
                "additionalProperties": false,
                "description": "What may reach workloads carrying this group. ⚠ Anything not allowed here is dropped — the fabric installs a default-deny for every port in a security group, so an empty section permits nothing.",
                "properties": {
                  "allowIcmp": {
                    "default": false,
                    "description": "Whether inbound ICMP is allowed from the remotes above. Off by default. ⚠ With this off, a workload in this group does not answer ping and does not receive path-MTU messages.",
                    "type": "boolean"
                  },
                  "remoteV4": {
                    "default": "",
                    "description": "The IPv4 range inbound traffic may come from, in CIDR form, or empty for no IPv4 rules at all. Use 0.0.0.0/0 for the whole internet.",
                    "example": "10.20.0.0/16",
                    "pattern": "^(?:((\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})?)$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  },
                  "remoteV6": {
                    "default": "",
                    "description": "The IPv6 range inbound traffic may come from, or empty for no IPv6 rules at all. ⚠ A group with only an IPv4 remote silently permits nothing over IPv6, which on a dual-stack subnet is not the same as permitting nothing.",
                    "example": "fd00:20::/48",
                    "pattern": "^(?:([0-9A-Fa-f:.]+/[0-9]{1,3})?)$",
                    "type": "string",
                    "x-cybercloud-widget": "cidr"
                  },
                  "tcpPorts": {
                    "default": "",
                    "description": "TCP ports inbound traffic may reach, as a comma-separated list of ports and ranges — for example 80,443,8000-8100. Empty means no TCP is allowed inbound. ⚠ There is no way to say 'every protocol'; 1-65535 says 'every TCP port'.",
                    "example": "80,443",
                    "pattern": "^(?:((6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?(,(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?)*)?)$",
                    "type": "string"
                  },
                  "udpPorts": {
                    "default": "",
                    "description": "UDP ports inbound traffic may reach, in the same form as tcpPorts. Empty means no UDP is allowed inbound.",
                    "example": "53",
                    "pattern": "^(?:((6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?(,(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3})(-(6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}))?)*)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks/securityGroups",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.securityGroups.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.securityGroups.List",
        "description": "One page of CyberCloud.Network/virtualNetworks/securityGroups. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.securityGroups"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.securityGroups.ShowEffectiveRulesResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/securityGroups/showEffectiveRules returns.",
        "properties": {
          "allowSameGroupTraffic": {
            "description": "Whether workloads carrying this same group reach each other without a rule. ⚠ Returned because it is the one permission that is not in the list above.",
            "type": "boolean"
          },
          "count": {
            "description": "How many rules there are. ⚠ Worth reading next to the body: one port list of three entries with two remotes is six rules.",
            "type": "integer"
          },
          "defaultAction": {
            "description": "What happens to traffic no rule above matches. It is always 'drop' — the fabric installs a default-deny for every port in a security group and this resource cannot change that.",
            "type": "string"
          },
          "note": {
            "description": "⚠ What this answer is and is not. It is what the platform asks the fabric for, derived from the resource's stored body — not a reading of the ACLs OVN currently holds.",
            "type": "string"
          },
          "rules": {
            "description": "Every rule this security group's body becomes, in the order it is written to the fabric, one sentence each. An empty list means the group permits nothing, which is a valid and fully restrictive configuration.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "allowSameGroupTraffic",
          "count",
          "defaultAction",
          "note",
          "rules"
        ],
        "title": "CyberCloud.Network.virtualNetworks.securityGroups.ShowEffectiveRulesResponse",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.subnets": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Network/virtualNetworks/subnets: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the subnet lives in. ⚠ It selects which reserved ranges the prefix is checked against, and it must be the network's own region — nothing checks that.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The subnet's own settings.",
            "properties": {
              "addressPrefix": {
                "additionalProperties": false,
                "description": "The range addresses are allocated from.",
                "properties": {
                  "v4": {
                    "default": "10.20.1.0/24",
                    "description": "The IPv4 prefix, in CIDR form. ⚠ Host bits are cleared by the fabric, so 10.20.1.7/24 is stored as 10.20.1.0/24 and both name the same network. It may not overlap a range the platform reserves, which is refused with the conflicting range named.",
                    "example": "10.20.1.0/24",
                    "pattern": "^(?:(\\d{1,3}\\.){3}\\d{1,3}/\\d{1,2})$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  },
                  "v6": {
                    "default": "",
                    "description": "The IPv6 prefix, or empty for an IPv4-only subnet. Setting both makes the subnet dual-stack — docs/plan/14 § IPv6.",
                    "example": "fd00:20:1::/64",
                    "pattern": "^(?:([0-9A-Fa-f:.]+/[0-9]{1,3})?)$",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  }
                },
                "required": [
                  "v4"
                ],
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose fabric carries the subnet. Must be the cluster the network is in — nothing checks that, and a subnet placed elsewhere binds to a Vpc that does not exist there.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "enableDhcp": {
                "default": false,
                "description": "Whether the fabric answers DHCP in this subnet. Off by default: an address is assigned to a workload's port when the port is created, and DHCP is for guests that insist on asking — a virtual machine rather than a container.",
                "type": "boolean"
              },
              "natOutgoing": {
                "default": false,
                "description": "Whether the fabric's node gateway masquerades this subnet's outbound traffic. Off by default. ⚠ ON A TENANT VIRTUAL NETWORK THIS FLAG DOES NOTHING: Kube-OVN honors it only for subnets of its default VPC, and every subnet here is in a tenant's own. It stays because the api-version is published. Outbound access for a subnet is a natGateways resource, which translates the subnet to a public IP address you hold.",
                "type": "boolean"
              },
              "private": {
                "default": false,
                "description": "Whether the subnet refuses traffic from other subnets. Off by default. ⚠ In this api-version it has no exception list, so on means no traffic from any other subnet in the network at all.",
                "type": "boolean"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Network/virtualNetworks/subnets",
        "type": "object"
      },
      "CyberCloud.Network.virtualNetworks.subnets.List": {
        "type": "object",
        "title": "CyberCloud.Network.virtualNetworks.subnets.List",
        "description": "One page of CyberCloud.Network/virtualNetworks/subnets. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Network.virtualNetworks.subnets"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Network.virtualNetworks.subnets.ListAddressUsageResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Network/virtualNetworks/subnets/listAddressUsage returns.",
        "properties": {
          "sampledAt": {
            "description": "When the fabric last reported these figures, RFC 3339. ⚠ Returned because a count with no timestamp is a count a caller will read as live, and these come from the Subnet object's status rather than from a live query.",
            "format": "date-time",
            "type": "string"
          },
          "v4": {
            "additionalProperties": false,
            "description": "IPv4 address usage.",
            "properties": {
              "available": {
                "description": "How many IPv4 addresses remain. ⚠ Zero here is the answer to 'why will nothing schedule in this subnet'.",
                "type": "integer"
              },
              "total": {
                "description": "How many IPv4 addresses the prefix contains that may be allocated, excluding the network address, the broadcast address and the gateway.",
                "type": "integer"
              },
              "used": {
                "description": "How many IPv4 addresses are currently allocated to ports.",
                "type": "integer"
              }
            },
            "required": [
              "available",
              "total",
              "used"
            ],
            "type": "object"
          },
          "v6": {
            "additionalProperties": false,
            "description": "IPv6 address usage.",
            "properties": {
              "available": {
                "description": "How many IPv6 addresses remain, as a decimal string, or empty for an IPv4-only subnet.",
                "type": "string"
              },
              "total": {
                "description": "How many IPv6 addresses the prefix contains, as a decimal string, or empty for an IPv4-only subnet. ⚠ A string because a /64 does not fit in the signed 64-bit integer SchemaKind.WholeNumber validates through.",
                "type": "string"
              }
            },
            "type": "object"
          }
        },
        "required": [
          "sampledAt"
        ],
        "title": "CyberCloud.Network.virtualNetworks.subnets.ListAddressUsageResponse",
        "type": "object"
      },
      "CyberCloud.Sample.widgets": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Sample/widgets: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the widget is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The widget's own settings.",
            "properties": {
              "allowedCidrs": {
                "description": "The ranges the widget claims to serve. Shape only; nothing enforces it.",
                "example": [
                  "10.0.0.0/8"
                ],
                "items": {
                  "pattern": "^(?:\\d{1,3}(\\.\\d{1,3}){3}/\\d{1,2})$",
                  "type": "string"
                },
                "type": "array",
                "x-cybercloud-widget": "cidr"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the widget's ConfigMap.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "enabled": {
                "default": false,
                "description": "Whether the widget is switched on. Defaults to off when absent.",
                "type": "boolean"
              },
              "message": {
                "description": "What the ConfigMap's 'message' key says.",
                "example": "hello",
                "maxLength": 1024,
                "minLength": 1,
                "type": "string"
              },
              "replicas": {
                "default": 1,
                "description": "How many copies of the ConfigMap's message the widget claims to hold.",
                "maximum": 9,
                "minimum": 1,
                "type": "integer"
              },
              "retiredOn": {
                "description": "When the widget was retired, or null while it is live.",
                "format": "date-time",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tier": {
                "default": "free",
                "description": "How much the widget's ConfigMap is allowed to say.",
                "enum": [
                  "free",
                  "basic",
                  "standard",
                  "premium"
                ],
                "type": "string",
                "x-cybercloud-widget": "sku"
              }
            },
            "required": [
              "clusterId",
              "message"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Sample/widgets",
        "type": "object"
      },
      "CyberCloud.Sample.widgets.List": {
        "type": "object",
        "title": "CyberCloud.Sample.widgets.List",
        "description": "One page of CyberCloud.Sample/widgets. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Sample.widgets"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Sample.widgets.PingRequest": {
        "additionalProperties": false,
        "description": "The body of a POST to CyberCloud.Sample/widgets/ping.",
        "properties": {
          "echo": {
            "default": "pong",
            "description": "Text the response repeats back.",
            "maxLength": 64,
            "type": "string"
          }
        },
        "title": "CyberCloud.Sample.widgets.PingRequest",
        "type": "object"
      },
      "CyberCloud.Sample.widgets.PingResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Sample/widgets/ping returns.",
        "properties": {
          "at": {
            "description": "When the ping was served.",
            "format": "date-time",
            "type": "string"
          },
          "echo": {
            "description": "What was echoed.",
            "type": "string"
          }
        },
        "required": [
          "at",
          "echo"
        ],
        "title": "CyberCloud.Sample.widgets.PingResponse",
        "type": "object"
      },
      "CyberCloud.Search.services": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Search/services: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the service is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The service's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the search service.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "coordinatingNodes": {
                "default": 0,
                "description": "Number of coordinating-only nodes — nodes that hold no data and no cluster state and exist to fan a search out and merge the results. Zero is the default and is right until a query pattern makes one data node the bottleneck for every search. They are sized by the same preset as the data nodes.",
                "maximum": 10,
                "minimum": 0,
                "type": "integer"
              },
              "dataNodes": {
                "default": 3,
                "description": "Number of data nodes. This is the capacity axis: total raw capacity is this count times the disk size below, before replicas. Every data node also carries the ingest role, so an indexing pipeline needs no separate pool.",
                "maximum": 20,
                "minimum": 1,
                "type": "integer"
              },
              "masterNodes": {
                "default": 3,
                "description": "Number of dedicated cluster-manager nodes. They hold the cluster state in a quorum, so three is the smallest count that survives losing one. One is offered for development and has no quorum at all. An even count is worse than the odd count below it and the API cannot say so — see the service's own documentation.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the operator is asked for a ServiceMonitor. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". ⚠ The metrics themselves come from the prometheus-exporter plugin, which the operator installs into every node on the first reconcile after this is turned on — so turning it on restarts the pods and turning it off does not remove the plugin.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per data node and per coordinating node, either by preset or explicitly. The cluster-manager nodes are sized by the platform and are not affected.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 8Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "m1.medium",
                    "description": "A sizing preset from docs/plan/12. Search nodes use the m1 family, which is 1 vCPU to 8 GiB, because OpenSearch is bound by heap and by the filesystem cache rather than by CPU. The m1 rungs below m1.small are deliberately not offered: OpenSearch derives its JVM heap from the container limit and a node under 4 GiB fails a bootstrap check after passing its readiness probe.",
                    "enum": [
                      "m1.2xlarge",
                      "m1.4xlarge",
                      "m1.large",
                      "m1.medium",
                      "m1.small",
                      "m1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume, per data node.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name for every node pool. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "100Gi",
                    "description": "Disk size per data node, in Kubernetes quantity form. Grows online; never shrinks. The cluster-manager and coordinating nodes get a fixed 10Gi that is not configurable and is counted against the storage quota anyway.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "3.1.0",
                "description": "OpenSearch version. ⚠ OpenSearch is a fork of Elasticsearch 7.10 (ADR-011 — Elasticsearch is SSPL and is not available to us) and the two have diverged since, so an Elasticsearch 8 client is not promised anything here. Upgrades between the values below are online and in the maintenance window; a third value is a new api-version.",
                "enum": [
                  "2.19.0",
                  "3.1.0"
                ],
                "type": "string"
              }
            },
            "required": [
              "clusterId",
              "coordinatingNodes",
              "dataNodes",
              "masterNodes",
              "version"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Search/services",
        "type": "object"
      },
      "CyberCloud.Search.services.List": {
        "type": "object",
        "title": "CyberCloud.Search.services.List",
        "description": "One page of CyberCloud.Search/services. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Search.services"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Search.services.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Search/services/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "endpoint": {
            "description": "The in-cluster REST endpoint, https://host:port. ⚠ No external address is returned, because there is none — see the service's own documentation on exposure.",
            "type": "string"
          },
          "password": {
            "description": "The administrative password, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          },
          "username": {
            "description": "The administrative user. Always \"admin\": the operator generates exactly one credential and does not name it.",
            "type": "string"
          }
        },
        "required": [
          "endpoint",
          "password",
          "username"
        ],
        "title": "CyberCloud.Search.services.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Storage.accounts": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Storage/accounts: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the account is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The account's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the object store.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "gateway": {
                "additionalProperties": false,
                "description": "The S3 gateway — docs/plan/15's ADR-008: \"the API is S3\".",
                "properties": {
                  "replicas": {
                    "default": 2,
                    "description": "Number of S3 gateway pods. The gateway is stateless, so this is a throughput and availability setting rather than a topology one.",
                    "maximum": 10,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "replicas"
                ],
                "type": "object"
              },
              "masters": {
                "default": 3,
                "description": "Number of master servers. The masters hold the volume topology in a Raft group, so three is the smallest count that survives losing one. One is offered for development and has no quorum at all.",
                "maximum": 5,
                "minimum": 1,
                "type": "integer"
              },
              "monitoring": {
                "additionalProperties": false,
                "description": "What the platform scrapes.",
                "properties": {
                  "enabled": {
                    "default": true,
                    "description": "Whether the operator is asked for a ServiceMonitor per component. On by default — docs/plan/12: \"a managed service the tenant cannot see the health of is a black box they will not trust with production\". Turning it off removes the metrics port as well as the scrape, which is the operator's own behaviour rather than this provider's.",
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "replication": {
                "default": "SameRack",
                "description": "Where the second copy of every object is placed. None keeps one copy and is for scratch data only. A placement the cluster's topology cannot satisfy leaves volumes read-only rather than failing the create. Immutable, because SeaweedFS applies this to volumes as they are created and never rewrites the ones that already exist — a change would split the account into two durability promises with no way to tell which object got which.",
                "enum": [
                  "DifferentDataCenter",
                  "DifferentRack",
                  "None",
                  "SameRack"
                ],
                "type": "string",
                "x-cybercloud-immutable": true
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory per volume server, either by preset or explicitly. The masters, the filer and the S3 gateway are sized by the platform and are not affected.",
                "properties": {
                  "cpu": {
                    "default": "",
                    "description": "Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "memory": {
                    "default": "",
                    "description": "Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  },
                  "preset": {
                    "default": "s1.small",
                    "description": "A sizing preset from docs/plan/12. Volume servers use the s1 family, which is 1 vCPU to 4 GiB.",
                    "enum": [
                      "s1.2xlarge",
                      "s1.4xlarge",
                      "s1.large",
                      "s1.medium",
                      "s1.micro",
                      "s1.nano",
                      "s1.small",
                      "s1.xlarge"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              },
              "storage": {
                "additionalProperties": false,
                "description": "The data volume, per volume server.",
                "properties": {
                  "class": {
                    "default": "",
                    "description": "StorageClass name for the volume servers. Empty means the cluster default.",
                    "type": "string",
                    "x-cybercloud-immutable": true,
                    "x-cybercloud-widget": "storageclass"
                  },
                  "size": {
                    "default": "100Gi",
                    "description": "Data volume size per volume server, in Kubernetes quantity form. Grows online; never shrinks.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "version": {
                "default": "4.41",
                "description": "SeaweedFS version. ⚠ SeaweedFS ships a release roughly weekly and maintains no long-term branch, so docs/plan/12's \"supported major versions\" is a shape this project does not have; the two values here are the two most recent releases and a new api-version is what adds a third.",
                "enum": [
                  "4.40",
                  "4.41"
                ],
                "type": "string"
              },
              "volumeServers": {
                "default": 3,
                "description": "Number of volume servers. This is the capacity axis: total raw capacity is this count times the volume size below, before replication.",
                "maximum": 16,
                "minimum": 1,
                "type": "integer"
              }
            },
            "required": [
              "clusterId",
              "masters",
              "replication",
              "version",
              "volumeServers"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Storage/accounts",
        "type": "object"
      },
      "CyberCloud.Storage.accounts.List": {
        "type": "object",
        "title": "CyberCloud.Storage.accounts.List",
        "description": "One page of CyberCloud.Storage/accounts. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Storage.accounts"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Storage.accounts.ListKeysResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Storage/accounts/listKeys returns. ⚠ Secret material. docs/plan/08 § The provider registry makes a secret action the only path a secret value leaves by; this schema is therefore the complete list of what does.",
        "properties": {
          "accessKeyId": {
            "description": "The access key id. Not secret on its own; useless without the pair below.",
            "type": "string"
          },
          "endpoint": {
            "description": "The in-cluster S3 endpoint, http://host:port. ⚠ No external address is returned, because there is none — see the account's own documentation on exposure.",
            "type": "string"
          },
          "region": {
            "description": "The region name an S3 client signs with. SeaweedFS does not route on it, and SigV4 refuses to sign without one, so it is returned rather than left for the caller to guess.",
            "type": "string"
          },
          "secretAccessKey": {
            "description": "The secret access key, read from the tenant's Vault for this call only.",
            "format": "password",
            "type": "string",
            "writeOnly": true,
            "x-cybercloud-secret": true
          }
        },
        "required": [
          "accessKeyId",
          "endpoint",
          "region",
          "secretAccessKey"
        ],
        "title": "CyberCloud.Storage.accounts.ListKeysResponse",
        "type": "object"
      },
      "CyberCloud.Storage.accounts.buckets": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Storage/accounts/buckets: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the bucket is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The bucket's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the bucket. Must be the cluster the account is in — nothing checks that, and a bucket placed elsewhere is applied into a namespace with no object store in it.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "quota": {
                "additionalProperties": false,
                "description": "How much the bucket may hold.",
                "properties": {
                  "size": {
                    "default": "",
                    "description": "The bucket's size limit, in Kubernetes quantity form. Empty means no limit other than the account's own provisioned capacity. ⚠ This is a limit inside capacity the account already reserved; it does not add any, and it is not what the bucket is billed on.",
                    "example": "50Gi",
                    "pattern": "^(?:(\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)?)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "versioning": {
                "default": false,
                "description": "Whether the bucket keeps previous versions of an overwritten object. Off by default. ⚠ docs/plan/15 § Object storage: SeaweedFS' S3 implementation is \"good but not complete\" and names object versioning as one of the partial areas, so the supported-operations table for the deployed version — not this property — is what says how completely it behaves.",
                "type": "boolean"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Storage/accounts/buckets",
        "type": "object"
      },
      "CyberCloud.Storage.accounts.buckets.List": {
        "type": "object",
        "title": "CyberCloud.Storage.accounts.buckets.List",
        "description": "One page of CyberCloud.Storage/accounts/buckets. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Storage.accounts.buckets"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Storage.accounts.buckets.StatsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Storage/accounts/buckets/stats returns.",
        "properties": {
          "objectCount": {
            "description": "How many objects the bucket holds, as of the last sample.",
            "type": "integer"
          },
          "sampledAt": {
            "description": "When the two figures above were sampled, RFC 3339. ⚠ Returned because a sampled number with no timestamp is a number a caller will read as live.",
            "format": "date-time",
            "type": "string"
          },
          "sizeBytes": {
            "description": "How many bytes the bucket holds before replication, as of the last sample. ⚠ Sampled rather than live — the operator refreshes every Bucket's status.usage from collection.list every five minutes — so it is not a number to write an assertion against immediately after a PUT.",
            "type": "integer"
          }
        },
        "required": [
          "objectCount",
          "sampledAt",
          "sizeBytes"
        ],
        "title": "CyberCloud.Storage.accounts.buckets.StatsResponse",
        "type": "object"
      },
      "CyberCloud.Storage.accounts.fileShares": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Storage/accounts/fileShares: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the share is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The share's own settings.",
            "properties": {
              "clusterId": {
                "description": "The cluster whose namespace holds the share. Must be the cluster the account is in — nothing checks that, and a share placed elsewhere is a claim against a driver whose filer reference resolves to nothing.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "quota": {
                "additionalProperties": false,
                "description": "How much the share may hold.",
                "properties": {
                  "size": {
                    "default": "100Gi",
                    "description": "The share's size, in Kubernetes quantity form. Enforced as a SeaweedFS collection quota on the mount. Grows online; never shrinks. ⚠ This is a ceiling inside capacity the account's volume servers already reserved; it does not add any, and docs/plan/15 § Metering bills the provisioned figure rather than what is used.",
                    "example": "100Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Storage/accounts/fileShares",
        "type": "object"
      },
      "CyberCloud.Storage.accounts.fileShares.List": {
        "type": "object",
        "title": "CyberCloud.Storage.accounts.fileShares.List",
        "description": "One page of CyberCloud.Storage/accounts/fileShares. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Storage.accounts.fileShares"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Storage.accounts.fileShares.ListMountTargetsResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Storage/accounts/fileShares/listMountTargets returns.",
        "properties": {
          "accessMode": {
            "description": "The access mode the claim was bound with. Always ReadWriteMany.",
            "type": "string"
          },
          "claimName": {
            "description": "The PersistentVolumeClaim a pod in the resource group's namespace names under volumes[].persistentVolumeClaim.claimName.",
            "type": "string"
          },
          "collection": {
            "description": "The SeaweedFS collection the share's quota is enforced on — `weed mount -collection=`. A mount that omits it writes outside the quota.",
            "type": "string"
          },
          "filer": {
            "description": "The account's filer, host:port, for a `weed mount -filer=` from a VM on the cluster network. ⚠ In-cluster only, for the reason the account's listKeys endpoint is.",
            "type": "string"
          },
          "path": {
            "description": "The filer path the share lives at — `weed mount -filer.path=`.",
            "type": "string"
          }
        },
        "required": [
          "accessMode",
          "claimName",
          "collection",
          "filer",
          "path"
        ],
        "title": "CyberCloud.Storage.accounts.fileShares.ListMountTargetsResponse",
        "type": "object"
      },
      "CyberCloud.Terminal.consoles": {
        "additionalProperties": false,
        "allOf": [
          {
            "$ref": "#/components/schemas/Resource"
          }
        ],
        "description": "A CyberCloud.Terminal/consoles: the envelope every resource is read in (allOf Resource, whose five members are readOnly and refused on a write) plus the body a caller writes — location, properties and tags. The body is generated from the provider registry's schema, which is the same object the write path validates against — docs/plan/08 § The provider registry.",
        "properties": {
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          },
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "location": {
            "description": "The region the console is billed in.",
            "example": "eu-central",
            "format": "cybercloud-region",
            "type": "string",
            "x-cybercloud-immutable": true,
            "x-cybercloud-widget": "region"
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "properties": {
            "additionalProperties": false,
            "description": "The console's own settings.",
            "properties": {
              "audit": {
                "additionalProperties": false,
                "description": "docs/plan/19 § Auditing. Who, when, from where, which subscription and for how long is always recorded and is not a setting.",
                "properties": {
                  "sessionRecording": {
                    "default": false,
                    "description": "Whether everything typed into and printed by this shell is recorded. ⚠ Off by default and immutable afterwards: a shell contains secrets, a keystroke log is a liability, and a recording that could be switched off mid-life would be worth nothing to the compliance requirement it exists for.",
                    "type": "boolean",
                    "x-cybercloud-immutable": true
                  }
                },
                "type": "object"
              },
              "clusterId": {
                "description": "The cluster whose namespace holds the shell and its home volume.",
                "format": "uuid",
                "type": "string",
                "x-cybercloud-immutable": true,
                "x-cybercloud-widget": "cluster"
              },
              "home": {
                "additionalProperties": false,
                "description": "The persistent home directory. The only part of a console that survives an idle reclaim.",
                "properties": {
                  "retentionDays": {
                    "default": 90,
                    "description": "How long $HOME is kept after the console was last attached to. ⚠ Nothing sweeps on it yet — it is carried so that a console created today is swept correctly by whatever does.",
                    "maximum": 365,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "size": {
                    "default": "5Gi",
                    "description": "The size of $HOME. It grows and never shrinks: a PersistentVolumeClaim refuses a decrease at the API.",
                    "example": "5Gi",
                    "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
                    "type": "string"
                  }
                },
                "required": [
                  "size"
                ],
                "type": "object"
              },
              "identity": {
                "additionalProperties": false,
                "description": "Who the shell runs as.",
                "properties": {
                  "principalId": {
                    "description": "The managed identity every command in this shell acts as. It may not be changed: a console whose identity moved would have an audit trail describing a shell that no longer exists.",
                    "format": "uuid",
                    "type": "string",
                    "x-cybercloud-immutable": true
                  }
                },
                "required": [
                  "principalId"
                ],
                "type": "object"
              },
              "image": {
                "additionalProperties": false,
                "description": "Which shell image to run.",
                "properties": {
                  "variant": {
                    "default": "default",
                    "description": "default carries every tool in docs/plan/19's table and takes about 40 seconds to pull cold; minimal carries shells, editors, cyc and kubectl and starts in about 8. The repository and the digest are the platform's.",
                    "enum": [
                      "default",
                      "minimal"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "network": {
                "additionalProperties": false,
                "description": "What the shell may reach. Nothing may ever reach the shell.",
                "properties": {
                  "egress": {
                    "default": "Internet",
                    "description": "Internet lets the shell reach public addresses as well as this subscription's own workloads and DNS — a shell that cannot git clone is not a shell. TenantOnly removes the public half and leaves the rest.",
                    "enum": [
                      "Internet",
                      "TenantOnly"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "session": {
                "additionalProperties": false,
                "description": "When a shell stops. Both numbers are ceilings a tenant may lower and neither may be turned off.",
                "properties": {
                  "idleTimeoutMinutes": {
                    "default": 20,
                    "description": "How long a shell may sit with nobody typing into it before it is reclaimed. The home volume survives; the pod does not. ⚠ There is no value meaning never — an idle terminal is a tenant paying for something they closed.",
                    "maximum": 120,
                    "minimum": 5,
                    "type": "integer"
                  },
                  "maxDurationHours": {
                    "default": 8,
                    "description": "The longest a single shell may run, busy or not. Enforced by the kubelet through the pod's own deadline, so it holds even when nothing of this platform is running.",
                    "maximum": 24,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "type": "object"
              },
              "sizing": {
                "additionalProperties": false,
                "description": "CPU and memory for the shell.",
                "properties": {
                  "preset": {
                    "default": "c1.small",
                    "description": "How much a shell gets. The ladder stops at 2 vCPU and 4 GiB, which is docs/plan/19's ceiling for this row — a workload that needs more needs a cluster and a job rather than a terminal.",
                    "enum": [
                      "c1.large",
                      "c1.medium",
                      "c1.small"
                    ],
                    "type": "string",
                    "x-cybercloud-widget": "cozy-preset"
                  }
                },
                "type": "object"
              }
            },
            "required": [
              "clusterId"
            ],
            "type": "object"
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "tags": {
            "type": "object",
            "description": "Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.",
            "additionalProperties": {
              "type": "string"
            },
            "maxProperties": 50,
            "x-cybercloud-widget": "tag-input"
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          }
        },
        "required": [
          "location"
        ],
        "title": "CyberCloud.Terminal/consoles",
        "type": "object"
      },
      "CyberCloud.Terminal.consoles.ConnectResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Terminal/consoles/connect returns.",
        "properties": {
          "hub": {
            "description": "The SignalR hub path to open. docs/plan/10 § SignalR.",
            "type": "string"
          },
          "idleTimeoutSeconds": {
            "description": "How long this shell may sit idle before it is reclaimed. Shown to the person, because a terminal that vanishes without warning reads as a bug.",
            "type": "integer"
          },
          "maxDurationSeconds": {
            "description": "How long this shell may run in total before the kubelet stops it.",
            "type": "integer"
          },
          "recording": {
            "description": "Whether this session is being recorded. ⚠ docs/plan/19 § Auditing requires the portal to be loud about this, which is why it is on the connect response rather than only on the resource body: a panel that had to fetch the resource to find out would render one frame of a terminal that lies.",
            "type": "boolean"
          },
          "sessionId": {
            "description": "The session to name on the terminal hub. ⚠ It is the shell pod's own UID, so it changes when a reclaimed console is re-created — which is exactly when a client's replay buffer has become meaningless.",
            "type": "string"
          },
          "state": {
            "description": "Starting while the shell is still being scheduled, Ready once it is running. A client opens the hub either way and sees output when there is some.",
            "type": "string"
          }
        },
        "required": [
          "hub",
          "idleTimeoutSeconds",
          "maxDurationSeconds",
          "recording",
          "sessionId",
          "state"
        ],
        "title": "CyberCloud.Terminal.consoles.ConnectResponse",
        "type": "object"
      },
      "CyberCloud.Terminal.consoles.List": {
        "type": "object",
        "title": "CyberCloud.Terminal.consoles.List",
        "description": "One page of CyberCloud.Terminal/consoles. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The resources on this page, ordered by id. Each element is exactly what a GET of that resource returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/CyberCloud.Terminal.consoles"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "CyberCloud.Terminal.consoles.TerminateResponse": {
        "additionalProperties": false,
        "description": "What CyberCloud.Terminal/consoles/terminate returns.",
        "properties": {
          "terminated": {
            "description": "True when this call stopped a running shell, false when there was none. ⚠ Both are success: the caller's goal is that no shell is running.",
            "type": "boolean"
          }
        },
        "required": [
          "terminated"
        ],
        "title": "CyberCloud.Terminal.consoles.TerminateResponse",
        "type": "object"
      },
      "Error": {
        "type": "object",
        "description": "One error. docs/plan/08 § Errors.",
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode"
          },
          "details": {
            "type": "array",
            "description": "Every other problem found. A form that has to be fixed one field per round trip is a form nobody finishes.",
            "items": {
              "$ref": "#/components/schemas/Error"
            }
          },
          "message": {
            "type": "string",
            "description": "For a human, naming the actual numbers. 'Quota exceeded' without the meter, the request and the remainder is a support ticket by construction."
          },
          "target": {
            "type": "string",
            "description": "An RFC 6901 JSON Pointer into the request body, so the portal can highlight the field."
          }
        },
        "required": [
          "code",
          "message"
        ],
        "additionalProperties": false
      },
      "ErrorCode": {
        "type": "string",
        "description": "A stable, documented, greppable identifier. ⚠ It is part of the API contract and changing one is a breaking change — docs/plan/08 § Errors. The closed set is CyberCloud.Core.ErrorCode.All.",
        "enum": [
          "AuthorizationFailed",
          "Conflict",
          "InternalError",
          "InvalidApiVersion",
          "InvalidGrainKey",
          "InvalidRequestBody",
          "InvalidResourceId",
          "InvalidResourceName",
          "InvalidResourceType",
          "OperationCanceled",
          "OperationInProgress",
          "OperationTimeout",
          "PolicyViolation",
          "PreconditionFailed",
          "ProvisioningFailed",
          "QuotaExceeded",
          "ResourceAlreadyExists",
          "ResourceGroupNotFound",
          "ResourceHasChildren",
          "ResourceNotFound",
          "SchemaInvalid",
          "ScopeLocked",
          "SubscriptionNotFound",
          "TenantNotFound",
          "TenantSuspended"
        ],
        "x-cybercloud-http-status": {
          "AuthorizationFailed": 403,
          "Conflict": 409,
          "InternalError": 500,
          "InvalidApiVersion": 400,
          "InvalidGrainKey": 400,
          "InvalidRequestBody": 400,
          "InvalidResourceId": 400,
          "InvalidResourceName": 400,
          "InvalidResourceType": 400,
          "OperationCanceled": 409,
          "OperationInProgress": 409,
          "OperationTimeout": 500,
          "PolicyViolation": 403,
          "PreconditionFailed": 412,
          "ProvisioningFailed": 500,
          "QuotaExceeded": 429,
          "ResourceAlreadyExists": 409,
          "ResourceGroupNotFound": 404,
          "ResourceHasChildren": 409,
          "ResourceNotFound": 404,
          "SchemaInvalid": 404,
          "ScopeLocked": 409,
          "SubscriptionNotFound": 404,
          "TenantNotFound": 404,
          "TenantSuspended": 403
        }
      },
      "ErrorResponse": {
        "type": "object",
        "description": "One shape, everywhere, Azure's. docs/plan/08 § Errors.",
        "properties": {
          "error": {
            "$ref": "#/components/schemas/Error"
          }
        },
        "required": [
          "error"
        ],
        "additionalProperties": false
      },
      "OperationProgress": {
        "type": "object",
        "description": "One progress entry. The array is this platform's addition to Azure's pattern and is what makes a nine-minute cluster creation tolerable — docs/plan/10.",
        "properties": {
          "at": {
            "type": "string",
            "format": "date-time"
          },
          "message": {
            "type": "string",
            "description": "What happened, naming the actual numbers."
          },
          "percentComplete": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "step": {
            "type": "string",
            "description": "The phase a reconciler reported — applying, waiting-for-ready."
          }
        },
        "required": [
          "at",
          "step"
        ],
        "additionalProperties": false
      },
      "OperationState": {
        "type": "string",
        "description": "Azure's status vocabulary. ⚠ Canceled is reached only after the delete path has run for everything already applied — cancellation completes rather than abandoning.",
        "enum": [
          "Canceled",
          "Failed",
          "NotStarted",
          "Running",
          "Succeeded"
        ]
      },
      "OperationStatus": {
        "type": "object",
        "description": "An operation's current state. ⚠ The members are docs/plan/10 § Long-running operations, over HTTP's, which is a subset of the OperationStatus wire type — no HTTP projection of that record is declared anywhere in the tree. id, status, startTime, percentComplete and progress are always present; endTime once the status is terminal; error once it is Failed.",
        "properties": {
          "endTime": {
            "type": "string",
            "format": "date-time",
            "description": "When the operation reached a terminal status. Absent until it does."
          },
          "error": {
            "$ref": "#/components/schemas/Error"
          },
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The operation id — the last segment of the URL this was polled from."
          },
          "percentComplete": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "progress": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OperationProgress"
            }
          },
          "startTime": {
            "type": "string",
            "format": "date-time",
            "description": "When the write was accepted."
          },
          "status": {
            "$ref": "#/components/schemas/OperationState"
          }
        },
        "required": [
          "status"
        ],
        "additionalProperties": false,
        "x-cybercloud-read-required": [
          "id",
          "percentComplete",
          "progress",
          "startTime",
          "status"
        ]
      },
      "Resource": {
        "type": "object",
        "title": "Resource",
        "description": "The envelope every resource is read in — docs/plan/10 § Request pipeline, step 9. Every type's schema is allOf this: a GET, a list element and a 202 all carry these five members beside the body the type declares, in this order, then tags. ⚠ All five are readOnly and none is required, although a read always carries every one — the same schema validates a PUT, and this platform refuses a read-only member on a write rather than ignoring it (docs/plan/08 § The write path, end to end). x-cybercloud-read-required lists what a read guarantees.",
        "properties": {
          "id": {
            "type": "string",
            "description": "The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.",
            "readOnly": true
          },
          "name": {
            "type": "string",
            "description": "The last segment of the path: the name the caller chose on the PUT.",
            "readOnly": true
          },
          "type": {
            "type": "string",
            "description": "The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.",
            "readOnly": true
          },
          "provisioningState": {
            "type": "string",
            "description": "Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.",
            "enum": [
              "Canceled",
              "Creating",
              "Deleting",
              "Failed",
              "Succeeded",
              "Updating"
            ],
            "readOnly": true
          },
          "etag": {
            "type": "string",
            "description": "The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.",
            "readOnly": true
          }
        },
        "x-cybercloud-read-required": [
          "etag",
          "id",
          "name",
          "provisioningState",
          "type"
        ]
      },
      "Scope": {
        "type": "object",
        "title": "Scope",
        "description": "A tenant, a subscription or a resource group, as the API renders it. ⚠ There is no provisioningState: a scope is one grain activation and converges before the call returns, which is the visible half of \"a scope is not a resource\".",
        "properties": {
          "id": {
            "type": "string",
            "description": "The scope's own path — docs/plan/06 § Identifiers."
          },
          "location": {
            "type": "string",
            "description": "The region: a tenant's home region or a group's default. ⚠ Absent rather than empty where the scope has none, so a client tests for the property instead of comparing against \"\"."
          },
          "name": {
            "type": "string",
            "description": "The name a human reads."
          },
          "type": {
            "type": "string",
            "description": "The Azure-shaped type string.",
            "enum": [
              "CyberCloud.Resources/subscriptions/resourceGroups",
              "CyberCloud.Resources/subscriptions",
              "CyberCloud.Resources/tenants"
            ]
          }
        },
        "required": [
          "id",
          "name",
          "type"
        ],
        "additionalProperties": false
      },
      "Scope.List": {
        "type": "object",
        "title": "Scope.List",
        "description": "One page of scopes — a tenant's subscriptions or a subscription's resource groups. ⚠ The page holds what the caller may read: a listing runs a permission check per member (docs/plan/07 § The enforcement seam), so a short or empty page means \"that is what you may see\" and never \"that is all there is\". Stop when nextLink is absent, never when a page is smaller than you asked for.",
        "properties": {
          "nextLink": {
            "type": "string",
            "description": "The absolute URL of the next page. Absent on the last page — there is no empty-string form, because an empty URL is one a polite client requests."
          },
          "value": {
            "type": "array",
            "description": "The scopes on this page, ordered by id. Each element is exactly what a GET of that scope returns, member for member.",
            "items": {
              "$ref": "#/components/schemas/Scope"
            }
          }
        },
        "required": [
          "value"
        ],
        "additionalProperties": false
      },
      "Scope.ResourceGroupCreate": {
        "type": "object",
        "title": "Scope.ResourceGroupCreate",
        "description": "The body of a PUT that creates a resource group.",
        "properties": {
          "location": {
            "type": "string",
            "description": "The region the group's resources default to. Required, and there is no platform-wide default to fall back on: a group whose region were guessed would place a tenant's data somewhere nobody chose.",
            "x-cybercloud-widget": "region"
          }
        },
        "required": [
          "location"
        ],
        "additionalProperties": false
      },
      "Scope.SubscriptionCreate": {
        "type": "object",
        "title": "Scope.SubscriptionCreate",
        "description": "The body of a PUT that creates a subscription.",
        "properties": {
          "displayName": {
            "type": "string",
            "description": "The name on an invoice and in every scope picker. Required — a subscription identified only by its GUID is one nobody can pick out of a list."
          }
        },
        "required": [
          "displayName"
        ],
        "additionalProperties": false
      }
    }
  },
  "x-cybercloud-resource-type-count": 33
}
