{
  "$id": "https://schema.cybercloud.io/charts/container-group/values.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The configuration surface of the container-group chart. GENERATED from values.yaml by `./build.sh Charts` and diffed in CI — docs/plan/03 § charts/. Edit values.yaml, never this file. Members carrying `x-cybercloud-api: false` are chart or platform plumbing and are not part of the resource body.",
  "properties": {
    "command": {
      "default": [],
      "description": "The main container's command, replacing its image's entrypoint — for example [\"sh\", \"-c\", \"echo hello; sleep 3600\"]. Empty runs the image as built.",
      "examples": [
        [
          "sh",
          "-c",
          "echo hello; sleep 3600"
        ]
      ],
      "items": {},
      "type": "array",
      "x-cybercloud-order": 1,
      "x-cybercloud-pointer": "/properties/command"
    },
    "containers": {
      "default": [],
      "description": "The containers, each name=image — for example web=nginx:1.27 or app=myregistry.example/team/app:2.1. One to ten; names are lower-case DNS labels and unique in the group. The first is the main container: the command and the ports are its.",
      "examples": [
        [
          "web=nginx:1.27",
          "log=busybox:1.37"
        ]
      ],
      "items": {},
      "type": "array",
      "x-cybercloud-order": 0,
      "x-cybercloud-pointer": "/properties/containers"
    },
    "cpu": {
      "default": "500m",
      "description": "The CPU the whole group may use, as a Kubernetes quantity — 500m is half a core. Reserved against your vCPU quota and enforced on the pod as one limit its containers share.",
      "examples": [
        "1"
      ],
      "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
      "type": "string",
      "x-cybercloud-order": 2,
      "x-cybercloud-pointer": "/properties/cpu"
    },
    "environment": {
      "default": [],
      "description": "Environment variables every container sees, each NAME=value. ⚠ The value is stored in this body in plaintext — anything secret goes in secureEnvironment.",
      "examples": [
        [
          "LOG_LEVEL=info"
        ]
      ],
      "items": {},
      "type": "array",
      "x-cybercloud-order": 5,
      "x-cybercloud-pointer": "/properties/environment"
    },
    "memory": {
      "default": "256Mi",
      "description": "The memory the whole group may use, as a Kubernetes quantity. Reserved against your memory quota and enforced on the pod as one limit.",
      "examples": [
        "1Gi"
      ],
      "pattern": "^(?:\\d+(\\.\\d+)?(m|k|M|G|T|P|E|Ki|Mi|Gi|Ti|Pi|Ei)?)$",
      "type": "string",
      "x-cybercloud-order": 3,
      "x-cybercloud-pointer": "/properties/memory"
    },
    "nameOverride": {
      "default": "",
      "description": "Replace the generated object-name stem. Empty means derive it from the release name.",
      "type": "string",
      "x-cybercloud-api": false,
      "x-cybercloud-internal-reason": "Helm plumbing. Nothing about it belongs in a resource body.",
      "x-cybercloud-order": 11,
      "x-cybercloud-pointer": "/properties/nameOverride"
    },
    "network": {
      "additionalProperties": false,
      "description": "Where the group's ports are reached. All empty means the cluster's pod network, reachable from nothing a tenant owns.",
      "properties": {
        "ipAddress": {
          "default": "",
          "description": "The IPv4 address the group answers on, inside the subnet's range, or empty for one the fabric picks. ⚠ Only with a subnet.",
          "pattern": "^(?:((\\d{1,3}\\.){3}\\d{1,3})?)$",
          "type": "string",
          "x-cybercloud-immutable": true,
          "x-cybercloud-order": 2,
          "x-cybercloud-pointer": "/properties/network/ipAddress",
          "x-cybercloud-widget": "cidr"
        },
        "publicIpAddress": {
          "default": "",
          "description": "A CyberCloud.Network/publicIpAddresses resource in this resource group, by name, translated one-to-one onto the group's address — every port the group listens on is reachable at it. Empty for none. ⚠ Only with a subnet.",
          "maxLength": 63,
          "pattern": "^(?:([a-z0-9]([-a-z0-9]*[a-z0-9])?)?)$",
          "type": "string",
          "x-cybercloud-immutable": true,
          "x-cybercloud-order": 3,
          "x-cybercloud-pointer": "/properties/network/publicIpAddress"
        },
        "subnet": {
          "default": "",
          "description": "The subnet of that network the group takes its address from, by name, or empty. ⚠ A name that is not a subnet of the network is refused by the fabric rather than by this API, and the group never starts.",
          "maxLength": 63,
          "pattern": "^(?:([a-z0-9]([-a-z0-9]*[a-z0-9])?)?)$",
          "type": "string",
          "x-cybercloud-immutable": true,
          "x-cybercloud-order": 1,
          "x-cybercloud-pointer": "/properties/network/subnet",
          "x-cybercloud-widget": "subnet"
        },
        "virtualNetwork": {
          "default": "",
          "description": "The CyberCloud.Network/virtualNetworks resource in this resource group, by name, or empty.",
          "maxLength": 63,
          "pattern": "^(?:([a-z0-9]([-a-z0-9]*[a-z0-9])?)?)$",
          "type": "string",
          "x-cybercloud-immutable": true,
          "x-cybercloud-order": 0,
          "x-cybercloud-pointer": "/properties/network/virtualNetwork"
        }
      },
      "type": "object",
      "x-cybercloud-order": 9,
      "x-cybercloud-pointer": "/properties/network"
    },
    "platform": {
      "additionalProperties": false,
      "description": "Resource identity injected by the reconciler. Not a tenant-facing setting.",
      "properties": {
        "apiVersion": {
          "default": "2026-08-01",
          "description": "The api-version the resource was last written at, rendered into the cybercloud.io/api-version label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 5,
          "x-cybercloud-pointer": "/properties/platform/apiVersion"
        },
        "managedBy": {
          "default": "cybercloud",
          "description": "Value of the cybercloud.io/managed-by label. Constant, and the selector every informer filters on.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 6,
          "x-cybercloud-pointer": "/properties/platform/managedBy"
        },
        "resourceGroup": {
          "default": "",
          "description": "Resource group name, rendered into the cybercloud.io/resource-group label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 2,
          "x-cybercloud-pointer": "/properties/platform/resourceGroup"
        },
        "resourceId": {
          "default": "",
          "description": "Resource GUID, rendered into the cybercloud.io/resource-id label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 3,
          "x-cybercloud-pointer": "/properties/platform/resourceId"
        },
        "resourceType": {
          "default": "CyberCloud.ContainerInstance/containerGroups",
          "description": "Fully qualified resource type, rendered into the cybercloud.io/resource-type label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 4,
          "x-cybercloud-pointer": "/properties/platform/resourceType"
        },
        "subscriptionId": {
          "default": "",
          "description": "Subscription GUID, rendered into the cybercloud.io/subscription-id label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 1,
          "x-cybercloud-pointer": "/properties/platform/subscriptionId"
        },
        "tenantId": {
          "default": "",
          "description": "Tenant GUID, rendered into the cybercloud.io/tenant-id label.",
          "type": "string",
          "x-cybercloud-api": false,
          "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
          "x-cybercloud-order": 0,
          "x-cybercloud-pointer": "/properties/platform/tenantId"
        }
      },
      "type": "object",
      "x-cybercloud-api": false,
      "x-cybercloud-internal-reason": "Written by the reconciler from the resource's own identity. A tenant cannot set it, so it is not part of the resource body.",
      "x-cybercloud-order": 10,
      "x-cybercloud-pointer": "/properties/platform"
    },
    "ports": {
      "default": [],
      "description": "The ports the main container listens on, each a number with an optional /TCP, /UDP or /SCTP — for example 80 or 53/UDP. Reached at the group's address on its subnet, and at its public address when it has one.",
      "examples": [
        [
          "80",
          "53/UDP"
        ]
      ],
      "items": {},
      "type": "array",
      "x-cybercloud-order": 7,
      "x-cybercloud-pointer": "/properties/ports"
    },
    "registry": {
      "additionalProperties": false,
      "description": "The credential a private registry's images are pulled with. All empty means every image is public.",
      "properties": {
        "password": {
          "default": "",
          "description": "A vault handle — path#field, optionally @version — whose value is the password or token. For a CyberCloud.ContainerRegistry that is its own credential: tenants/<tenantId>/CyberCloud.ContainerRegistry/registries/<registryId>#adminPassword. ⚠ Under your own tenant's prefix, or refused.",
          "maxLength": 512,
          "pattern": "^(?:([^#@\\s]+#[^#@\\s]+(@[^#@\\s]+)?)?)$",
          "type": "string",
          "x-cybercloud-order": 2,
          "x-cybercloud-pointer": "/properties/registry/password",
          "x-cybercloud-widget": "secret-ref"
        },
        "server": {
          "default": "",
          "description": "The registry's host, with a port when it is not 443 — for example registry.example.com. ⚠ The kubelet resolves it from the node, not from inside the cluster.",
          "maxLength": 253,
          "pattern": "^(?:([a-z0-9]([-a-z0-9.]*[a-z0-9])?(:\\d{1,5})?)?)$",
          "type": "string",
          "x-cybercloud-order": 0,
          "x-cybercloud-pointer": "/properties/registry/server"
        },
        "username": {
          "default": "",
          "description": "The user the registry knows — admin for a CyberCloud.ContainerRegistry, as its listCredentials answers.",
          "maxLength": 256,
          "type": "string",
          "x-cybercloud-order": 1,
          "x-cybercloud-pointer": "/properties/registry/username"
        }
      },
      "type": "object",
      "x-cybercloud-order": 8,
      "x-cybercloud-pointer": "/properties/registry"
    },
    "restartPolicy": {
      "default": "Always",
      "description": "Always restarts a container whenever it exits; OnFailure only when it exits non-zero; Never lets the group run once — a batch job — and the group stays Succeeded with its logs readable.",
      "enum": [
        "Always",
        "OnFailure",
        "Never"
      ],
      "type": "string",
      "x-cybercloud-order": 4,
      "x-cybercloud-pointer": "/properties/restartPolicy"
    },
    "secureEnvironment": {
      "default": [],
      "description": "Environment variables whose values are vault handles, each NAME=path#field, optionally @version. Resolved when the group is rendered into a Secret the containers read; the values never enter this body. ⚠ Every path must be under your own tenant's vault prefix, tenants/<tenantId>/; any other is refused.",
      "examples": [
        [
          "DB_PASSWORD=tenants/00000000-0000-0000-0000-000000000000/db#password"
        ]
      ],
      "items": {},
      "type": "array",
      "x-cybercloud-order": 6,
      "x-cybercloud-pointer": "/properties/secureEnvironment"
    }
  },
  "required": [
    "containers",
    "cpu",
    "memory"
  ],
  "title": "container-group chart values",
  "type": "object",
  "x-cybercloud-api-version": "2026-08-01",
  "x-cybercloud-chart": "container-group",
  "x-cybercloud-chart-version": "0.1.0",
  "x-cybercloud-resource-type": "CyberCloud.ContainerInstance/containerGroups"
}
