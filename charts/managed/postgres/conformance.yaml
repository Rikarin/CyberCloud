# Conformance manifest — docs/plan/12 § The pattern, once, piece 8.
#
# What the conformance suite asserts about CyberCloud.DBforPostgreSQL/servers. This file is data:
# the suite that reads it lives in test/ and does not exist yet, so today `Build.Charts` checks only
# that the file is present, non-empty, and names the same resource type as Chart.yaml. That is a
# weaker check than it will be and the target says so in its own log rather than implying otherwise.

resourceType: CyberCloud.DBforPostgreSQL/servers
apiVersion: "2026-08-01"
chart: postgres

# Objects the reconciler must have applied once the resource reports Succeeded.
renders:
  - group: postgresql.cnpg.io
    version: v1
    kind: Cluster
    count: 1
  - group: postgresql.cnpg.io
    version: v1
    kind: Pooler
    count: 1
    when: pooling.enabled

# ADR-013 — every rendered object carries all seven labels. Asserted against real rendered output,
# which is what the Labels architecture gate in docs/plan/23 § The architecture gates requires.
labels:
  required:
    - cybercloud.io/tenant-id
    - cybercloud.io/subscription-id
    - cybercloud.io/resource-group
    - cybercloud.io/resource-id
    - cybercloud.io/resource-type
    - cybercloud.io/api-version
    - cybercloud.io/managed-by
  # ⚠ Not the resource type verbatim. A `/` is not a legal label value character, so the value is
  # lower-cased with `/` replaced by `_` — KubeLabels.ResourceTypeValue.
  values:
    cybercloud.io/resource-type: cybercloud.dbforpostgresql_servers
    cybercloud.io/managed-by: cybercloud

# Behaviour a tenant is entitled to. Each is a statement the suite can fail on, not a feature list.
assertions:
  - id: connect-in-cluster
    says: A client connects to the read-write Service with the credentials from listKeys and runs SELECT 1.
  - id: pooler-is-the-default-endpoint
    says: With pooling.enabled the advertised connection string points at the Pooler, not at the cluster Service, so turning pooling off later is a visible change rather than a silent one.
  - id: failover-preserves-committed-writes
    says: Deleting the primary pod promotes a replica and every transaction that returned success before the delete is still readable afterwards.
  - id: pitr-window-is-the-retention-days
    says: A restore to a timestamp retentionDays-minus-one days old succeeds; a restore older than retentionDays fails with a stated error rather than an empty database.
  - id: extensions-are-the-allow-list
    says: Every value the schema's extensions enum permits installs; anything else is refused by the API before it reaches a cluster.
  - id: storage-grows-never-shrinks
    says: Increasing storage.size resizes online; decreasing it is refused at the API.
  - id: secrets-never-round-trip
    says: A GET of the resource never returns bootstrap.password in any form. The only path out is the listKeys action.

# Meters the type must emit — docs/plan/12 § The pattern, once, piece 3. Listed here so the
# conformance suite and the provider's Describe can be diffed against one file rather than agreeing
# by coincidence.
meters:
  - postgres.instance.hours
  - postgres.storage.gib.hours
  - postgres.backup.gib.hours

# ── owed ──────────────────────────────────────────────────────────────────────────────────────
#
# Named here rather than left to be discovered, because an absent capability that nobody wrote down
# is indistinguishable from one nobody thought of. The convention is charts/managed/ferretdb's.
owed:
  - id: a-named-initdb-secret-kept-every-server-from-starting
    status: closed
    closed: 2026-09-17
    says: >
      ⚠ NO MANAGED POSTGRESQL HAD EVER STARTED, AND THE FIRST RUN WITH THE OPERATOR PRESENT IS WHAT
      SAID SO. PostgresServers.ClusterJson rendered `bootstrap.initdb.secret: { name: <name>-app }`
      while its own remarks said the seam was deliberately left unrendered so that CloudNativePG
      would generate the owner's password. Read against v1.30.0 (api/v1/cluster_funcs.go),
      `ShouldInitDBCreateApplicationSecret` is true only while the reference is nil or its name is
      empty: a NAMED secret is one the operator expects somebody else to have written, and nothing in
      this platform writes it. The initdb Job mounts it and the kubelet answers
      `CreateContainerConfigError: secret "story-db-app" not found`, forever. The Cluster object reads
      back matching its spec, so PostgresServerReconciler reported Converged over it — Converged has
      never meant "PostgreSQL answers", and this is the row where that distinction cost a database.
      ⚠ A UNIT TEST PINNED THE DEFECT AS CORRECT: PostgresSecretTests required the name to be there,
      on the reasoning that the operator reads the password out of a Secret named in the CR. It does,
      and that is exactly why naming one the platform does not write is fatal. The test now asserts
      the reference is absent and says why. No fake API server in the tree could have shown the
      difference; test/CyberCloud.Bundle.Cluster.Conformance § M1StoryOnAFreshCluster — the first
      test that installs cloudnative-pg through charts/bundle/install.sh and then drives a server
      through the real write path — failed on its first run with the message above and passed once
      the reference was removed, which is the sabotage reading in both directions.
      ✅ WHAT THAT TEST NOW PROVES FOR THIS CHART, AND WHAT IT DOES NOT. `connect-in-cluster` above is
      exercised: the story fetches the credential through listKeys, and `psql` inside the primary
      connects to the `<name>-rw` Service with it and answers SELECT 1. With `pooling.enabled=false`,
      so `pooler-is-the-default-endpoint` is NOT exercised — a Pooler would add a PgBouncer pull to a
      lane measured in minutes; the host asserted is `<name>-rw`, which is the pooling-off answer.
      Not exercised either, and still owed: `failover-preserves-committed-writes`,
      `pitr-window-is-the-retention-days`, `extensions-are-the-allow-list` against a real cluster,
      `storage-grows-never-shrinks`, and the operator's half of
      `the-recovery-window-is-hollow-because-cloudnativepg-owns-its-claims` — the story creates and
      never deletes, so no claim has yet been adopted by a re-created Cluster under a real operator.
      Each is one more step on the same fixture, and the fixture exists now.
  - id: shared-preload-libraries-is-not-a-parameter
    status: closed
    closed: 2026-08-12
    says: >
      ⚠ CLOSED, AND KEPT BECAUSE THE SHAPE OF THE MISS IS WORTH MORE THAN THE FIX.
      PostgresServers.ClusterJson and templates/cluster.yaml both wrote
      `spec.postgresql.parameters.shared_preload_libraries` whenever /properties/extensions was
      non-empty. CloudNativePG declares that key as `AdditionalLibraries []string` on
      PostgresConfiguration — a SIBLING of `parameters` (api/v1/cluster_types.go) — lists it in
      FixedConfigurationParameters (pkg/postgres/configuration.go), and its validating webhook walks
      spec.postgresql.parameters and answers a fixed key with "Can't set fixed configuration
      parameter" (internal/webhook/v1/cluster_webhook.go). So every server created with pgvector,
      postgis, pg_stat_statements or timescaledb was refused at admission AFTER the caller was told
      202. Both spellings now render a list beside `parameters`.
      ⚠ THE DEFAULT IS `[]`, WHICH IS THE WHOLE REASON IT SURVIVED. Every test and every manual run
      used a body that named no extension, so the one placement that is refused was the one nothing
      exercised, and the suite that would notice cannot: the cluster-backed conformance suites run
      against a bare k3s with no CloudNativePG operator and no admission webhook, so a green cluster
      run is not evidence about this and never was.
      ⚠ AND IT WAS FOUND BY WRITING THE SECOND RENDERING, not by reading the first.
      CyberCloud.Providers.DocumentDB renders the same CRD independently — src/Providers/README.md
      § Hard rule forbids sharing a renderer — and got it right, which is what made the disagreement
      visible. Recorded at charts/managed/ferretdb/conformance.yaml § owed until 2026-08-12.
  - id: the-cluster-pair-is-held-together-by-one-test
    says: >
      templates/cluster.yaml and PostgresServers.ClusterJson are two hand-maintained spellings of one
      CloudNativePG Cluster and NOTHING in ./build.sh compares them. ChartSurfaces filters
      `templates/` out of the chart tree on purpose and build/Build.Charts.cs line-filters the same
      directory, so no emitter has ever read a Helm template — ADR-012's fifth surface reaches
      values.yaml and stops there. What holds the pair together is
      ChartRegistryPairTests.TheChartPutsThePreloadLibrariesBesideParametersAndNotInsideThem and
      PostgresReconcilerTests.ThePreloadLibrariesAreASiblingOfParametersInBothSpellings, which assert
      one field each by regular expression over the template. ⚠ That is a check per field somebody
      remembered, not a check of the object, and `shared-preload-libraries-is-not-a-parameter` above
      is what a field nobody remembered costs. Closing it is an emitter that renders the chart and
      diffs it against the registry's projection, which is a seam ChartSurfaces does not have.
  - id: extension-names-are-not-library-names
    status: closed
    closed: 2026-08-18
    says: >
      ⚠ CLOSED, AND THE FINDING IS KEPT BECAUSE THE FIX IS A TABLE AND THE DEFECT WAS THE ABSENCE OF
      ONE. PostgresServers.ExtensionCatalogue and the `$catalogue` dict at the top of
      templates/cluster.yaml now map each allowed value to the extension it installs as and the
      library it needs preloaded, and ClusterJson reads them as two lists rather than one:
      `pgvector` renders `CREATE EXTENSION IF NOT EXISTS vector;` and NO preload entry, `postgis`
      likewise, and only `pg_stat_statements` and `timescaledb` reach
      `spec.postgresql.shared_preload_libraries` — under their own names. Read upstream on
      2026-08-18: pgvector's README says `CREATE EXTENSION vector` and asks for no preload;
      PostgreSQL's own pg_stat_statements page says the module "must be loaded by adding
      pg_stat_statements to shared_preload_libraries … because it requires additional shared memory";
      timescaledb's `extension_load_without_preload` (src/extension_utils.c) raises "extension
      \"timescaledb\" must be preloaded" with the hint "Please preload the timescaledb library via
      shared_preload_libraries".
      ⚠ THE TWO FAILURES WERE NOT THE SAME SIZE, which is why the preload half mattered more. A wrong
      CREATE EXTENSION fails inside a bootstrap job and leaves a server without one feature; a
      `shared_preload_libraries` entry with no library behind it is read by the POSTMASTER before it
      accepts a connection, so it fails startup for every database on the instance.
      ⚠ WHAT IS STILL OWED IS THE EVIDENCE, NOT THE FIX. `extensions-are-the-allow-list` above still
      asks for one run per allowed value against a real cluster and nothing runs it: the
      cluster-backed suites start a bare k3s with no CloudNativePG operator and no PostgreSQL, so
      nothing in this tree can observe a `CREATE EXTENSION` succeed or a postmaster start. What the
      unit suite proves is that the two spellings render the same two lists —
      ChartRegistryPairTests.TheExtensionCatalogueIsTheChartsExtensionCatalogue diffs the tables row
      for row, and PostgresReconcilerTests.AnExtensionNameIsNotALibraryName pins every allowed value
      in one body, because the defect was per-value and a check naming only the value somebody
      thought of is what let it ship.
      ⚠ AND CyberCloud.Providers.DocumentDB NEVER HAD IT. It renders the same CloudNativePG CRD
      independently and keeps the vocabularies apart by construction —
      `DocumentDbAccounts.SharedPreloadLibraries` is `[pg_cron, pg_documentdb_core, pg_documentdb]`
      and `ExtensionStatement` is `CREATE EXTENSION IF NOT EXISTS documentdb CASCADE;`, three lines
      apart with a remark saying the prefixes differ. Its extension set is a platform constant rather
      than a tenant allow-list, so it has one row to be right about and no list to conflate. THAT is
      the difference: the defect here was a tenant-facing enum doing two jobs.
      ── The finding, as recorded before the fix ────────────────────────────────────────────────
      /properties/extensions is one list used as two vocabularies: it reaches
      `bootstrap.initdb.postInitApplicationSQL` as `CREATE EXTENSION IF NOT EXISTS <value>` and
      `spec.postgresql.shared_preload_libraries` as a library name, and PostgreSQL does not agree
      that those are the same string. `pgvector` installs as the extension `vector`; `pgvector` and
      `postgis` need no preload entry at all, while `timescaledb` and `pg_stat_statements` do. So the
      allow-list as spelled produces a CREATE EXTENSION that fails for pgvector and asks the
      postmaster to preload two libraries that do not exist. ⚠ Noticed while moving the key out of
      `parameters` and deliberately NOT fixed there: the placement was a live 422 on every extension
      body and this is a different defect with a different fix — a table mapping each allowed value
      to its extension name and its preload requirement, which is a schema-shaped change rather than
      a rendering one. Closing it also wants one run against a real cluster per allowed value, which
      is what `extensions-are-the-allow-list` above already asks for and nothing yet runs.
  - id: the-recovery-window-is-hollow-because-cloudnativepg-owns-its-claims
    status: closed
    closed: 2026-09-15
    says: >
      ⚠ CLOSED BY #69 — THE TEARDOWN DETACHES THE CLAIMS AND THE RESTORE ADOPTS THEM — AND THE
      FINDING IS KEPT BECAUSE THE SHAPE OF THE FIX IS NOT THE SHAPE THE ROW BELOW PREDICTED. The
      third answer it lists (withdraw the window) was refused on the row's own argument: this is the
      window a tenant most expects to work. The second (snapshot before teardown) needs a
      VolumeSnapshot class and controller the bundle does not pin and a Cluster spec on the restore
      that differs from the stored body, which breaks the byte-for-byte restore docs/plan/08 § Soft
      delete promises. The first was taken, and it is TWO moves rather than the one the row names.
      PostgresServerReconciler.DeleteAsync now applies `cnpg.io/reconciliationLoop: disabled`, lists
      the claims by the operator's own `cnpg.io/cluster={name}` label (IKubeClusterConnection.ListAsync,
      added for it), clears every owner reference through VolumeCustody.DetachAsync
      (IKubeClusterConnection.SetOwnerAsync — a JSON merge patch, because server-side apply cannot
      remove an entry another manager wrote), and only then deletes the Pooler and the Cluster.
      ⚠ THE SECOND MOVE IS THE ONE `--keep-pvc` NEVER NEEDED, AND WITHOUT IT THE RESTORE IS AN initdb
      BESIDE THE DATA RATHER THAN OVER IT. Read against v1.30.0: `getManagedPVCs` lists a cluster's
      claims by CONTROLLER REFERENCE (`internal/controller/cluster_controller.go`, the
      `.metadata.controller` field index over `IsOwnedByCluster`), so a detached claim is one the
      operator has never heard of. A re-created Cluster sees no claims, `createPrimaryInstance` takes
      serial 1, `CreateInstancePVCs` reads AlreadyExists as success (`create.go`), and the initdb job
      runs `EnsureTargetDirectoriesDoNotExist` (`pkg/management/postgres/initdb.go`), which finds a
      valid control file and RENAMES the data directory aside before initialising a fresh one. So
      ReconcileAsync, when it finds labelled claims and no Cluster, creates the Cluster carrying the
      pause annotation (checked first thing in `ClusterReconciler.reconcile`), reads its uid back,
      writes a controller reference naming that uid onto every claim (VolumeCustody.AdoptAsync), and
      releases the pause with the ordinary apply. The operator then classifies the claims as dangling
      — a `ready` claim with no pod and no job (`status.go`'s classifyPVC) — and
      `ensureInstancesAreCreated` re-creates the pods over them. ⚠ NOT `cnpg.io/pvcStatus: detached`:
      that is what `--keep-pvc` stamps, and `hasUnknownStatus` makes the operator IGNORE a claim
      carrying it, which is the opposite of what a restore needs. The claims keep `ready`.
      ⚠ WHAT THE SUITES PROVE, AND THE ONE THING THEY CANNOT. PostgresReconcilerTests pins the order
      (pause, every detach, then the deletes — as one event sequence, because three lists cannot say
      which came first), the fail-closed halves (a teardown that cannot list or cannot detach deletes
      nothing and retries; a conflict on the pause is ignored, because the pause is courtesy and the
      detach is correctness), the adoption naming the uid the API server answered, and that the
      claims are listed under the label and never counted off the body — serials 1 and 3, because a
      failover leaves exactly that. VolumeCustodyTests makes the guard fire before any write.
      ProviderConformanceTests.TheClaimsATeardownKeepsSurviveItAndTheFinalTeardownRemovesThem no
      longer skips this family: FakeKubeCluster now issues uids and garbage-collects a dependent with
      its owner by uid, the case plants CloudNativePG's claims owned by the applied Cluster, and it
      asserts they survive the soft delete with no owner, belong to the restored Cluster's NEW uid
      after the restore, and are gone after the purge. Sabotage-tested on 2026-09-15: skipping the
      detach turns that one case red at the survival assertion; skipping the adopt turns it red at
      the controller assertion; nothing else moves. ⚠ WHAT IS STILL OWED IS THE OPERATOR'S HALF.
      No suite in this repository can watch CloudNativePG elect a primary over reattached claims whose
      status it has never held — the cluster-backed lane starts a bare k3s with no operator (#2).
      Read against v1.30.0, that path is `reconcileTargetPrimaryFromPods` with an empty
      TargetPrimary: the reattached primary's instance manager shuts it down as a former primary
      (`reconcileOldPrimary`), the operator marks a failover, and the most advanced instance is
      elected — the ordinary failover machinery, and it is READ rather than observed. A run against a
      cluster with the operator installed, deleting and restoring a server with rows in it, is what
      turns this row's "closed" into evidence. ⚠ Such a cluster exists under test since 2026-09-17
      (`a-named-initdb-secret-kept-every-server-from-starting` above): `connect-in-cluster` is
      exercised on it and this row's delete-and-restore is not yet — the story creates and never
      deletes — so the operator's half stays owed, and the fixture to pay it on is now there.
      ── The finding, as recorded before the fix ────────────────────────────────────────────────
      ⚠ THIS TYPE DOES NOT LEAK ITS VOLUMES, AND THE REASON IT DOES NOT IS WORSE THAN THE LEAK.
      Issue #65 lists CyberCloud.DBforPostgreSQL/servers among the three operator-delegated types
      whose claims a purge leaves behind, alongside DBforMySQL/servers and Storage/accounts. Read
      against CloudNativePG v1.30.0 — the version charts/bundle/cloudnative-pg/component.yaml pins —
      that premise is false for this row, and only for this row. CloudNativePG creates its PVCs
      itself rather than through a volumeClaimTemplate, and it stamps an OWNER REFERENCE onto every
      one of them: pkg/reconciler/persistentvolumeclaim/build.go calls `.WithClusterInheritance(cluster)`,
      which is `cluster.SetInheritedDataAndOwnership(objectMeta)` (api/v1/cluster_funcs.go), which is
      `utils.SetAsOwnedBy(obj, cluster.ObjectMeta, cluster.TypeMeta)` with `Controller: true`
      (pkg/utils/ownership.go). There is no reclaim or retain setting anywhere in the operator —
      `.spec.storage.pvcTemplate` is a PersistentVolumeClaimSpec and can reach no metadata — and the
      only owner-reference removal in the whole tree is the human-driven
      `kubectl cnpg destroy --keep-pvc`.
      ⚠ SO A PURGE LEAVES NOTHING, AND A SOFT DELETE LEAVES NOTHING EITHER. That is the finding.
      PostgresServerReconciler.DeleteAsync deletes the Cluster on every teardown, and a soft delete
      is a teardown — ProviderConformanceTests says so in as many words: "a soft delete tears the
      data plane down like any other delete; what the window preserves is the name, the stored
      desired state, the committed quota and the volumes". The first three survive here and the
      fourth does not: Kubernetes garbage-collects `{name}-{serial}`, `{name}-{serial}-wal` and every
      tablespace claim as soon as the Cluster is gone. So PostgresServers.SoftDeleteDays = 7 promises
      a window whose restore comes back to an initdb.
      ⚠ AND NOTHING IN THIS TREE CAN SEE IT. ProviderConformanceTests' retained-volume case reads
      volumeClaimTemplates out of the documents a provider APPLIED and skips a family that renders
      none — this provider applies a Cluster and a Pooler, so it skips, and the skip is correct.
      The cluster-backed suites start a bare k3s with no CloudNativePG operator, so no run in this
      repository has ever had a controller present to garbage-collect anything. The window has been
      green since it was declared and has never once been exercised against the operator that
      empties it.
      ⚠ WHAT CLOSING IT LOOKS LIKE, so that the next person does not start from the leak. Either the
      soft-delete teardown stops deleting the Cluster for this family — which contradicts the
      contract every other type keeps and leaves a parked resource's pods running and unbilled — or
      it strips the claims' owner references before deleting, which is what `--keep-pvc` does and
      what a RetainedVolumesAsync-shaped seam cannot express, because that member NAMES claims for a
      purge to remove and this needs claims DETACHED before a delete. A third answer is to withdraw
      SupportsSoftDelete(7) from this type until one of the two exists, on the same argument
      PostgresProvider already makes in its own remarks: "a recovery window the platform does not
      honour is a promise made" to a tenant who will find out when they use it.
  - id: backup-destination-is-not-filled-in
    status: closed
    closed: 2026-09-24
    says: >
      ⚠ FOUND BY #91 ON THE FIRST CONFORMANCE RUN THAT VALIDATED AGAINST CLOUDNATIVEPG'S REAL
      DEFINITION, AND IT WAS EVERY DEFAULT-BODIED SERVER. The schema defaults backup.enabled to true
      and destinationPath to "" — "Empty means the platform fills it in from the tenant's default
      bucket", the description said — and ClusterJson rendered the pair as
      `spec.backup.barmanObjectStore.destinationPath: ""`, which
      charts/bundle/cloudnative-pg/crds/clusters.postgresql.cnpg.io.yaml refuses with `minLength: 1`.
      Nothing fills it in: no code reads a tenant's default bucket, and there is no such bucket to
      read. FakeKubeCluster echoed the object, so twelve assertions per run were green over a Cluster
      no API server would have stored; against a cluster the bundle installed, every server created
      with the defaults would have failed its first pass with the operator's field named and no hint
      that the tenant's own property was the one to set.
      WHAT CLOSED THE REFUSAL: PostgresServerReconciler asks PostgresServers.BackupDestinationProblem
      before it applies anything and fails the pass — terminal, InvalidRequestBody, target
      /properties/backup/destinationPath — with a sentence naming both moves that fix it: name an
      s3://bucket/prefix, or set backup.enabled false. The description now says so, and
      PostgresServers.Body names a destination so every suite converges a body the definition admits.
      PostgresReconcilerTests.BackupsOnWithNoDestinationAreRefusedBeforeAnythingIsAppliedAndNameTheProperty
      pins the refusal, the empty apply list, and the no-backup body rendering no backup block.
      ✅ THE PROMISE, KEPT BY #30 (2026-09-24). The empty destination is now the platform's object
      store: the reconciler makes the server a bucket of its own GUID, issues a key scoped to it
      through the store's IAM API (IObjectStoreGrants), mints the key into the vault
      (ObjectStoreCredentials.EnsureAsync — issue, mint-once, withdraw a race's losing key), renders
      it into {name}-backup-s3 and names that Secret in barmanObjectStore.s3Credentials with the
      store's data-plane endpointURL. The refusal turned round: a NAMED destination is what
      BackupDestinationProblem refuses now, because no 2026-08-01 property can carry its credentials.
      PostgresReconcilerTests.BackupsOnArchiveToTheServersOwnBucketWithAKeyTheVaultHolds and
      ADestinationOfTheTenantsOwnIsRefusedBeforeAnythingIsAppliedAndNamesTheProperty; against the
      real operator and a real SeaweedFS, RecoveryVaultAgainstCloudNativePg. What remains is listed
      under the rows below that name the store.
  - id: synchronous-is-a-member-of-postgresql
    status: closed
    closed: 2026-09-18
    says: >
      ⚠ FOUND BY THE REVIEW OF #91, ONE FLAG AWAY FROM THE BODY EVERY SUITE CONVERGED. With
      synchronousReplication true, PostgresServers.ClusterJson and templates/cluster.yaml both wrote
      `spec.postgresql_synchronous: {method: any, number: 1}` — a sibling of `postgresql`. CloudNativePG
      declares the block as `Synchronous` on PostgresConfiguration, beside `parameters`
      (api/v1/cluster_types.go), and charts/bundle/cloudnative-pg/crds/clusters.postgresql.cnpg.io.yaml
      declares `method` and `number` at spec.postgresql.synchronous and nothing under spec by the other
      name. Server-side apply builds a typed object before it prunes, so the API server — and, since
      #91, FakeKubeCluster — answered ".spec.postgresql_synchronous: field not declared in schema" and
      wrote nothing: every server a tenant created with synchronous replication failed its first pass.
      ⚠ THE DEFAULT IS `false`, WHICH IS THE WHOLE REASON IT SURVIVED #91. The sixteen .Conformance
      suites converge one default body per family, and the definition was only ever asked about that
      body; PostgresReconcilerTests.AsynchronousReplicationRendersNoSynchronousBlockAtAll asserted the
      ABSENCE of the wrong key, which the wrong renderer satisfied. Both spellings now render the block
      inside `postgresql`, PostgresReconcilerTests.SynchronousReplicationRendersTheBlockWhereTheDefinitionDeclaresIt
      pins the placement, and ChartRegistryPairTests.TheChartPutsTheSynchronousBlockInsidePostgresql
      pins the template. What keeps the next flag from doing the same is
      ProviderConformanceTests.EveryPropertyVariantTheSchemaAdmitsRendersAShapeTheDefinitionAdmits,
      which renders every property of every cluster-backed family at a value other than the one the
      case's body carries and holds the fake to the committed definition on each.
  - id: statement-pooling-is-a-mode-the-pooler-does-not-have
    status: closed
    closed: 2026-09-18
    says: >
      ⚠ FOUND BY THE VARIANT ROW ON ITS FIRST RUN, MINUTES AFTER IT WAS WRITTEN. The 2026-08-01 schema
      publishes PgBouncer's three pooling modes — session, transaction, statement — and
      charts/bundle/cloudnative-pg/crds/poolers.postgresql.cnpg.io.yaml declares spec.pgbouncer.poolMode
      as an enum of two: session and transaction. So `statement` was a value the API accepted, the
      portal offered, and no operator could honour: the Cluster applied, the Pooler was refused with
      "Unsupported value: \"statement\": supported values: \"session\", \"transaction\"", and the
      operation failed after the tenant was told 202 — with the operator's field named rather than the
      tenant's property.
      WHAT CLOSED THE REFUSAL: PostgresServerReconciler asks PostgresServers.PoolingModeProblem before
      it applies anything and fails the pass — terminal, InvalidRequestBody, target
      /properties/pooling/mode — naming the two modes that work and the flag that makes the mode moot.
      The description says so (seven generated surfaces moved with it).
      PostgresReconcilerTests.StatementPoolingIsRefusedBeforeAnythingIsAppliedAndNamesTheProperty pins
      the refusal, the empty apply list, and the no-pooler body applying the Cluster alone.
      ⚠ THE VALUE STAYS IN THE SCHEMA, because an api-version is immutable and
      OpenApiCompatibility.EnumValueRemoved refuses the removal; what changed is what choosing it
      means. A next api-version drops it. PgBouncer's statement pooling is real and CloudNativePG does
      not expose it, so there is nothing to render it to — this is a published value that never had an
      implementation, the same shape as `backup-destination-is-not-filled-in` above.
  - id: the-default-bucket-is-not-filled-in
    status: closed
    closed: 2026-09-24
    says: >
      THE OTHER HALF OF `backup-destination-is-not-filled-in`, FOUND THE SAME DAY BY THE FIRST SUITE
      TO PUT A Cluster IN FRONT OF THE OPERATOR THE BUNDLE INSTALLS, AND THE HALF #91 DID NOT CLOSE.
      Before #91 the values row said "Empty means the platform fills it in from the tenant's default
      bucket", and with backup.enabled true — the default — the Cluster carried
      spec.backup.barmanObjectStore with destinationPath "" and no s3Credentials. CloudNativePG
      1.30.0's CRD answers `spec.backup.barmanObjectStore.destinationPath: Invalid value: "": ...
      should be at least 1 chars long` — measured with `kubectl apply --dry-run=server` against the
      bundle's own install on 2026-09-18 — so a server created from the default body never came up
      on a real cluster: the apply was a 422, the operation failed, and no pod was ever scheduled.
      #91 closed THAT at the reconciler: the empty destination is refused before anything is applied,
      naming the tenant's property, and the values row now says the platform does not fill in a
      default bucket yet. AND A DESTINATION ALONE DOES NOT RESCUE IT, which is what this row is
      about: with `destinationPath: s3://tenant-bucket/x` — the shape PostgresServers.Body now
      converges in every suite — the same dry run answers `spec.backup.barmanObjectStore: Invalid
      value: ...: missing credentials. One and only one of azureCredentials, s3Credentials and
      googleCredentials are required`, and this chart renders no credentials block at all — while
      the operator warns that native barmanObjectStore "is deprecated and will be completely removed
      in CloudNativePG 1.31.0" in favour of the Barman Cloud plugin. So there is NO body with
      backup.enabled true that the real operator admits from this family today. The fake cluster
      and the k3s harness, both validating against the committed definition since #91, still admit
      the destination-only body, because "one and only one" credential is the operator's admission
      webhook and not the definition's schema — which is why the Docker-free suite and this family's
      own k3s lane are green over it. Found by
      CyberCloud.RecoveryServices/vaults (#30); its operator lane creates the companion server with
      backups OFF — the one body the operator admits — and asserts the vault's refusal of it
      (charts/managed/recovery-vault/conformance.yaml § owed, `the-store-is-the-servers`).
      ✅ CLOSED BY #30 (2026-09-24), WITH ONE DECISION TAKEN AGAINST THIS ROW'S FIRST SUGGESTION: the
      default destination is a bucket on the PLATFORM's object store, not a tenant bucket the Storage
      provider mints. A tenant bucket would put the server's backups inside something the tenant can
      delete, and its credentials would have to cross from one provider's vault path to another's —
      a write the cross-resource seam refuses by design. The platform store issues a key per server,
      scoped to that server's bucket, and the vault holds it; `s3Credentials` names the Secret the
      reconciler renders from the vault. Rendered as the in-tree barmanObjectStore, which the pinned
      1.30.0 still serves — the plugin is `the-in-tree-archiver-goes-in-1-31` below.
      docs/plan/12 § PostgreSQL's "to the tenant's bucket" is corrected to say so.
  - id: the-in-tree-archiver-goes-in-1-31
    says: >
      The backup section is CloudNativePG's in-tree `barmanObjectStore`, and the operator's own
      warning says it "will be completely removed in CloudNativePG 1.31.0". The bundle pins 1.30.0, so
      it serves today and the round trip is measured against it (RecoveryVaultAgainstCloudNativePg).
      Before the bundle moves past 1.30 the section has to become the Barman Cloud plugin:
      charts/bundle gains plugin-barman-cloud (and cert-manager, which the plugin's TLS needs), this
      chart renders a barmancloud.cnpg.io ObjectStore per server with the same endpoint, bucket and
      Secret and `spec.plugins: [{name: barman-cloud.cloudnative-pg.io, isWALArchiver: true}]`
      instead of `spec.backup`, a restore's bootstrap names the plugin in `recovery.source`, and the
      vault's ScheduledBackup method (RecoveryVaults.BackupMethod) becomes `plugin` in the same
      change. charts/bundle/bundle.yaml § The ordering rule applies: a new api-version is not needed — the body does not
      change — but the bundle bump and the render must land together, or every server's archive
      stops at the upgrade.
  - id: an-issued-key-can-outlive-a-crash
    says: >
      ObjectStoreCredentials.EnsureAsync issues a key at the store, then mints it into the vault; the
      store generates the pair (SeaweedFS's CreateAccessKey takes no caller-chosen value), so the
      order cannot be reversed. A silo that dies between the two leaves a live key at the store that
      no vault holds — it opens only this server's bucket, and the next pass issues another. The
      race's loser withdraws its own key; a crash's does not. Closing it is ListAccessKeys for the
      principal and a DeleteAccessKey for every key the vault does not name, on each pass that finds
      the vault already holding one — one more call on IObjectStoreGrants.
  - id: the-iam-endpoint-is-guarded-by-the-network
    says: >
      SeaweedFS 3.80's IAM API (`weed iam`) answered a CreateUser signed with a key it had never
      issued, in the spike behind #30 — the S3 API refused the same key, so the store's identities
      were enforced and the IAM process's own check was not. The platform's grants sign every IAM
      request with the administrator's key regardless; what keeps a tenant from minting themselves a
      key to every bucket is that ObjectStorageOptions.IamEndpoint must be reachable from the
      platform's hosts and routable from no tenant pod. Nothing enforces that: the CloudNativePG lane
      runs the store on the Docker bridge where k3s pods could reach :8111. Closing it is a
      NetworkPolicy/egress rule in the tenant namespaces the platform renders, or a store version
      whose IAM API authenticates — re-measured before the pin moves.
  - id: an-iam-change-can-open-the-store-for-a-moment
    says: >
      SUSPECTED, AND RECORDED BECAUSE THE CONSEQUENCE IS A BREACH. In one full Fast-lane run (seventy
      suites and their containers on one host), SeaweedFsGrantsTests saw a freshly issued key WRITE
      INTO ANOTHER SERVER'S BUCKET right after its own first write succeeded; run alone, in isolation
      and in the CloudNativePG lane it has always been refused. The reading that fits: SeaweedFS 3.80's
      S3 gateway reloads its identities from the filer on every IAM change, and a reload that reads the
      document mid-write sees no identities — which SeaweedFS treats as authentication OFF
      (`iam.isAuthEnabled = len(identities) > 0`, the same line charts/managed/seaweedfs records for
      tenant accounts). If so, every key issuance opens the platform's store to anyone for a moment.
      The test now waits for a never-issued key to be refused before it asserts the neighbour's
      refusal, which settles the test and not the hazard. Closing it is measuring it (a probe that
      hammers a bogus key during a burst of issuances), then a store configuration that fails closed:
      identities from a static file the platform rewrites, or a newer SeaweedFS whose embedded IAM
      does — re-measured, since 4.41 in the same spike answered a bogus key as an administrator in the
      filer-held mode.
  - id: the-backups-outlive-the-server-and-nothing-reclaims-them
    says: >
      A server's teardown deletes its Pooler and Cluster and deliberately leaves {name}-backup-s3:
      recovery points outlive the server (the vault's schedule owns them), and a restore from a Backup
      reads the credential Secret its status names, so removing it would make the restore a vault
      exists for — the one after the server is gone — impossible. Nothing reclaims the Secret, the
      bucket, the store identity or the vault path after a purge either: RetainedVolumesAsync names
      claims and has no member for a bucket. Closing it is a reclaim step on the purge that empties
      and removes pg-{id} once no vault still lists a point of it, withdraws the principal and deletes
      the vault path — and until then the store accumulates one bucket per server ever created, which
      nothing meters (charts/managed/recovery-vault/conformance.yaml § owed,
      `backup-storage-is-not-metered`).
