# The bundle roster — charts/bundle/README.md § What a component owes.
#
# Every directory beside this file is a component; every component is listed here; the Bundle gate in
# build/Build.Architecture.cs fails when those two sets differ. The roster exists so that adding a
# component is a visible diff in one file rather than a directory somebody has to notice — the same
# reason Build.Architecture.cs keeps `ArchitectureGates` next to the run that produces the outcomes.
#
# ⚠ THIS FILE CARRIES NO VERSIONS. A pin lives in exactly one place, its component's `component.yaml`,
# because a version written twice is a version that disagrees with itself. What the roster carries is
# the ORDER, which is a property of the set and belongs to the set.

# The phases, in the order `install.sh` applies them. A phase is a barrier: every component in phase N
# is installed and its CRDs are established before phase N+1 begins.
phases:
  - phase: 10
    name: fabric
    why: >
      The CNI. Nothing else schedules until pods have addresses, so this is not merely first, it is
      the only phase whose failure has no diagnostic worth reading — every later component reports
      "pod pending" and none of them reports why.
  - phase: 15
    name: certificates
    why: >
      cert-manager, and it is here because two phase-40 components' webhooks mount a Secret only
      cert-manager creates. ⚠ It was not on Task #109's starting list; it was found by reading
      `cluster-api-components.yaml` and the Kamaji chart's templates rather than their READMEs. A
      cluster missing it accepts every install and admits no Cluster.
  - phase: 20
    name: crds
    why: >
      Definitions with no controller behind them in this bundle. Three managed charts render a
      PodMonitor and nothing here reconciles one; the definition still has to exist or the apply is
      refused. Installed early because a CRD is cheap and a missing CRD fails an apply that already
      returned 202 to a tenant.
  - phase: 25
    name: storage
    why: >
      The default StorageClass, and it is this early because three later phases provision claims
      before any tenant does: KubeVirt's DataVolumes in phase 30, Kamaji's etcd in phase 40 — whose
      failure mode docs/plan/09 already names as "storage class unavailable" — and every data-service
      operator in phase 50. ⚠ It is a StorageClass and a provisioner, NOT a CSI driver, and the
      component says so in its own first paragraph. The class installed here is single-replica and
      node-local with no kernel module in it, which is what lets the bundle install onto a cluster we
      did not build; the replicated path is declared in openebs-localpv/component.yaml § which stage
      is on, and nothing here installs it.
  - phase: 30
    name: virtualisation
    why: >
      KubeVirt and CDI. The Cluster API infrastructure provider in phase 40 reconciles a Machine into
      a VirtualMachine, so it needs both groups present before its first reconcile, not merely before
      its first cluster.
  - phase: 40
    name: cluster-api
    why: >
      Core Cluster API and the two providers charts/managed/kubernetes names. Kamaji is here rather
      than in phase 50 because the control-plane provider's webhook resolves a cluster-scoped
      `DataStore` at admission — see kamaji/component.yaml.
  - phase: 50
    name: data-services
    why: >
      The operators behind the catalogue. They are mutually independent — no data-service operator
      reads another's CRDs — so the order within this phase does not matter and is alphabetical.
  - phase: 60
    name: observability
    why: >
      Last, because it is the only phase whose absence degrades rather than breaks: a monitor
      workspace that cannot be created is a missing feature, and a missing CNI is a dead cluster.

components:
  - name: kube-ovn
    phase: 10
  - name: cert-manager
    phase: 15
  - name: prometheus-operator-crds
    phase: 20
  - name: openebs-localpv
    phase: 25
  - name: kubevirt
    phase: 30
  - name: containerized-data-importer
    phase: 30
  - name: cluster-api
    phase: 40
  - name: kamaji
    phase: 40
  - name: kamaji-control-plane-provider
    phase: 40
  - name: cluster-api-provider-kubevirt
    phase: 40
  - name: clickhouse-operator
    phase: 50
  - name: cloudnative-pg
    phase: 50
  - name: mariadb-operator
    phase: 50
  - name: opensearch-operator
    phase: 50
  - name: rabbitmq-cluster-operator
    phase: 50
  - name: redis-operator
    phase: 50
  - name: seaweedfs-operator
    phase: 50
  - name: strimzi-kafka-operator
    phase: 50
  - name: victoria-metrics-operator
    phase: 60

# ── The ordering rule ─────────────────────────────────────────────────────────────────────────
#
# charts/managed/opensearch/conformance.yaml § owed, `api-group-is-deprecated`: "Closing it is a new
# api-version on the resource type plus a charts/bundle/ bump, in that order, and the two must not be
# done in one commit: a bundle that moved first would strand every existing service."
#
# Two halves, and they are enforced by different things because only one of them is a property of a
# file on disk.
#
#   * THE DIRECTION is enforced by the Bundle gate's coverage check, on every run, over every chart.
#     A component whose pin no longer serves a group/version some chart still renders fails the
#     build, naming both. That is the half that matters: the failure mode being prevented is an
#     object applied, accepted by the API server, and reconciled by nothing.
#   * THE GRANULARITY — "not in one commit" — is enforced over the tip commit only, because that is
#     the unit the sentence is about. The gate reads HEAD; a shallow clone with no parent is reported
#     as not inspected rather than passed.
ordering:
  rule: A commit must not change a component's version pin and a charts/managed/*/templates/ file.
  enforcedBy: build/Build.Architecture.cs, BundleGate
  citation: charts/managed/opensearch/conformance.yaml § owed, api-group-is-deprecated

# ── owed ──────────────────────────────────────────────────────────────────────────────────────
#
# Named here rather than left to be discovered. Every item below was found while pinning this bundle
# and none of it is speculative.
owed:
  - id: most-of-the-roster-has-never-been-installed
    says: >
      ⚠ READ THIS BEFORE ANY OTHER ITEM. ⚠ AND THE ID NO LONGER CARRIES A COUNT, WHICH IS THE FOURTH
      CORRECTION RATHER THAN THE FOURTH COUNT. It read `nothing-here-has-been-installed`, then
      `one-of-eighteen-has-been-installed`, then `most-of-the-roster-has-never-been-installed`; the
      denominator in the second was already wrong when openebs-localpv landed, and "eighteen" survived
      into README.md, deploy/README.md and build/Build.Bundle.cs' prose. Renaming it to
      `three-of-nineteen` on 2026-09-03 would have been the same mistake a fourth time — a count
      written into an ID is a count nothing checks, and every reader of the ID then quotes it.
      THE COUNT, ON 2026-09-03: `components:` above holds NINETEEN rows and charts/bundle/ holds
      nineteen directories. THREE of them are installed onto a real API server by
      test/CyberCloud.Bundle.Cluster.Conformance, which runs THIS DIRECTORY'S OWN `install.sh` in
      every case:
      cert-manager, phase 15 — `cert-manager.io/v1` is served afterwards and a self-signed
      Certificate reaches Ready with a parseable certificate in the Secret it names. Sabotage-verified
      both ways: deleting the `values:` block from cert-manager/component.yaml turns the run red, and
      so does removing the `--set` from the installer.
      openebs-localpv, phase 25 — see `one-volume-has-been-provisioned` below.
      openebs-localpv AND cloudnative-pg TOGETHER, in ONE run, across TWO phases — see
      `an-operator-created-and-bound-the-claim` below, which is the row that closes the sentence this
      directory exists for.
      WHAT THAT PROVES: the install MECHANISM, and now one end-to-end path through it. `install.sh`
      runs unattended against a cluster it is handed; it reads a pin out of a component.yaml rather
      than carrying one; its `--wait` makes "installed" mean "serving" FOR A HELM COMPONENT — see
      `the-manifest-path-waits-for-nothing`, which is the clause that turned out to be false for six
      of the nineteen; a `serves:` line is true of the cluster afterwards; and two components install
      onto one node without fighting.
      WHAT IT DOES NOT PROVE, AND THE DISTANCE IS STILL THE WHOLE OF THIS ROW: that the other SIXTEEN
      pins install. Nothing has exercised any `manifest:` component, so the two-document apply with
      the establishment wait between them is unexercised and `kubectl` has never been invoked BY THIS
      SCRIPT under test — the kubectl in the cloudnative-pg suite is the test's own, applying a
      rendered chart. Nothing runs kube-ovn, KubeVirt, CDI, Cluster API or Kamaji, which is where the
      three virtual machines and most of the risk are.
      ⚠ THE PHASE ORDER IS NOW RUN AND THE PHASE BARRIER IS STILL NOT, AND THE TWO ARE DIFFERENT
      CLAIMS. `BundleInstallSelection` drives a full `--dry-run` and asserts that all nineteen
      components are attempted once each, in the roster's order, under ascending phase headers — the
      first assertion here that covers every row, and it costs under a second because it touches no
      cluster. What a dry run cannot answer is whether "installed" implies "serving" at a phase
      boundary, and for the six `manifest:` rows the answer is no.
      ⚠ THE LANE, DECIDED RATHER THAN DEFERRED, AND MEASURED. The suite is 4 m 30 s – 4 m 47 s green
      across two runs with the cloudnative-pg class in it, against 2 m 20 s – 3 m 15 s without: the class costs about 1 m 50 s
      of serial tail, of which 26 s is the two-component install, 68 s is PostgreSQL reaching Ready,
      and the rest is a second k3s start. It stays in the per-PR lane, and NOT because that is cheap.
      The only nightly lane that exists is `Build.E2E`, whose own preconditions refuse to run without
      `--e2e-base-url` pointing at a real staging deployment and without a `cyc` CLI to drive; a
      Testcontainers k3s suite moved there would be run by nothing, which is a worse outcome than a
      slow one. A lane for container-backed suites that are too slow for per-PR and need no
      deployment does not exist, and creating one is the decision this row now owes.
  - id: an-operator-created-and-bound-the-claim
    says: >
      ✅ THE SENTENCE THIS DIRECTORY EXISTS FOR IS NO LONGER OWED, AND IT IS THE ONLY ROW HERE THAT
      HAS EVER BEEN ABLE TO SAY SO. `one-volume-has-been-provisioned` set the target in its own words
      — "a reconciler applies a resource, an operator creates the claim, the claim binds" — and
      test/CyberCloud.Bundle.Cluster.Conformance § CloudNativePgOnAnEmptyCluster now runs it:
      ONE `install.sh` run installs openebs-localpv (phase 25) and cloudnative-pg (phase 50) onto one
      fresh k3s; `helm template charts/managed/postgres` renders a `postgresql.cnpg.io/v1` Cluster;
      the API server admits it against the definition the bundle just installed; CloudNativePG creates
      a PersistentVolumeClaim NAMING `openebs-hostpath`; the claim binds; and the cluster reports
      Ready.
      ⚠ WHAT MAKES IT DIFFERENT FROM THE STORAGE ROW IS ONE FIELD ON THE API SERVER. The claim carries
      a CONTROLLER ownerReference to the `Cluster`, kind and apiVersion asserted, so the test could
      not have written it — which is precisely the criticism `one-volume-has-been-provisioned` makes
      of itself ("a 64Mi directory by a test's own hand-written claim").
      ⚠ AND THE VOLUME'S NODE-LOCAL PATH IS ASSERTED, FOR THE SAME REASON AS OVER THERE. This cluster
      has two default classes; openebs writes under /var/openebs/local and k3s's own provisioner under
      /var/lib/rancher/k3s/storage, so the path is the one assertion that survives a mix-up in every
      other one. Measured on a ten-CPU host on 2026-09-03: the claim exists 8 s after the apply, is
      Bound at 18 s, and the cluster is Ready at 68 s.
      ⚠ WHAT IT DOES NOT SAY. It is one of the two charts that render this group — charts/managed/
      ferretdb renders a Cluster too and is not exercised — and it is not the chart's DEFAULT body:
      seven values are overridden, each with the reason beside it in the test, and two of them
      (`pooling.enabled`, `monitoring.enabled`) switch off objects that would need a second and third
      bundle component. It says nothing about the other seventeen charts, and nothing at all about a
      RECONCILER: the resource here is applied by a test running helm and kubectl, not by the
      platform's own reconcile path.
  - id: the-manifest-path-waits-for-nothing
    says: >
      ⚠ FOUND ON 2026-09-03 BY READING install.sh WHILE ADDING A SELECTOR TO IT, AND IT REFUTES A
      SENTENCE THIS FILE AND README.md BOTH REPEAT. The claim is that `--wait` makes "installed" mean
      "serving". That is true of the THIRTEEN `helm` and `helm-archive` components and false of the
      SIX `manifest:` ones — kubevirt and containerized-data-importer in phase 30, cluster-api,
      kamaji-control-plane-provider and cluster-api-provider-kubevirt in phase 40, and
      rabbitmq-cluster-operator in phase 50. The `manifest` branch is a bare
      `kubectl apply --server-side` with no wait of any kind. The ONE wait it can reach —
      `kubectl wait --for=condition=Established crd --all` — runs only when the component declares a
      `manifestExtra`, which two of the six do, and even then it runs INSIDE the component, before its
      second document, rather than at a phase boundary.
      ⚠ SO `phases:` ABOVE OVERSTATES ITS OWN GUARANTEE. It says "every component in phase N is
      installed and its CRDs are established before phase N+1 begins". For a manifest component,
      "installed" means the apply returned — the operator Deployment may not have a running pod, and
      its definitions may not be Established — and phase 40's four rows are exactly where that matters,
      because cluster-api-provider-kubevirt and the Kamaji control-plane provider both admit against
      CRDs and webhooks the rows before them installed.
      ⚠ IT IS NOT FIXED HERE, DELIBERATELY. The fix is a `kubectl wait` per manifest component —
      Established on its own CRDs, Available on its Deployments — and every candidate wait is a guess
      until somebody has run those six components against an API server, which nothing has. Writing
      the wait first would produce a barrier that is asserted and untested, which is the same shape of
      claim this row exists to retract.
  - id: a-selector-that-matched-nothing-reported-success
    says: >
      ✅ FIXED ON 2026-09-03, RECORDED BECAUSE THE READING IS WORTH MORE THAN THE FIX.
      `install.sh --phase 99` printed one empty phase header and exited 0 under "Bundle applied",
      and `--verify --phase 99` printed "Every pin resolves" having resolved none. The cause was one
      line — `phases="$only_phase"` took the caller's word rather than filtering the roster — and the
      shape is the mirror image of the defect `verify_component`'s own comment already records: "a
      verifier that fails when there is nothing to verify is the same defect as one that passes when
      there is". This was the second half, and it is the more dangerous one, because its output reads
      like a green run and a typo in a phase number is a plausible thing to type.
      ⚠ THE USAGE TEXT WAS WRONG IN THE SAME AREA AND IS ALSO CORRECTED. It said `--phase` is "for
      repairing one row"; phases 30, 40 and 50 hold two, four and eight rows, so fourteen of the
      nineteen components could not be addressed individually at all. `--component <name>` now exists,
      is repeatable, filters the roster and never reorders it — the roster carries the order, and a
      flag that reordered it would be a second place the order is written.
  - id: redis-operator-is-archived-and-its-chart-is-broken
    says: >
      ⚠ THE FOURTH SIGHTING OF ADR-010 CLAUSE 1'S AMENDMENT, AND THE FIRST WHERE THE OPERATOR EXISTS.
      github.com/spotahome/redis-operator reports `archived: true` and was last pushed 2024-07-01.
      Its newest chart release is `Chart-3.3.0`, 2023-08-17; its newest operator tag is `v1.3.0-rc1`,
      a PRE-RELEASE, and the last non-prerelease is v1.2.4 from 2022-12-28. Chart 3.3.0 pins
      `quay.io/spotahome/redis-operator:v1.3.0` and THAT TAG DOES NOT EXIST — quay.io serves
      v1.3.0-rc1 and nothing named v1.3.0. So the chart's default values produce a Deployment that
      cannot pull, on a project that cannot fix it. redis-operator/component.yaml pins the digest
      instead. What is owed is the DECISION, not the workaround: an archived operator behind
      CyberCloud.Cache/redis is a CVE with no upstream, and the alternatives are a maintained Valkey
      operator (hyperspike/valkey-operator, unaudited) or owning the reconciler. ADR-011 is satisfied
      either way — the licence is Apache-2.0 and the ENGINE image is already Valkey, which
      charts/managed/valkey/values.yaml pins deliberately because spotahome's own default is
      `redis:6.2.6-alpine`.
  - id: strimzi-1x-drops-the-group-the-chart-renders
    says: >
      ⚠ THE COVERAGE CHECK EXISTS BECAUSE OF THIS ROW AND WOULD HAVE FAILED ON IT. Strimzi 1.0.0
      removed `kafka.strimzi.io/v1beta2` from the Kafka and KafkaNodePool definitions; 1.1.0 serves
      `v1` alone, with `conversion: strategy: None`. charts/managed/kafka renders v1beta2, so this
      bundle pins 0.51.0 — the last release serving both, where v1beta2 is `served: true, storage:
      true, deprecated: true`. Closing it is a new api-version on CyberCloud.Messaging/kafkaClusters
      rendering `kafka.strimzi.io/v1`, THEN a bundle bump, in that order and not in one commit.
  - id: the-replicated-stage-is-not-installed
    says: >
      ✅ THIS ROW USED TO BE `no-csi` AND THE FIRST HALF OF IT IS CLOSED. charts/bundle/openebs-localpv
      installs a DEFAULT StorageClass, so a cluster built from this bundle provisions claims and every
      stateful managed service can converge. ⚠ THE OLD ROW ALSO UNDERSTATED ITS OWN SUBJECT BY NINE
      CHARTS AND IS CORRECTED HERE. It said "two managed charts carry a `@widget storageclass` row
      (charts/managed/postgres, charts/managed/seaweedfs)". Counted on 2026-08-20: ELEVEN charts name
      a storage class in a template — clickhouse, cloud-shell, ferretdb, harbor, mariadb, nats,
      opensearch, postgres, rabbitmq, seaweedfs, valkey — and eleven carry the `@widget storageclass`
      row, which is not the same eleven: kafka has the widget and renders no claim of its own, and
      cloud-shell renders a claim with no widget. charts/README.md and README.md carried the same
      two-chart sentence and are corrected with it.
      WHAT IS STILL OWED IS THE SECOND STAGE, AND IT IS A STAGE RATHER THAN A GAP. What is installed
      is single-replica and node-local: one copy, no DRBD, no kernel module, and a requested size that
      nothing enforces. A node loss loses that node's volumes. That is the right default — it is what
      makes this bundle installable on a cluster we did not build, which is docs/plan/09's phase 0 —
      and it is not what a paying customer's data sits on. The parts list for turning the replicated
      stage on is openebs-localpv/component.yaml § which stage is on, and the trigger is
      docs/plan/24 § The replicated-storage switch.
      ⚠ ONE PART OF THAT LIST IS A CONTRADICTION IN THIS REPOSITORY RATHER THAN A TASK. ADR-011's
      table marks LINSTOR (GPL-3.0) and DRBD (GPL-2.0) ✓ on their own terms, and
      build/Build.Bundle.cs § BundleLicenceAllowList is an ALLOW-list holding Apache-2.0,
      BSD-3-Clause, MIT and MPL-2.0 — so the gate fails a component the ADR permits. ADR-011
      § Enforcement's wording, "fails on any SSPL/BUSL/AGPL image outside an allow-list", is what
      makes that easy to read the wrong way round. Widening the list with the reason beside it is the
      sanctioned move and is deliberately not done in advance of the component that needs it.
  - id: one-volume-has-been-provisioned
    says: >
      ✅ THIS ROW USED TO BE `nothing-has-provisioned-a-volume` AND THAT SENTENCE IS NO LONGER TRUE.
      test/CyberCloud.Bundle.Cluster.Conformance § OpenEbsLocalPvOnAnEmptyCluster runs THIS
      DIRECTORY'S OWN `install.sh --phase 25` against a fresh k3s and then binds a claim through the
      class it installs. The four assertions this row used to ask for, all present, all firsthand:
      the claim names `storageClassName: openebs-hostpath` EXPLICITLY; a pod mounts it, writes a file
      and reads the bytes back, because WaitForFirstConsumer means a claim with no consumer proves
      nothing; the bound PersistentVolume's `spec.storageClassName` and provisioned-by annotation are
      read back OFF THE API SERVER rather than off the claim; and the default-class annotation is
      asserted to be on OUR class.
      ⚠ THE TRAP THIS ROW WARNED ABOUT WAS REAL AND WAS MEASURED RATHER THAN REASONED ABOUT. A
      throwaway probe on 2026-09-02, against the same fixture with NOTHING INSTALLED: the fresh k3s
      serves exactly one StorageClass — `local-path`, annotated default, and itself
      `volumeBindingMode: WaitForFirstConsumer` — and a BARE claim plus a pod reaches `Bound` with the
      pod `Succeeded`, on a PersistentVolume of class `local-path` at
      `/var/lib/rancher/k3s/storage/pvc-…`. So the obvious test would have been green over a cluster
      install.sh never touched, and — because k3s's own class already waits for a first consumer —
      even "there is a pod" would not have distinguished it. What does the distinguishing is the
      explicit name, the volume's class read back, and the volume's node-local path: openebs writes
      under `/var/openebs/local` and Rancher under `/var/lib/rancher/k3s/storage`, so the path is the
      one assertion that survives a mix-up in every other one, and it is asserted.
      ⚠ THE DEFAULT-CLASS ASSERTION IS THE ONE WITH A SILENT FAILURE BEHIND IT, AND IT IS
      SABOTAGE-VERIFIED. Deleting the `values:` block from openebs-localpv/component.yaml turns the
      run red twice — once in the daemonless companion, in a tenth of a second, on the manifest; and
      once on the cluster, on the missing annotation. Neither `helm` nor `install.sh` reports
      anything in that state: the install succeeds, the Deployment goes Ready, the class exists, and
      the eleven managed charts that leave `storageClassName` at `""` keep silently using k3s's
      provisioner. That is why the flag is asserted in two places and not one.
      ✅ ONE VOLUME WAS NOT THE SENTENCE THE DIRECTORY EXISTS FOR, AND THE SENTENCE IS NOW RUN. What
      is bound HERE is still a 64Mi directory by a test's own hand-written claim, and that has not
      changed. What has is that `an-operator-created-and-bound-the-claim` above does the other half:
      a charts/managed/postgres Cluster, applied against the definition charts/bundle/cloudnative-pg
      installed, whose claim CloudNativePG creates — controller ownerReference asserted — and binds
      through this component's class. The recommendation this paragraph carried, "a phase-50 operator,
      not a bigger storage test", was taken.
      ⚠ AND IT IS ONE COPY OF A DIRECTORY ON ONE NODE, WITH NO QUOTA. Binding a volume says nothing
      about surviving the loss of the node it is on — `the-replicated-stage-is-not-installed` is
      where that lives, and this row closing does not move it.
  - id: licence-scan-is-a-declaration-not-a-scan
    says: >
      ADR-011 § Enforcement asks for "a build gate [that] runs a licence scan over the chart set and
      the container images in the platform bundle". The Bundle gate checks a DECLARED SPDX id per
      component against an allow-list. That catches a component added under SSPL or BUSL by somebody
      who wrote the licence down honestly, and catches nothing else — it does not read a LICENSE file
      and does not open an image. build/Build.Licence.cs is still `NotImplementedYet`, and the
      distance between what it will do and what this gate does is the distance between a scan and an
      attestation.
  - id: images-are-not-pinned-by-digest
    says: >
      Every component but redis-operator installs at its upstream's own default image, which is a tag
      rather than a digest. A tag is mutable and an air-gapped mirror is a values override this
      directory does not model. Closing it is a digest per image per component and a mirror map,
      which is the same shape charts/managed/harbor's `imageRegistry` escape hatch takes for a single
      service.
