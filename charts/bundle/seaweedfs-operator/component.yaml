# Bundle component — charts/bundle/README.md § What a component owes.
#
# The SeaweedFS operator, behind CyberCloud.Storage/accounts and .../buckets — docs/plan/15 § The
# three kinds' object-storage row.

component: seaweedfs-operator
phase: 50
licence: Apache-2.0
licenceEvidence: https://raw.githubusercontent.com/seaweedfs/seaweedfs-operator/seaweedfs-operator-0.1.38/LICENSE

# Packaged chart published as a GitHub release asset, like clickhouse-operator, so the release tag
# and the chart version are both part of the pin.
install: helm-archive
archive: https://github.com/seaweedfs/seaweedfs-operator/releases/download/seaweedfs-operator-0.1.38/seaweedfs-operator-0.1.38.tgz
release: seaweedfs-operator-0.1.38
chart: seaweedfs-operator
version: 0.1.38
appVersion: 1.0.35

source: https://api.github.com/repos/seaweedfs/seaweedfs-operator/releases/latest
checked: 2026-08-19

# Read off templates/crd-seaweed.yaml in chart 0.1.38: thirteen kinds, all `seaweed.seaweedfs.com`,
# all at v1. charts/managed/seaweedfs renders Seaweed; charts/managed/seaweedfs-bucket renders
# Bucket; charts/managed/seaweedfs-fileshare renders SeaweedCSIDriver, which this operator expands
# into the seaweedfs-csi-driver's controller and node workloads — so the CSI driver is NOT a bundle
# component of its own, and docs/plan/15's "a CSI driver in the cluster bundle" is per account
# instead. See that chart's SOURCE for why (one driver per filer).
serves:
  - seaweed.seaweedfs.com/v1

requiredBy:
  - charts/managed/seaweedfs
  - charts/managed/seaweedfs-bucket
  - charts/managed/seaweedfs-fileshare

# Every container image this component's pinned artefact renders, with the digest its registry served
# on 2026-09-03. Regenerate with `./charts/bundle/images.sh --resolve`; check with `images.sh`.
#
# ⚠ THIS IS A RECORD, NOT A PIN, AND THE DIFFERENCE IS WORTH BEING EXACT ABOUT. The tag is what the
# chart passes to the kubelet; the digest beside it is what that tag resolved to when somebody looked.
# A tag is mutable, so an upstream rebuild moves these bytes without moving any version in this
# directory — and `images.sh` is what turns that from invisible into a red run.
images:
  - chrislusf/seaweedfs-operator:1.0.35@sha256:eb783fe8c6cec9024aea61ef822d60b2f1442f7d6601c6a33853d513bdd1ac27
  - curlimages/curl:8.8.0@sha256:73e4d532ea62d7505c5865b517d3704966ffe916609bedc22af6833dc9969bcd
  - registry.k8s.io/ingress-nginx/kube-webhook-certgen:v20231011-8b53cabe0@sha256:a7943503b45d552785aa3b5e457f169a5661fb94d82b8a3373bcd9ebaf9aac80

notes: >
  ⚠ THE DEFINITIONS ARE IN `templates/`, NOT IN `crds/`, AND THAT CHANGES WHAT AN UNINSTALL DOES.
  A chart's `crds/` directory is never deleted by `helm uninstall`; a templated definition is. So
  removing this release removes the Seaweed and Bucket kinds and, with them, every tenant's object —
  which is the opposite of what charts/bundle/cloudnative-pg does with the same verb.
  ⚠ charts/managed/opensearch/conformance.yaml § owed, `listkeys-has-no-handler`, contrasts the two
  services and names this one as the harder case: "there the credential does not exist at all and the
  resource never converges". That is a provider gap and not this component's — the operator installs
  and reconciles regardless of it.
  ⚠ THE CSI DRIVER'S IMAGES ARE NOT IN `images:` ABOVE, AND THAT IS THE LIST'S DEFINITION RATHER THAN
  A GAP IN IT. `images:` is what the pinned ARTEFACT renders; `chrislusf/seaweedfs-csi-driver:v1.4.20`,
  `chrislusf/seaweedfs-mount:v1.4.20` and the registry.k8s.io sidecars are pulled when the operator
  expands a tenant's SeaweedCSIDriver at run time — defaults compiled into
  api/v1/seaweedcsidriver_types.go, not lines in any template images.sh renders. So they are
  tag-pinned by the operator's code and digest-recorded by nothing; the day images.sh learns to
  render a sample CR they belong here.
