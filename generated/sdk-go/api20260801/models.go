// Code generated by CyberCloud.ResourceManager.Contracts.Generation.GoSdkEmitter. DO NOT EDIT.
// @generated from the published OpenAPI document openapi/2026-08-01.json — docs/plan/21 § Generation's one hop.
// ./build.sh Generate overwrites this file and ./build.sh Architecture fails on a difference.

// Package api20260801 is the CyberCloud API at api-version 2026-08-01, typed.
//
// One client per resource type under its provider, one struct per schema nested exactly as
// the wire is, Begin* for every long-running verb, and a pager for every collection.
package api20260801

import "encoding/json"

// ErrorCode is a stable, documented, greppable identifier. ⚠ Part of the API contract.
type ErrorCode string

const (
	ErrorCodeAuthorizationFailed   ErrorCode = "AuthorizationFailed"
	ErrorCodeConflict              ErrorCode = "Conflict"
	ErrorCodeInternalError         ErrorCode = "InternalError"
	ErrorCodeInvalidApiVersion     ErrorCode = "InvalidApiVersion"
	ErrorCodeInvalidGrainKey       ErrorCode = "InvalidGrainKey"
	ErrorCodeInvalidRequestBody    ErrorCode = "InvalidRequestBody"
	ErrorCodeInvalidResourceId     ErrorCode = "InvalidResourceId"
	ErrorCodeInvalidResourceName   ErrorCode = "InvalidResourceName"
	ErrorCodeInvalidResourceType   ErrorCode = "InvalidResourceType"
	ErrorCodeOperationCanceled     ErrorCode = "OperationCanceled"
	ErrorCodeOperationInProgress   ErrorCode = "OperationInProgress"
	ErrorCodeOperationTimeout      ErrorCode = "OperationTimeout"
	ErrorCodePolicyViolation       ErrorCode = "PolicyViolation"
	ErrorCodePreconditionFailed    ErrorCode = "PreconditionFailed"
	ErrorCodeProvisioningFailed    ErrorCode = "ProvisioningFailed"
	ErrorCodeQuotaExceeded         ErrorCode = "QuotaExceeded"
	ErrorCodeResourceAlreadyExists ErrorCode = "ResourceAlreadyExists"
	ErrorCodeResourceGroupNotFound ErrorCode = "ResourceGroupNotFound"
	ErrorCodeResourceHasChildren   ErrorCode = "ResourceHasChildren"
	ErrorCodeResourceNotFound      ErrorCode = "ResourceNotFound"
	ErrorCodeSchemaInvalid         ErrorCode = "SchemaInvalid"
	ErrorCodeScopeLocked           ErrorCode = "ScopeLocked"
	ErrorCodeSubscriptionNotFound  ErrorCode = "SubscriptionNotFound"
	ErrorCodeTenantNotFound        ErrorCode = "TenantNotFound"
	ErrorCodeTenantSuspended       ErrorCode = "TenantSuspended"
)

// Error is the one error shape — docs/plan/08 § Errors.
type Error struct {
	Code    ErrorCode `json:"code"`
	Message string    `json:"message"`
	// An RFC 6901 JSON Pointer into the request body, so a form can highlight the field.
	Target *string `json:"target,omitempty"`
	// Every other problem found. A form that has to be fixed one field per round trip is a form nobody finishes.
	Details []Error `json:"details,omitempty"`
}

// Error renders the code and the message, which is what a log line wants.
func (e *Error) Error() string {
	return string(e.Code) + ": " + e.Message
}

// OperationState is Azure's status vocabulary. ⚠ Terminal means Succeeded, Failed or Canceled; poll until then.
type OperationState string

const (
	OperationStateCanceled   OperationState = "Canceled"
	OperationStateFailed     OperationState = "Failed"
	OperationStateNotStarted OperationState = "NotStarted"
	OperationStateRunning    OperationState = "Running"
	OperationStateSucceeded  OperationState = "Succeeded"
)

// OperationProgress is One progress entry. The array is this platform's addition to Azure's pattern and is what makes a nine-minute cluster creation tolerable — docs/plan/10.
type OperationProgress struct {
	At string `json:"at"`
	// What happened, naming the actual numbers.
	Message         *string `json:"message,omitempty"`
	PercentComplete *int64  `json:"percentComplete,omitempty"`
	// The phase a reconciler reported — applying, waiting-for-ready.
	Step string `json:"step"`
}

// OperationStatus is An operation's current state. ⚠ The members are docs/plan/10 § Long-running operations, over HTTP's, which is a subset of the OperationStatus wire type — no HTTP projection of that record is declared anywhere in the tree. id, status, startTime, percentComplete and progress are always present; endTime once the status is terminal; error once it is Failed.
type OperationStatus struct {
	// When the operation reached a terminal status. Absent until it does.
	EndTime *string `json:"endTime,omitempty"`
	// Present once the status is Failed, and the reason.
	Error *Error `json:"error,omitempty"`
	// The operation id — the last segment of the URL this was polled from.
	ID              string `json:"id"`
	PercentComplete int64  `json:"percentComplete"`
	// Oldest first. What makes a nine-minute cluster creation tolerable — docs/plan/10.
	Progress []OperationProgress `json:"progress"`
	// When the write was accepted.
	StartTime string         `json:"startTime"`
	Status    OperationState `json:"status"`
}

// ScopeType is the values /type accepts. ⚠ Closed: the write path refuses anything else.
type ScopeType string

const (
	ScopeTypeCyberCloudResourcesSubscriptionsResourceGroups ScopeType = "CyberCloud.Resources/subscriptions/resourceGroups"
	ScopeTypeCyberCloudResourcesSubscriptions               ScopeType = "CyberCloud.Resources/subscriptions"
	ScopeTypeCyberCloudResourcesTenants                     ScopeType = "CyberCloud.Resources/tenants"
)

// ScopeResource is a tenant, a subscription or a resource group, as the API renders it. ⚠ There is no provisioningState: a scope converges before the call returns.
type ScopeResource struct {
	// The scope's own path — docs/plan/06 § Identifiers.
	ID string `json:"id"`
	// The region: a tenant's home region or a group's default. ⚠ Absent rather than empty where the scope has none, so a client tests for the property instead of comparing against "".
	Location *string `json:"location,omitempty"`
	// The name a human reads.
	Name string `json:"name"`
	// The Azure-shaped type string.
	Type ScopeType `json:"type"`
}

// SubscriptionCreateContent is the body of a PUT that creates a subscription.
type SubscriptionCreateContent struct {
	// The name on an invoice and in every scope picker. Required — a subscription identified only by its GUID is one nobody can pick out of a list.
	DisplayName string `json:"displayName"`
}

// ResourceGroupCreateContent is the body of a PUT that creates a resource group.
type ResourceGroupCreateContent struct {
	// The region the group's resources default to. Required, and there is no platform-wide default to fall back on: a group whose region were guessed would place a tenant's data somewhere nobody chose.
	Location string `json:"location"`
}

// ProvisioningState is the values /provisioningState carries. ⚠ Read-only: the server sets it, and a write that carries it is refused.
type ProvisioningState string

const (
	ProvisioningStateCanceled  ProvisioningState = "Canceled"
	ProvisioningStateCreating  ProvisioningState = "Creating"
	ProvisioningStateDeleting  ProvisioningState = "Deleting"
	ProvisioningStateFailed    ProvisioningState = "Failed"
	ProvisioningStateSucceeded ProvisioningState = "Succeeded"
	ProvisioningStateUpdating  ProvisioningState = "Updating"
)

// Resource is The envelope every resource is read in — docs/plan/10 § Request pipeline, step 9. Every type's schema is allOf this: a GET, a list element and a 202 all carry these five members beside the body the type declares, in this order, then tags. ⚠ All five are readOnly and none is required, although a read always carries every one — the same schema validates a PUT, and this platform refuses a read-only member on a write rather than ignoring it (docs/plan/08 § The write path, end to end). x-cybercloud-read-required lists what a read guarantees.
type Resource struct {
	// The concurrency token. Send it back as If-Match on a write to refuse a lost update — docs/plan/08 § The write path, end to end.
	Etag string `json:"etag"`
	// The resource's own path — docs/plan/06 § Identifiers — which is also the URL it was read from.
	ID string `json:"id"`
	// The last segment of the path: the name the caller chose on the PUT.
	Name string `json:"name"`
	// Azure's provisioning vocabulary — docs/plan/06 § Tags, locks. ⚠ Deleting is a state a listing still shows: a resource whose teardown has not converged keeps running and keeps being metered.
	ProvisioningState ProvisioningState `json:"provisioningState"`
	// The fully qualified resource type — the same string this path item's x-cybercloud-resource-type carries.
	Type string `json:"type"`
}

// ClickHouseClusterPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type ClickHouseClusterPreset string

const (
	ClickHouseClusterPresetM12xlarge ClickHouseClusterPreset = "m1.2xlarge"
	ClickHouseClusterPresetM14xlarge ClickHouseClusterPreset = "m1.4xlarge"
	ClickHouseClusterPresetM1Large   ClickHouseClusterPreset = "m1.large"
	ClickHouseClusterPresetM1Medium  ClickHouseClusterPreset = "m1.medium"
	ClickHouseClusterPresetM1Micro   ClickHouseClusterPreset = "m1.micro"
	ClickHouseClusterPresetM1Nano    ClickHouseClusterPreset = "m1.nano"
	ClickHouseClusterPresetM1Small   ClickHouseClusterPreset = "m1.small"
	ClickHouseClusterPresetM1Xlarge  ClickHouseClusterPreset = "m1.xlarge"
)

// ClickHouseClusterVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type ClickHouseClusterVersion string

const (
	ClickHouseClusterVersionN248 ClickHouseClusterVersion = "24.8"
	ClickHouseClusterVersionN253 ClickHouseClusterVersion = "25.3"
)

// ClickHouseClusterData is ClickHouse cluster: the body a caller writes. A managed ClickHouse cluster on the Altinity operator, with declared shards and replicas and a ClickHouse Keeper quorum. The schema is the tenant's.
type ClickHouseClusterData struct {
	// The region the cluster is billed in.
	Location string `json:"location"`
	// The cluster's own settings.
	Properties *ClickHouseClusterProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// ClickHouseClusterProperties is The cluster's own settings.
type ClickHouseClusterProperties struct {
	// The cluster whose namespace holds the ClickHouse cluster.
	ClusterID string `json:"clusterId"`
	// Number of ClickHouse Keeper nodes. Keeper is a Raft quorum, so three is the smallest count that survives losing one and an even count tolerates no more failures than the odd count below it. One is offered for development and has no quorum at all.
	KeeperNodes int64 `json:"keeperNodes"`
	// What the platform scrapes.
	Monitoring *ClickHouseClusterPropertiesMonitoring `json:"monitoring,omitempty"`
	// Number of replicas per shard. This is the availability axis, and it only applies to tables the tenant creates as Replicated — the resource does not manage tables. Total server count is shards times replicas.
	Replicas int64 `json:"replicas"`
	// Number of shards. This is the capacity and parallelism axis: a table's data is split across shards and a query fans out to all of them. ⚠ Resharding an existing table is not something the operator or this resource does, so growing this moves new data only.
	Shards int64 `json:"shards"`
	// CPU and memory per ClickHouse server, either by preset or explicitly. The Keeper nodes are sized by the platform and are not affected.
	Sizing *ClickHouseClusterPropertiesSizing `json:"sizing,omitempty"`
	// The data volume, per ClickHouse server.
	Storage *ClickHouseClusterPropertiesStorage `json:"storage,omitempty"`
	// ClickHouse version, which is also the ClickHouse Keeper version — the two share a release train and a wire protocol. Only long-term-support lines are offered; a new api-version is what adds a third.
	Version ClickHouseClusterVersion `json:"version"`
}

// ClickHouseClusterPropertiesMonitoring is What the platform scrapes.
type ClickHouseClusterPropertiesMonitoring struct {
	// Whether ClickHouse's own Prometheus endpoint is served on port 9363. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". ⚠ It makes the metrics exist; the object that scrapes them is not built — see conformance.yaml § owed.
	Enabled *bool `json:"enabled,omitempty"`
}

// ClickHouseClusterPropertiesSizing is CPU and memory per ClickHouse server, either by preset or explicitly. The Keeper nodes are sized by the platform and are not affected.
type ClickHouseClusterPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 8Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Analytics uses the m1 family, which is 1 vCPU to 8 GiB.
	Preset *ClickHouseClusterPreset `json:"preset,omitempty"`
}

// ClickHouseClusterPropertiesStorage is The data volume, per ClickHouse server.
type ClickHouseClusterPropertiesStorage struct {
	// StorageClass name for the ClickHouse servers and the Keeper nodes. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Data volume size per ClickHouse server, in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
}

// ClickHouseClusterResource is one ClickHouse cluster, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type ClickHouseClusterResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data ClickHouseClusterData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *ClickHouseClusterResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// ClickHouseClusterListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type ClickHouseClusterListKeysResult struct {
	// The name to write in ON CLUSTER and in a Distributed table's first argument. Returned rather than left for the caller to guess, because it is the one platform-chosen string that reaches the tenant's SQL.
	ClusterName string `json:"clusterName"`
	// The in-cluster HTTP endpoint, http://host:port. ⚠ No external address is returned, because there is none — see the cluster's own documentation on exposure.
	HttpEndpoint string `json:"httpEndpoint"`
	// The in-cluster native-protocol endpoint, host:port, for clickhouse-client and every driver that speaks the binary protocol.
	NativeEndpoint string `json:"nativeEndpoint"`
	// The password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The user the credential below belongs to.
	Username string `json:"username"`
}

// ValkeyCacheMaxmemoryPolicy is the values /properties/maxmemoryPolicy accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCacheMaxmemoryPolicy string

const (
	ValkeyCacheMaxmemoryPolicyNoeviction     ValkeyCacheMaxmemoryPolicy = "noeviction"
	ValkeyCacheMaxmemoryPolicyAllkeysLru     ValkeyCacheMaxmemoryPolicy = "allkeys-lru"
	ValkeyCacheMaxmemoryPolicyAllkeysLfu     ValkeyCacheMaxmemoryPolicy = "allkeys-lfu"
	ValkeyCacheMaxmemoryPolicyAllkeysRandom  ValkeyCacheMaxmemoryPolicy = "allkeys-random"
	ValkeyCacheMaxmemoryPolicyVolatileLru    ValkeyCacheMaxmemoryPolicy = "volatile-lru"
	ValkeyCacheMaxmemoryPolicyVolatileLfu    ValkeyCacheMaxmemoryPolicy = "volatile-lfu"
	ValkeyCacheMaxmemoryPolicyVolatileRandom ValkeyCacheMaxmemoryPolicy = "volatile-random"
	ValkeyCacheMaxmemoryPolicyVolatileTtl    ValkeyCacheMaxmemoryPolicy = "volatile-ttl"
)

// ValkeyCacheMode is the values /properties/mode accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCacheMode string

const (
	ValkeyCacheModeSentinel ValkeyCacheMode = "Sentinel"
)

// ValkeyCacheFsync is the values /properties/persistence/fsync accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCacheFsync string

const (
	ValkeyCacheFsyncAlways   ValkeyCacheFsync = "always"
	ValkeyCacheFsyncEverysec ValkeyCacheFsync = "everysec"
	ValkeyCacheFsyncNo       ValkeyCacheFsync = "no"
)

// ValkeyCachePersistenceMode is the values /properties/persistence/mode accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCachePersistenceMode string

const (
	ValkeyCachePersistenceModeNone ValkeyCachePersistenceMode = "None"
	ValkeyCachePersistenceModeRDB  ValkeyCachePersistenceMode = "RDB"
	ValkeyCachePersistenceModeAOF  ValkeyCachePersistenceMode = "AOF"
)

// ValkeyCachePreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCachePreset string

const (
	ValkeyCachePresetM1Nano    ValkeyCachePreset = "m1.nano"
	ValkeyCachePresetM1Micro   ValkeyCachePreset = "m1.micro"
	ValkeyCachePresetM1Small   ValkeyCachePreset = "m1.small"
	ValkeyCachePresetM1Medium  ValkeyCachePreset = "m1.medium"
	ValkeyCachePresetM1Large   ValkeyCachePreset = "m1.large"
	ValkeyCachePresetM1Xlarge  ValkeyCachePreset = "m1.xlarge"
	ValkeyCachePresetM12xlarge ValkeyCachePreset = "m1.2xlarge"
	ValkeyCachePresetM14xlarge ValkeyCachePreset = "m1.4xlarge"
)

// ValkeyCacheVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type ValkeyCacheVersion string

const (
	ValkeyCacheVersionN7 ValkeyCacheVersion = "7"
	ValkeyCacheVersionN8 ValkeyCacheVersion = "8"
)

// ValkeyCacheData is Valkey cache: the body a caller writes. A managed Valkey cache with Sentinel failover, on spotahome/redis-operator. Valkey rather than Redis, per ADR-011; every Redis client works against it.
type ValkeyCacheData struct {
	// The region the cache is billed in.
	Location string `json:"location"`
	// The cache's own settings.
	Properties *ValkeyCacheProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// ValkeyCacheProperties is The cache's own settings.
type ValkeyCacheProperties struct {
	// The cluster whose namespace holds the RedisFailover.
	ClusterID string `json:"clusterId"`
	// What happens when the cache reaches its memory limit. noeviction returns an error to the writer rather than dropping a key somebody is relying on.
	MaxmemoryPolicy *ValkeyCacheMaxmemoryPolicy `json:"maxmemoryPolicy,omitempty"`
	// Replication topology. Sentinel is the only one this operator implements, and a client written against it is not portable to a sharded deployment, so it may not change after create.
	Mode *ValkeyCacheMode `json:"mode,omitempty"`
	// What the platform scrapes.
	Monitoring *ValkeyCachePropertiesMonitoring `json:"monitoring,omitempty"`
	// What survives a restart. None of the three settings makes a cache a database.
	Persistence *ValkeyCachePropertiesPersistence `json:"persistence,omitempty"`
	// Number of Valkey instances, including the primary. One is a single point of failure and is offered for development only.
	Replicas int64 `json:"replicas"`
	// CPU and memory, either by preset or explicitly.
	Sizing *ValkeyCachePropertiesSizing `json:"sizing,omitempty"`
	// Major Valkey version. Minor upgrades are applied automatically in the maintenance window.
	Version ValkeyCacheVersion `json:"version"`
}

// ValkeyCachePropertiesMonitoring is What the platform scrapes.
type ValkeyCachePropertiesMonitoring struct {
	// Whether the operator runs a metrics exporter beside every Valkey and every Sentinel pod.
	Enabled *bool `json:"enabled,omitempty"`
}

// ValkeyCachePropertiesPersistence is What survives a restart. None of the three settings makes a cache a database.
type ValkeyCachePropertiesPersistence struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// How often the append-only file reaches the disk. Read only when the mode is AOF; everysec can lose the last second of writes.
	Fsync *ValkeyCacheFsync `json:"fsync,omitempty"`
	// None keeps nothing, RDB snapshots periodically, AOF appends every write.
	Mode *ValkeyCachePersistenceMode `json:"mode,omitempty"`
	// Persistent volume size in Kubernetes quantity form. Unused when the mode is None, which keeps the data directory in memory.
	Size *string `json:"size,omitempty"`
}

// ValkeyCachePropertiesSizing is CPU and memory, either by preset or explicitly.
type ValkeyCachePropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset. It also sets maxmemory, so a cache evicts or refuses before the kernel kills the pod.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Caches use the m1 family, which is 1 vCPU to 8 GiB.
	Preset *ValkeyCachePreset `json:"preset,omitempty"`
}

// ValkeyCacheResource is one Valkey cache, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type ValkeyCacheResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data ValkeyCacheData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *ValkeyCacheResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// ValkeyCacheListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type ValkeyCacheListKeysResult struct {
	// The in-cluster DNS name of the Sentinel service. ⚠ Not the primary's own address — a client asks Sentinel which member is the primary, because that answer changes at every failover.
	Host string `json:"host"`
	// The Sentinel master group to ask for. Every client library takes this as a separate argument from the host.
	MasterName string `json:"masterName"`
	// The requirepass value, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The Sentinel TCP port.
	Port int64 `json:"port"`
}

// ContainerRegistryPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type ContainerRegistryPreset string

const (
	ContainerRegistryPresetS12xlarge ContainerRegistryPreset = "s1.2xlarge"
	ContainerRegistryPresetS14xlarge ContainerRegistryPreset = "s1.4xlarge"
	ContainerRegistryPresetS1Large   ContainerRegistryPreset = "s1.large"
	ContainerRegistryPresetS1Medium  ContainerRegistryPreset = "s1.medium"
	ContainerRegistryPresetS1Micro   ContainerRegistryPreset = "s1.micro"
	ContainerRegistryPresetS1Nano    ContainerRegistryPreset = "s1.nano"
	ContainerRegistryPresetS1Small   ContainerRegistryPreset = "s1.small"
	ContainerRegistryPresetS1Xlarge  ContainerRegistryPreset = "s1.xlarge"
)

// ContainerRegistryVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type ContainerRegistryVersion string

const (
	ContainerRegistryVersionN214 ContainerRegistryVersion = "2.14"
	ContainerRegistryVersionN215 ContainerRegistryVersion = "2.15"
)

// ContainerRegistryData is Container registry: the body a caller writes. A private OCI container registry on Harbor, with a web portal, an image store and a seven-day recovery window.
type ContainerRegistryData struct {
	// The region the registry is billed in.
	Location string `json:"location"`
	// The registry's own settings.
	Properties *ContainerRegistryProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// ContainerRegistryProperties is The registry's own settings.
type ContainerRegistryProperties struct {
	// The cluster whose namespace holds the registry.
	ClusterID string `json:"clusterId"`
	// What the platform scrapes.
	Monitoring *ContainerRegistryPropertiesMonitoring `json:"monitoring,omitempty"`
	// Whether this registry may be destroyed before its seven-day recovery window is out. Once true it stays true for the rest of the registry's life, and a purge is refused while it is set — a flag whose holder can clear it and then purge is one round-trip of protection.
	PurgeProtection *bool `json:"purgeProtection,omitempty"`
	// How many replicas of each stateless component — the API core, the web portal and the job service — run. Two is the smallest count that survives a node drain. The registry itself, the database and Redis each own a volume and run one replica whatever this says.
	Replicas int64 `json:"replicas"`
	// CPU and memory for the registry pod, either by preset or explicitly. The other five components are sized by the platform and are not affected.
	Sizing *ContainerRegistryPropertiesSizing `json:"sizing,omitempty"`
	// Where the image layers live.
	Storage *ContainerRegistryPropertiesStorage `json:"storage,omitempty"`
	// Harbor minor version. The platform pins the patch, because Harbor publishes image tags per patch and a bare minor resolves to nothing. A minor leaving support is a portal notice and a 120-day window — docs/plan/12 § Cross-cutting decisions.
	Version ContainerRegistryVersion `json:"version"`
}

// ContainerRegistryPropertiesMonitoring is What the platform scrapes.
type ContainerRegistryPropertiesMonitoring struct {
	// Whether Harbor's core exports Prometheus metrics and a PodMonitor selects them. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". Turning it off removes the metrics port as well as the scrape, so nothing is left listening on an unscraped address.
	Enabled *bool `json:"enabled,omitempty"`
}

// ContainerRegistryPropertiesSizing is CPU and memory for the registry pod, either by preset or explicitly. The other five components are sized by the platform and are not affected.
type ContainerRegistryPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. The registry uses the s1 family, which is 1 vCPU to 4 GiB.
	Preset *ContainerRegistryPreset `json:"preset,omitempty"`
}

// ContainerRegistryPropertiesStorage is Where the image layers live.
type ContainerRegistryPropertiesStorage struct {
	// StorageClass name for the image volume. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Image storage, in Kubernetes quantity form. Grows online; never shrinks. ⚠ This is a filesystem volume rather than the tenant's object-storage bucket, which is what docs/plan/13 asks for and what the platform cannot yet give it — see the registry's own documentation.
	Size string `json:"size"`
}

// ContainerRegistryResource is one Container registry, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type ContainerRegistryResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data ContainerRegistryData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *ContainerRegistryResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// ContainerRegistryListCredentialsResult is what listCredentials returns. ⚠ Secret material — never log or persist this.
type ContainerRegistryListCredentialsResult struct {
	// The in-cluster registry endpoint — what docker login takes. ⚠ http rather than https, and no external address is returned, because there is none.
	Endpoint string `json:"endpoint"`
	// The administrator's password, read from the tenant's Vault for this call only. Minted at create; never derived from the resource id.
	Password string `json:"password"`
	// Harbor's web UI, which is a different Service from the API endpoint because this row renders no single front door.
	PortalUrl string `json:"portalUrl"`
	// The administrator's username. Always admin — Harbor seeds that row and nothing renames it.
	Username string `json:"username"`
}

// ManagedKubernetesClusterVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type ManagedKubernetesClusterVersion string

const (
	ManagedKubernetesClusterVersionN132 ManagedKubernetesClusterVersion = "1.32"
	ManagedKubernetesClusterVersionN133 ManagedKubernetesClusterVersion = "1.33"
)

// ManagedKubernetesClusterData is Managed Kubernetes cluster: the body a caller writes. A Kubernetes cluster whose control plane runs as pods in the management cluster and whose workers are isolated virtual machines. Node pools are a child resource.
type ManagedKubernetesClusterData struct {
	// The region the cluster is billed in.
	Location string `json:"location"`
	// The cluster's own settings.
	Properties *ManagedKubernetesClusterProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// ManagedKubernetesClusterProperties is The cluster's own settings.
type ManagedKubernetesClusterProperties struct {
	// The management cluster the control plane runs in. ⚠ This is not the cluster being created: it is the cluster whose API server accepts the Cluster API objects that create one.
	ClusterID string `json:"clusterId"`
	// The control plane, which runs as pods in the management cluster.
	ControlPlane *ManagedKubernetesClusterPropertiesControlPlane `json:"controlPlane,omitempty"`
	// What the platform scrapes.
	Monitoring *ManagedKubernetesClusterPropertiesMonitoring `json:"monitoring,omitempty"`
	// The address space of the cluster being created.
	Network *ManagedKubernetesClusterPropertiesNetwork `json:"network,omitempty"`
	// The Kubernetes minor version of the control plane. The patch level is the platform's. ⚠ Upgrade the control plane before the node pools and by at most one minor at a time; a node pool may run up to three minors behind and may never run ahead. Neither rule is enforced by this API today — an illegal pair is refused by the cluster's own admission, after the create was accepted.
	Version ManagedKubernetesClusterVersion `json:"version"`
}

// ManagedKubernetesClusterPropertiesControlPlane is The control plane, which runs as pods in the management cluster.
type ManagedKubernetesClusterPropertiesControlPlane struct {
	// How many copies of the API server, controller manager and scheduler to run. Two survives a node failure; one is offered for development. ⚠ Unlike an etcd quorum this is a plain replica count — the datastore is separate and is shared — so an even number is not the mistake it would be on a Raft member set.
	Replicas int64 `json:"replicas"`
}

// ManagedKubernetesClusterPropertiesMonitoring is What the platform scrapes.
type ManagedKubernetesClusterPropertiesMonitoring struct {
	// Whether the control plane's own metrics endpoints are scraped. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". ⚠ It covers the control plane, which runs in the management cluster. Nothing scrapes inside the cluster being created; that needs an agent in the bundle.
	Enabled *bool `json:"enabled,omitempty"`
}

// ManagedKubernetesClusterPropertiesNetwork is The address space of the cluster being created.
type ManagedKubernetesClusterPropertiesNetwork struct {
	// The CIDR block pods are addressed from. ⚠ It must not overlap the management cluster's own pod or service range, and nothing checks that — an overlap produces a cluster whose nodes route the platform's addresses to themselves.
	PodCidr string `json:"podCidr"`
	// The CIDR block Service cluster IPs are allocated from. The same overlap warning applies.
	ServiceCidr string `json:"serviceCidr"`
}

// ManagedKubernetesClusterResource is one Managed Kubernetes cluster, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type ManagedKubernetesClusterResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data ManagedKubernetesClusterData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *ManagedKubernetesClusterResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// ManagedKubernetesClusterListCredentialsResult is what listCredentials returns. ⚠ Secret material — never log or persist this.
type ManagedKubernetesClusterListCredentialsResult struct {
	// The URL the kubeconfig points at, https://host:port. ⚠ It is an address inside the management cluster and is not routable from anywhere else — the control plane is deliberately not exposed, because there is no upstream field to attach a CIDR allow-list to. Returned separately so that a caller does not have to parse YAML to discover that.
	ApiServerEndpoint string `json:"apiServerEndpoint"`
	// When the credential stops working, RFC 3339. ⚠ Returned because a credential with no stated expiry is one every caller will paste into CI and never rotate — docs/plan/13 makes "a kubectl credential that expires" part of what a tenant is buying.
	ExpiresAt string `json:"expiresAt"`
	// A complete kubeconfig for the cluster, YAML. ⚠ docs/plan/13 requires this to be short-lived and scoped rather than the cluster's admin credential; what Cluster API generates is the admin one, and narrowing it needs a certificate request against the cluster this platform cannot yet reach.
	Kubeconfig string `json:"kubeconfig"`
}

// NodePoolSize is the values /properties/size accepts. ⚠ Closed: the write path refuses anything else.
type NodePoolSize string

const (
	NodePoolSizeS12xlarge NodePoolSize = "s1.2xlarge"
	NodePoolSizeS14xlarge NodePoolSize = "s1.4xlarge"
	NodePoolSizeS1Large   NodePoolSize = "s1.large"
	NodePoolSizeS1Medium  NodePoolSize = "s1.medium"
	NodePoolSizeS1Micro   NodePoolSize = "s1.micro"
	NodePoolSizeS1Nano    NodePoolSize = "s1.nano"
	NodePoolSizeS1Small   NodePoolSize = "s1.small"
	NodePoolSizeS1Xlarge  NodePoolSize = "s1.xlarge"
)

// NodePoolVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type NodePoolVersion string

const (
	NodePoolVersionN132 NodePoolVersion = "1.32"
	NodePoolVersionN133 NodePoolVersion = "1.33"
)

// NodePoolData is Node pool: the body a caller writes. A group of identically sized worker virtual machines in a managed Kubernetes cluster, optionally under a cluster-autoscaler.
type NodePoolData struct {
	// The region the pool is billed in.
	Location string `json:"location"`
	// The pool's own settings.
	Properties *NodePoolProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// NodePoolProperties is The pool's own settings.
type NodePoolProperties struct {
	// Whether a cluster-autoscaler may resize the pool.
	Autoscale *NodePoolPropertiesAutoscale `json:"autoscale,omitempty"`
	// The management cluster the machine objects are applied to. Must be the one the cluster is in — nothing checks that, and a pool placed elsewhere produces a MachineDeployment naming a Cluster that is not there, which Cluster API accepts and never reconciles.
	ClusterID string `json:"clusterId"`
	// How many worker VMs the pool runs. ⚠ When autoscaling is on this is the starting size and the autoscaler moves it; quota is reserved against the maximum in that case, not against this.
	Count int64 `json:"count"`
	// The root volume of each VM, in Kubernetes quantity form. It holds the operating system, the container images and every writable layer, so a pool running large images needs more of it than a pool running small ones.
	OsDiskSize string `json:"osDiskSize"`
	// The VM size, from the platform's sizing catalogue. Kubernetes nodes use the s1 family, which is 1 vCPU to 4 GiB. ⚠ Immutable: a Cluster API machine template cannot be resized in place, so changing this would mean replacing every VM in the pool, which is a different operation from the one a PUT looks like.
	Size NodePoolSize `json:"size"`
	// How machines are replaced when the pool changes.
	Upgrade *NodePoolPropertiesUpgrade `json:"upgrade,omitempty"`
	// The Kubernetes minor version the nodes run. ⚠ It may be up to three minors behind the cluster's control plane and may never be ahead of it. This API does not check that — the cluster's version is a different resource — so an illegal pair is accepted here and produces nodes that join and then misbehave.
	Version NodePoolVersion `json:"version"`
}

// NodePoolPropertiesAutoscale is Whether a cluster-autoscaler may resize the pool.
type NodePoolPropertiesAutoscale struct {
	// Whether the pool carries autoscaler bounds. Off by default. ⚠ Turning it on changes what the pool costs: quota is then reserved against maxCount, because a pool that may grow to twenty machines has to have twenty machines' worth of headroom to grow into.
	Enabled *bool `json:"enabled,omitempty"`
	// The largest the autoscaler may grow the pool to, and what quota is reserved against while autoscaling is on. ⚠ Nothing checks that it is at least minCount — that is a relation between two properties of one body, which the schema validates nothing about.
	MaxCount *int64 `json:"maxCount,omitempty"`
	// The smallest the autoscaler may shrink the pool to. Ignored when autoscaling is off.
	MinCount *int64 `json:"minCount,omitempty"`
}

// NodePoolPropertiesUpgrade is How machines are replaced when the pool changes.
type NodePoolPropertiesUpgrade struct {
	// How many extra machines may exist during a rolling replacement. One means a new VM boots and joins before an old one is removed, which is why it is the default: it costs one machine's capacity and loses none.
	MaxSurge *int64 `json:"maxSurge,omitempty"`
	// How many machines may be missing during a rolling replacement. Zero with a surge of one is the safe pair; raising it is faster and reduces the pool's capacity while it runs. ⚠ Both being zero would make an upgrade unable to start, and nothing refuses that pair.
	MaxUnavailable *int64 `json:"maxUnavailable,omitempty"`
}

// NodePoolResource is one Node pool, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type NodePoolResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data NodePoolData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *NodePoolResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// MariaDBServerHighAvailability is the values /properties/highAvailability accepts. ⚠ Closed: the write path refuses anything else.
type MariaDBServerHighAvailability string

const (
	MariaDBServerHighAvailabilityNone   MariaDBServerHighAvailability = "None"
	MariaDBServerHighAvailabilityGalera MariaDBServerHighAvailability = "Galera"
)

// MariaDBServerPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type MariaDBServerPreset string

const (
	MariaDBServerPresetS1Nano    MariaDBServerPreset = "s1.nano"
	MariaDBServerPresetS1Micro   MariaDBServerPreset = "s1.micro"
	MariaDBServerPresetS1Small   MariaDBServerPreset = "s1.small"
	MariaDBServerPresetS1Medium  MariaDBServerPreset = "s1.medium"
	MariaDBServerPresetS1Large   MariaDBServerPreset = "s1.large"
	MariaDBServerPresetS1Xlarge  MariaDBServerPreset = "s1.xlarge"
	MariaDBServerPresetS12xlarge MariaDBServerPreset = "s1.2xlarge"
	MariaDBServerPresetS14xlarge MariaDBServerPreset = "s1.4xlarge"
)

// MariaDBServerVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type MariaDBServerVersion string

const (
	MariaDBServerVersionN1011 MariaDBServerVersion = "10.11"
	MariaDBServerVersionN114  MariaDBServerVersion = "11.4"
	MariaDBServerVersionN118  MariaDBServerVersion = "11.8"
)

// MariaDBServerData is MariaDB server: the body a caller writes. A managed MariaDB server on mariadb-operator, speaking the MySQL wire protocol, with Galera for high availability. MariaDB is MySQL-compatible on a documented subset and is not MySQL — see the supported-subset table before migrating.
type MariaDBServerData struct {
	// The region the server is billed in.
	Location string `json:"location"`
	// The server's own settings.
	Properties *MariaDBServerProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// MariaDBServerProperties is The server's own settings.
type MariaDBServerProperties struct {
	// What exists in the database the moment it comes up.
	Bootstrap *MariaDBServerPropertiesBootstrap `json:"bootstrap,omitempty"`
	// The cluster whose namespace holds the MariaDB objects.
	ClusterID string `json:"clusterId"`
	// Replication topology. Galera is three synchronous instances; None is a single instance and a single point of failure, offered for development only.
	HighAvailability *MariaDBServerHighAvailability `json:"highAvailability,omitempty"`
	// What the platform scrapes.
	Monitoring *MariaDBServerPropertiesMonitoring `json:"monitoring,omitempty"`
	// CPU and memory, either by preset or explicitly.
	Sizing *MariaDBServerPropertiesSizing `json:"sizing,omitempty"`
	// The data volume. Every instance gets its own.
	Storage *MariaDBServerPropertiesStorage `json:"storage,omitempty"`
	// Major MariaDB version, by LTS series. Minor upgrades are applied automatically in the maintenance window.
	Version MariaDBServerVersion `json:"version"`
}

// MariaDBServerPropertiesBootstrap is What exists in the database the moment it comes up.
type MariaDBServerPropertiesBootstrap struct {
	// Name of the application database created on first start. Lower case only, because on Linux a database is a directory and its name is therefore case-sensitive.
	Database *string `json:"database,omitempty"`
	// Account granted every privilege on the application database. Capped at MySQL's 32 characters rather than MariaDB's longer limit, because this row is sold as MySQL-compatible.
	Username *string `json:"username,omitempty"`
}

// MariaDBServerPropertiesMonitoring is What the platform scrapes.
type MariaDBServerPropertiesMonitoring struct {
	// Whether the operator runs a mysqld-exporter beside the server.
	Enabled *bool `json:"enabled,omitempty"`
}

// MariaDBServerPropertiesSizing is CPU and memory, either by preset or explicitly.
type MariaDBServerPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.
	Preset *MariaDBServerPreset `json:"preset,omitempty"`
}

// MariaDBServerPropertiesStorage is The data volume. Every instance gets its own.
type MariaDBServerPropertiesStorage struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Data volume size in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
}

// MariaDBServerResource is one MariaDB server, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type MariaDBServerResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data MariaDBServerData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *MariaDBServerResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// MariaDBServerListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type MariaDBServerListKeysResult struct {
	// The authentication plugin a client must be configured for. ⚠ Not MySQL 8's caching_sha2_password default — see the supported-subset table. Returned rather than documented because a wrong default here is a connection that fails with a message about a plugin nobody chose.
	AuthenticationPlugin string `json:"authenticationPlugin"`
	// The application database.
	Database string `json:"database"`
	// The in-cluster DNS name to connect to. ⚠ The primary Service's when high availability is on, so the topology is part of the connection string rather than hidden behind it.
	Host string `json:"host"`
	// The application account's password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The TCP port.
	Port int64 `json:"port"`
	// The application account.
	Username string `json:"username"`
}

// PostgreSQLServerExtensions is the values /properties/extensions accepts. ⚠ Closed: the write path refuses anything else.
type PostgreSQLServerExtensions string

const (
	PostgreSQLServerExtensionsPgvector         PostgreSQLServerExtensions = "pgvector"
	PostgreSQLServerExtensionsPostgis          PostgreSQLServerExtensions = "postgis"
	PostgreSQLServerExtensionsPgStatStatements PostgreSQLServerExtensions = "pg_stat_statements"
	PostgreSQLServerExtensionsTimescaledb      PostgreSQLServerExtensions = "timescaledb"
)

// PostgreSQLServerMode is the values /properties/pooling/mode accepts. ⚠ Closed: the write path refuses anything else.
type PostgreSQLServerMode string

const (
	PostgreSQLServerModeSession     PostgreSQLServerMode = "session"
	PostgreSQLServerModeTransaction PostgreSQLServerMode = "transaction"
	PostgreSQLServerModeStatement   PostgreSQLServerMode = "statement"
)

// PostgreSQLServerPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type PostgreSQLServerPreset string

const (
	PostgreSQLServerPresetS1Nano    PostgreSQLServerPreset = "s1.nano"
	PostgreSQLServerPresetS1Micro   PostgreSQLServerPreset = "s1.micro"
	PostgreSQLServerPresetS1Small   PostgreSQLServerPreset = "s1.small"
	PostgreSQLServerPresetS1Medium  PostgreSQLServerPreset = "s1.medium"
	PostgreSQLServerPresetS1Large   PostgreSQLServerPreset = "s1.large"
	PostgreSQLServerPresetS1Xlarge  PostgreSQLServerPreset = "s1.xlarge"
	PostgreSQLServerPresetS12xlarge PostgreSQLServerPreset = "s1.2xlarge"
	PostgreSQLServerPresetS14xlarge PostgreSQLServerPreset = "s1.4xlarge"
)

// PostgreSQLServerVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type PostgreSQLServerVersion string

const (
	PostgreSQLServerVersionN16 PostgreSQLServerVersion = "16"
	PostgreSQLServerVersionN17 PostgreSQLServerVersion = "17"
	PostgreSQLServerVersionN18 PostgreSQLServerVersion = "18"
)

// PostgreSQLServerData is PostgreSQL server: the body a caller writes. A managed PostgreSQL cluster on CloudNativePG, with replication, PgBouncer and backup to the tenant's object store.
type PostgreSQLServerData struct {
	// The region the server is billed in.
	Location string `json:"location"`
	// The server's own settings.
	Properties *PostgreSQLServerProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// PostgreSQLServerProperties is The server's own settings.
type PostgreSQLServerProperties struct {
	// Backup to the tenant's object store, using CloudNativePG's barman-cloud.
	Backup *PostgreSQLServerPropertiesBackup `json:"backup,omitempty"`
	// What exists in the database the moment it comes up.
	Bootstrap *PostgreSQLServerPropertiesBootstrap `json:"bootstrap,omitempty"`
	// The cluster whose namespace holds the CloudNativePG objects.
	ClusterID string `json:"clusterId"`
	// Extensions to install, from the platform allow-list. An arbitrary-extension escape hatch is a code-execution surface and is not offered.
	Extensions []PostgreSQLServerExtensions `json:"extensions,omitempty"`
	// What the platform scrapes.
	Monitoring *PostgreSQLServerPropertiesMonitoring `json:"monitoring,omitempty"`
	// PgBouncer in front of the cluster.
	Pooling *PostgreSQLServerPropertiesPooling `json:"pooling,omitempty"`
	// Number of instances, including the primary. One is a single point of failure and is offered for development only.
	Replicas int64 `json:"replicas"`
	// CPU and memory, either by preset or explicitly.
	Sizing *PostgreSQLServerPropertiesSizing `json:"sizing,omitempty"`
	// The data volume.
	Storage *PostgreSQLServerPropertiesStorage `json:"storage,omitempty"`
	// Whether commits wait for a replica. Costs write latency and removes the window in which a failover loses transactions.
	SynchronousReplication *bool `json:"synchronousReplication,omitempty"`
	// Major PostgreSQL version. Minor upgrades are applied automatically in the maintenance window.
	Version PostgreSQLServerVersion `json:"version"`
}

// PostgreSQLServerPropertiesBackup is Backup to the tenant's object store, using CloudNativePG's barman-cloud.
type PostgreSQLServerPropertiesBackup struct {
	// Object-store URL for base backups and WAL, for example s3://tenant-bucket/postgres. Empty means the platform fills it in from the tenant's default bucket.
	DestinationPath *string `json:"destinationPath,omitempty"`
	// Whether continuous backup and WAL archiving run.
	Enabled *bool `json:"enabled,omitempty"`
	// How long base backups and WAL are kept. The point-in-time-recovery window is this number of days.
	RetentionDays *int64 `json:"retentionDays,omitempty"`
}

// PostgreSQLServerPropertiesBootstrap is What exists in the database the moment it comes up.
type PostgreSQLServerPropertiesBootstrap struct {
	// Name of the application database created on first start.
	Database *string `json:"database,omitempty"`
	// Role that owns the application database.
	Owner *string `json:"owner,omitempty"`
}

// PostgreSQLServerPropertiesMonitoring is What the platform scrapes.
type PostgreSQLServerPropertiesMonitoring struct {
	// Whether CloudNativePG emits a PodMonitor for the platform's metrics stack.
	Enabled *bool `json:"enabled,omitempty"`
}

// PostgreSQLServerPropertiesPooling is PgBouncer in front of the cluster.
type PostgreSQLServerPropertiesPooling struct {
	// Whether to run a connection pooler. On by default — a managed Postgres without one fails at the first serverless workload, and adding it later changes the connection string.
	Enabled *bool `json:"enabled,omitempty"`
	// Number of pooler pods.
	Instances *int64 `json:"instances,omitempty"`
	// PgBouncer pooling mode. Transaction pooling is the useful one and breaks session-scoped features such as prepared statements and advisory locks.
	Mode *PostgreSQLServerMode `json:"mode,omitempty"`
}

// PostgreSQLServerPropertiesSizing is CPU and memory, either by preset or explicitly.
type PostgreSQLServerPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.
	Preset *PostgreSQLServerPreset `json:"preset,omitempty"`
}

// PostgreSQLServerPropertiesStorage is The data volume.
type PostgreSQLServerPropertiesStorage struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Data volume size in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
	// Size of the separate write-ahead-log volume. Empty means the WAL shares the data volume.
	WalSize *string `json:"walSize,omitempty"`
}

// PostgreSQLServerResource is one PostgreSQL server, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type PostgreSQLServerResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data PostgreSQLServerData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *PostgreSQLServerResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// PostgreSQLServerListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type PostgreSQLServerListKeysResult struct {
	// The application database.
	Database string `json:"database"`
	// The in-cluster DNS name to connect to. ⚠ The pooler's when pooling is on, so turning pooling off later is a visible change to this value rather than a silent one.
	Host string `json:"host"`
	// The owning role's password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The TCP port.
	Port int64 `json:"port"`
	// The owning role.
	Username string `json:"username"`
}

// DocumentDatabaseAccountPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type DocumentDatabaseAccountPreset string

const (
	DocumentDatabaseAccountPresetS12xlarge DocumentDatabaseAccountPreset = "s1.2xlarge"
	DocumentDatabaseAccountPresetS14xlarge DocumentDatabaseAccountPreset = "s1.4xlarge"
	DocumentDatabaseAccountPresetS1Large   DocumentDatabaseAccountPreset = "s1.large"
	DocumentDatabaseAccountPresetS1Medium  DocumentDatabaseAccountPreset = "s1.medium"
	DocumentDatabaseAccountPresetS1Micro   DocumentDatabaseAccountPreset = "s1.micro"
	DocumentDatabaseAccountPresetS1Nano    DocumentDatabaseAccountPreset = "s1.nano"
	DocumentDatabaseAccountPresetS1Small   DocumentDatabaseAccountPreset = "s1.small"
	DocumentDatabaseAccountPresetS1Xlarge  DocumentDatabaseAccountPreset = "s1.xlarge"
)

// DocumentDatabaseAccountVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type DocumentDatabaseAccountVersion string

const (
	DocumentDatabaseAccountVersionN25 DocumentDatabaseAccountVersion = "2.5"
	DocumentDatabaseAccountVersionN27 DocumentDatabaseAccountVersion = "2.7"
)

// DocumentDatabaseAccountData is Document database account: the body a caller writes. MongoDB-compatible, not MongoDB: FerretDB over PostgreSQL, covering CRUD, indexes and the wire protocol. Multi-document transactions are not supported. Runs on a CloudNativePG cluster, so failover, backup and point-in-time recovery are the operator's.
type DocumentDatabaseAccountData struct {
	// The region the account is billed in.
	Location string `json:"location"`
	// The account's own settings.
	Properties *DocumentDatabaseAccountProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// DocumentDatabaseAccountProperties is The account's own settings.
type DocumentDatabaseAccountProperties struct {
	// Backup to an object store, using CloudNativePG's barman-cloud. docs/plan/12: because it is PostgreSQL underneath, backup and point-in-time recovery are the operator's.
	Backup *DocumentDatabaseAccountPropertiesBackup `json:"backup,omitempty"`
	// The cluster whose namespace holds the PostgreSQL cluster and the FerretDB gateway.
	ClusterID string `json:"clusterId"`
	// The FerretDB gateway — what speaks the MongoDB wire protocol.
	Gateway *DocumentDatabaseAccountPropertiesGateway `json:"gateway,omitempty"`
	// What the platform scrapes.
	Monitoring *DocumentDatabaseAccountPropertiesMonitoring `json:"monitoring,omitempty"`
	// The CloudNativePG cluster the documents actually live in.
	Postgres *DocumentDatabaseAccountPropertiesPostgres `json:"postgres,omitempty"`
	// CPU and memory per PostgreSQL instance, either by preset or explicitly. The FerretDB gateway pods are sized by the platform and are not affected.
	Sizing *DocumentDatabaseAccountPropertiesSizing `json:"sizing,omitempty"`
	// The data volume, per PostgreSQL instance.
	Storage *DocumentDatabaseAccountPropertiesStorage `json:"storage,omitempty"`
	// FerretDB minor version. ⚠ One property moves two images: the DocumentDB PostgreSQL extension and the FerretDB gateway are released as a matched pair and a mismatched pair is a proxy talking to an extension it does not know. FerretDB maintains no long-term branch, so the two values here are the two most recent releases and a third is a new api-version.
	Version DocumentDatabaseAccountVersion `json:"version"`
}

// DocumentDatabaseAccountPropertiesBackup is Backup to an object store, using CloudNativePG's barman-cloud. docs/plan/12: because it is PostgreSQL underneath, backup and point-in-time recovery are the operator's.
type DocumentDatabaseAccountPropertiesBackup struct {
	// Object-store URL for base backups and WAL, for example s3://tenant-bucket/documentdb. Empty means no backup configuration is rendered, whatever enabled says.
	DestinationPath *string `json:"destinationPath,omitempty"`
	// Whether continuous backup and WAL archiving run. ⚠ Off by default, and only because there is no destination to default to: turning it on without a destinationPath below renders no backup configuration at all rather than an empty one, so the two properties have to be set together.
	Enabled *bool `json:"enabled,omitempty"`
	// How long base backups and WAL are kept. The point-in-time-recovery window is this number of days.
	RetentionDays *int64 `json:"retentionDays,omitempty"`
}

// DocumentDatabaseAccountPropertiesGateway is The FerretDB gateway — what speaks the MongoDB wire protocol.
type DocumentDatabaseAccountPropertiesGateway struct {
	// Number of FerretDB pods. The gateway is stateless — it translates and forwards, and every byte is in PostgreSQL — so this is a throughput and availability setting rather than a topology one.
	Replicas int64 `json:"replicas"`
}

// DocumentDatabaseAccountPropertiesMonitoring is What the platform scrapes.
type DocumentDatabaseAccountPropertiesMonitoring struct {
	// Whether both halves of this service are scraped: CloudNativePG is asked for a PodMonitor over the PostgreSQL pods, and the platform writes one over the FerretDB pods because FerretDB has no operator to ask. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production".
	Enabled *bool `json:"enabled,omitempty"`
}

// DocumentDatabaseAccountPropertiesPostgres is The CloudNativePG cluster the documents actually live in.
type DocumentDatabaseAccountPropertiesPostgres struct {
	// Number of PostgreSQL instances, including the primary. One is a single point of failure and is offered for development only. Failover, replication and point-in-time recovery are CloudNativePG's, which is why this row costs 1.2 engineer-months rather than a rebuild of them.
	Instances int64 `json:"instances"`
}

// DocumentDatabaseAccountPropertiesSizing is CPU and memory per PostgreSQL instance, either by preset or explicitly. The FerretDB gateway pods are sized by the platform and are not affected.
type DocumentDatabaseAccountPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Databases use the s1 family, which is 1 vCPU to 4 GiB.
	Preset *DocumentDatabaseAccountPreset `json:"preset,omitempty"`
}

// DocumentDatabaseAccountPropertiesStorage is The data volume, per PostgreSQL instance.
type DocumentDatabaseAccountPropertiesStorage struct {
	// StorageClass name for the PostgreSQL volumes. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Data volume size per PostgreSQL instance, in Kubernetes quantity form. Every instance carries a full copy, so raw consumption is this times the instance count. Grows online; never shrinks.
	Size string `json:"size"`
}

// DocumentDatabaseAccountResource is one Document database account, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type DocumentDatabaseAccountResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data DocumentDatabaseAccountData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *DocumentDatabaseAccountResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// DocumentDatabaseAccountListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type DocumentDatabaseAccountListKeysResult struct {
	// The database the DocumentDB extension is installed in, which is the one a driver authenticates against.
	Database string `json:"database"`
	// The in-cluster MongoDB endpoint, mongodb://host:port/. ⚠ No external address is returned, because there is none — see the account's own documentation on exposure.
	Endpoint string `json:"endpoint"`
	// That role's password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The PostgreSQL role a client authenticates as. Not secret on its own; useless without the password below.
	Username string `json:"username"`
}

// MailDomainPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type MailDomainPreset string

const (
	MailDomainPresetC1Large  MailDomainPreset = "c1.large"
	MailDomainPresetC1Medium MailDomainPreset = "c1.medium"
	MailDomainPresetC1Micro  MailDomainPreset = "c1.micro"
	MailDomainPresetC1Nano   MailDomainPreset = "c1.nano"
	MailDomainPresetC1Small  MailDomainPreset = "c1.small"
	MailDomainPresetC1Xlarge MailDomainPreset = "c1.xlarge"
)

// MailDomainVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type MailDomainVersion string

const (
	MailDomainVersionN23 MailDomainVersion = "2.3"
	MailDomainVersionN24 MailDomainVersion = "2.4"
)

// MailDomainData is Mail domain: the body a caller writes. A managed mail domain on Dovecot, Postfix and Rspamd, with a per-tenant mail store, DKIM signing, and the SPF, DKIM, DMARC and MX records the domain must publish before the platform will send for it.
type MailDomainData struct {
	// The region the mail domain is billed in.
	Location string `json:"location"`
	// The domain's own settings.
	Properties *MailDomainProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// MailDomainProperties is The domain's own settings.
type MailDomainProperties struct {
	// The local part that receives mail addressed to no known mailbox. Empty means unrouted mail is rejected at RCPT TO, which is the default and the better answer for deliverability: a catch-all accepts every dictionary attack and turns the domain into a backscatter source.
	CatchAll *string `json:"catchAll,omitempty"`
	// The cluster whose namespace holds the mail back end.
	ClusterID string `json:"clusterId"`
	// Request a dedicated outbound IP with a warm-up schedule, rather than sharing the platform's warmed pool. Subject to a volume threshold and to approval; requesting it here does not by itself allocate one.
	DedicatedIp *bool `json:"dedicatedIp,omitempty"`
	// The mail domain this resource hosts, for example example.com. Immutable: the DKIM record, the SPF record and the MX all name it, so changing it would invalidate every record the tenant has published.
	Domain string `json:"domain"`
	// Spam and virus filtering.
	Filtering *MailDomainPropertiesFiltering `json:"filtering,omitempty"`
	// Smart hosts to relay outbound mail through instead of delivering it directly. Empty means the platform's own outbound pool.
	RelayHosts []string `json:"relayHosts,omitempty"`
	// Server-side rules through Dovecot's Pigeonhole, editable over ManageSieve.
	Sieve *bool `json:"sieve,omitempty"`
	// CPU and memory for the back end, either by preset or explicitly.
	Sizing *MailDomainPropertiesSizing `json:"sizing,omitempty"`
	// The mail store.
	Storage *MailDomainPropertiesStorage `json:"storage,omitempty"`
	// Dovecot version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.
	Version MailDomainVersion `json:"version"`
}

// MailDomainPropertiesFiltering is Spam and virus filtering.
type MailDomainPropertiesFiltering struct {
	// Scan attachments with ClamAV through Rspamd. Adds roughly 1 GiB of resident memory for the signature database.
	Antivirus *bool `json:"antivirus,omitempty"`
	// The Rspamd score at or above which a message is rejected outright rather than filed as junk.
	RejectThreshold *int64 `json:"rejectThreshold,omitempty"`
}

// MailDomainPropertiesSizing is CPU and memory for the back end, either by preset or explicitly.
type MailDomainPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Mail back ends use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.
	Preset *MailDomainPreset `json:"preset,omitempty"`
}

// MailDomainPropertiesStorage is The mail store.
type MailDomainPropertiesStorage struct {
	// The default per-mailbox quota, in Kubernetes quantity form. A mailbox may override it. Enforced by Dovecot, not by the volume.
	MailboxQuota *string `json:"mailboxQuota,omitempty"`
	// The mail volume size, in Kubernetes quantity form. Holds every mailbox in the domain. Grows online; never shrinks.
	Size string `json:"size"`
}

// MailDomainResource is one Mail domain, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type MailDomainResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data MailDomainData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *MailDomainResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// KafkaClusterPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type KafkaClusterPreset string

const (
	KafkaClusterPresetC1Nano    KafkaClusterPreset = "c1.nano"
	KafkaClusterPresetC1Micro   KafkaClusterPreset = "c1.micro"
	KafkaClusterPresetC1Small   KafkaClusterPreset = "c1.small"
	KafkaClusterPresetC1Medium  KafkaClusterPreset = "c1.medium"
	KafkaClusterPresetC1Large   KafkaClusterPreset = "c1.large"
	KafkaClusterPresetC1Xlarge  KafkaClusterPreset = "c1.xlarge"
	KafkaClusterPresetC12xlarge KafkaClusterPreset = "c1.2xlarge"
	KafkaClusterPresetC14xlarge KafkaClusterPreset = "c1.4xlarge"
)

// KafkaClusterVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type KafkaClusterVersion string

const (
	KafkaClusterVersionN38 KafkaClusterVersion = "3.8"
	KafkaClusterVersionN39 KafkaClusterVersion = "3.9"
)

// KafkaClusterData is Kafka cluster: the body a caller writes. A managed Apache Kafka cluster on Strimzi in KRaft mode, with Cruise Control, configurable retention and an optional firewalled external listener.
type KafkaClusterData struct {
	// The region the cluster is billed in.
	Location string `json:"location"`
	// The cluster's own settings.
	Properties *KafkaClusterProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// KafkaClusterProperties is The cluster's own settings.
type KafkaClusterProperties struct {
	// The cluster whose namespace holds the Strimzi objects.
	ClusterID string `json:"clusterId"`
	// Cruise Control — partition rebalancing and broker-load reporting.
	CruiseControl *KafkaClusterPropertiesCruiseControl `json:"cruiseControl,omitempty"`
	// Exposure outside the cluster, via a load balancer and a firewall allow-list.
	External *KafkaClusterPropertiesExternal `json:"external,omitempty"`
	// The in-cluster listener every client reaches.
	Listener *KafkaClusterPropertiesListener `json:"listener,omitempty"`
	// What the platform scrapes.
	Monitoring *KafkaClusterPropertiesMonitoring `json:"monitoring,omitempty"`
	// Number of Kafka nodes, each acting as both a KRaft controller and a broker. Use an odd number: a quorum of two tolerates no failures, and one is a single point of failure offered for development only.
	Nodes int64 `json:"nodes"`
	// How long the cluster keeps messages by default. docs/plan/12 calls retention sizing an ongoing operational concern rather than a one-time setting.
	Retention *KafkaClusterPropertiesRetention `json:"retention,omitempty"`
	// CPU and memory per node, either by preset or explicitly.
	Sizing *KafkaClusterPropertiesSizing `json:"sizing,omitempty"`
	// The log volume, per node.
	Storage *KafkaClusterPropertiesStorage `json:"storage,omitempty"`
	// Defaults applied to topics the cluster creates automatically. A topic created explicitly overrides them.
	Topics *KafkaClusterPropertiesTopics `json:"topics,omitempty"`
	// Apache Kafka version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.
	Version KafkaClusterVersion `json:"version"`
}

// KafkaClusterPropertiesCruiseControl is Cruise Control — partition rebalancing and broker-load reporting.
type KafkaClusterPropertiesCruiseControl struct {
	// Whether Cruise Control runs. On by default: docs/plan/12 puts it in the chart rather than in a follow-up, because rebalancing a Kafka cluster by hand is the operational cost that makes this the most demanding service in the catalogue.
	Enabled *bool `json:"enabled,omitempty"`
}

// KafkaClusterPropertiesExternal is Exposure outside the cluster, via a load balancer and a firewall allow-list.
type KafkaClusterPropertiesExternal struct {
	// Source ranges permitted to reach the external listener. Required in substance rather than in schema: an empty list with external exposure on renders a load balancer that accepts nothing, which is the safe reading of an unfinished configuration.
	AllowedCidrs []string `json:"allowedCidrs,omitempty"`
	// Whether the cluster is reachable from outside the Kubernetes cluster. Off by default — docs/plan/12 § Cross-cutting decisions makes external exposure never the default, because a managed broker on a public IP with a weak password is the most common cloud breach there is.
	Enabled *bool `json:"enabled,omitempty"`
}

// KafkaClusterPropertiesListener is The in-cluster listener every client reaches.
type KafkaClusterPropertiesListener struct {
	// Whether the in-cluster listener requires TLS. On by default; turning it off is a plaintext broker on the pod network.
	Tls *bool `json:"tls,omitempty"`
}

// KafkaClusterPropertiesMonitoring is What the platform scrapes.
type KafkaClusterPropertiesMonitoring struct {
	// Whether the operator runs a Kafka Exporter alongside the cluster, exposing consumer-lag and topic metrics to the platform's metrics stack.
	Enabled *bool `json:"enabled,omitempty"`
}

// KafkaClusterPropertiesRetention is How long the cluster keeps messages by default. docs/plan/12 calls retention sizing an ongoing operational concern rather than a one-time setting.
type KafkaClusterPropertiesRetention struct {
	// Default retention in hours. A week by default.
	Hours *int64 `json:"hours,omitempty"`
	// Default retention by partition size, in Kubernetes quantity form. Empty means retention is by time alone.
	Size *string `json:"size,omitempty"`
}

// KafkaClusterPropertiesSizing is CPU and memory per node, either by preset or explicitly.
type KafkaClusterPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.
	Preset *KafkaClusterPreset `json:"preset,omitempty"`
}

// KafkaClusterPropertiesStorage is The log volume, per node.
type KafkaClusterPropertiesStorage struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Whether deleting the resource also deletes the log volumes. Off by default, so a mistaken delete leaves the data recoverable by hand.
	DeleteClaim *bool `json:"deleteClaim,omitempty"`
	// Log volume size per node, in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
}

// KafkaClusterPropertiesTopics is Defaults applied to topics the cluster creates automatically. A topic created explicitly overrides them.
type KafkaClusterPropertiesTopics struct {
	// How many replicas must acknowledge a write before it is committed. Must be below the replication factor, or the cluster cannot tolerate one broker restart.
	MinInSyncReplicas *int64 `json:"minInSyncReplicas,omitempty"`
	// Default partition count for a new topic.
	Partitions *int64 `json:"partitions,omitempty"`
	// Default replication factor for a new topic. Must not exceed the node count, or every produce to a new topic fails.
	ReplicationFactor *int64 `json:"replicationFactor,omitempty"`
}

// KafkaClusterResource is one Kafka cluster, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type KafkaClusterResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data KafkaClusterData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *KafkaClusterResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// KafkaClusterListKeysResultSecurityProtocol is the values /securityProtocol accepts. ⚠ Closed: the write path refuses anything else.
type KafkaClusterListKeysResultSecurityProtocol string

const (
	KafkaClusterListKeysResultSecurityProtocolSASLSSL       KafkaClusterListKeysResultSecurityProtocol = "SASL_SSL"
	KafkaClusterListKeysResultSecurityProtocolSASLPLAINTEXT KafkaClusterListKeysResultSecurityProtocol = "SASL_PLAINTEXT"
)

// KafkaClusterListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type KafkaClusterListKeysResult struct {
	// The in-cluster bootstrap address, host:port. ⚠ The external listener's address is not returned here — an address a client cannot reach from where it is running is worse than an absent one.
	BootstrapServers string `json:"bootstrapServers"`
	// The SCRAM user's password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// What a client must configure: SASL_SSL when the listener requires TLS, SASL_PLAINTEXT when it does not.
	SecurityProtocol KafkaClusterListKeysResultSecurityProtocol `json:"securityProtocol"`
	// The SCRAM user.
	Username string `json:"username"`
}

// NATSClusterPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type NATSClusterPreset string

const (
	NATSClusterPresetC12xlarge NATSClusterPreset = "c1.2xlarge"
	NATSClusterPresetC14xlarge NATSClusterPreset = "c1.4xlarge"
	NATSClusterPresetC1Large   NATSClusterPreset = "c1.large"
	NATSClusterPresetC1Medium  NATSClusterPreset = "c1.medium"
	NATSClusterPresetC1Micro   NATSClusterPreset = "c1.micro"
	NATSClusterPresetC1Nano    NATSClusterPreset = "c1.nano"
	NATSClusterPresetC1Small   NATSClusterPreset = "c1.small"
	NATSClusterPresetC1Xlarge  NATSClusterPreset = "c1.xlarge"
)

// NATSClusterVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type NATSClusterVersion string

const (
	NATSClusterVersionN210 NATSClusterVersion = "2.10"
	NATSClusterVersionN211 NATSClusterVersion = "2.11"
)

// NATSClusterData is NATS cluster: the body a caller writes. A managed NATS cluster with JetStream on file storage, optional leaf-node connectivity and an optional firewalled external listener.
type NATSClusterData struct {
	// The region the cluster is billed in.
	Location string `json:"location"`
	// The cluster's own settings.
	Properties *NATSClusterProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// NATSClusterProperties is The cluster's own settings.
type NATSClusterProperties struct {
	// The cluster whose namespace holds the NATS servers.
	ClusterID string `json:"clusterId"`
	// Exposure outside the cluster, via a load balancer and a firewall allow-list.
	External *NATSClusterPropertiesExternal `json:"external,omitempty"`
	// JetStream — the persistence layer streams and consumers live in. docs/plan/12 has it on unconditionally, so there is no switch to turn it off.
	Jetstream *NATSClusterPropertiesJetstream `json:"jetstream,omitempty"`
	// Leaf-node connectivity — docs/plan/12: "so a tenant's edge can attach".
	LeafNodes *NATSClusterPropertiesLeafNodes `json:"leafNodes,omitempty"`
	// Server limits a client can run into.
	Limits *NATSClusterPropertiesLimits `json:"limits,omitempty"`
	// What the platform scrapes.
	Monitoring *NATSClusterPropertiesMonitoring `json:"monitoring,omitempty"`
	// Number of NATS servers. docs/plan/12 says three or five: JetStream replicates through a Raft group, so an even count buys no extra fault tolerance over the odd count below it. One is offered for development only and has no quorum at all.
	Servers int64 `json:"servers"`
	// CPU and memory per server, either by preset or explicitly.
	Sizing *NATSClusterPropertiesSizing `json:"sizing,omitempty"`
	// The JetStream file store, per server.
	Storage *NATSClusterPropertiesStorage `json:"storage,omitempty"`
	// NATS server version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field.
	Version NATSClusterVersion `json:"version"`
}

// NATSClusterPropertiesExternal is Exposure outside the cluster, via a load balancer and a firewall allow-list.
type NATSClusterPropertiesExternal struct {
	// Source ranges permitted to reach the external listener. Required in substance rather than in schema: an empty list with external exposure on renders a load balancer that accepts nothing, which is the safe reading of an unfinished configuration.
	AllowedCidrs []string `json:"allowedCidrs,omitempty"`
	// Whether the cluster is reachable from outside the Kubernetes cluster. Off by default — docs/plan/12 § Cross-cutting decisions makes external exposure never the default, because a managed broker on a public IP with a weak password is the most common cloud breach there is.
	Enabled *bool `json:"enabled,omitempty"`
}

// NATSClusterPropertiesJetstream is JetStream — the persistence layer streams and consumers live in. docs/plan/12 has it on unconditionally, so there is no switch to turn it off.
type NATSClusterPropertiesJetstream struct {
	// Ceiling for memory-backed streams, in Kubernetes quantity form. Empty means no memory store at all, so every stream is file-backed — which is what docs/plan/12 asks for and what the volume above is sized for. A value here must leave room for the server itself inside the container's memory limit.
	MaxMemoryStore *string `json:"maxMemoryStore,omitempty"`
}

// NATSClusterPropertiesLeafNodes is Leaf-node connectivity — docs/plan/12: "so a tenant's edge can attach".
type NATSClusterPropertiesLeafNodes struct {
	// Whether the servers accept leaf-node connections. Off by default: a leaf node joins the cluster's subject space, so it is a topology change rather than a client.
	Enabled *bool `json:"enabled,omitempty"`
}

// NATSClusterPropertiesLimits is Server limits a client can run into.
type NATSClusterPropertiesLimits struct {
	// Largest number of client connections one server accepts.
	MaxConnections *int64 `json:"maxConnections,omitempty"`
	// Largest message a client may publish, in bytes. Raising it costs memory on every server, because a server buffers a whole message before routing it.
	MaxPayload *int64 `json:"maxPayload,omitempty"`
}

// NATSClusterPropertiesMonitoring is What the platform scrapes.
type NATSClusterPropertiesMonitoring struct {
	// Whether a PodMonitor selects these servers' monitoring endpoint. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". The endpoint itself is always served; this decides whether anything scrapes it.
	Enabled *bool `json:"enabled,omitempty"`
}

// NATSClusterPropertiesSizing is CPU and memory per server, either by preset or explicitly.
type NATSClusterPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.
	Preset *NATSClusterPreset `json:"preset,omitempty"`
}

// NATSClusterPropertiesStorage is The JetStream file store, per server.
type NATSClusterPropertiesStorage struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// JetStream file-store volume size per server, in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
}

// NATSClusterResource is one NATS cluster, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type NATSClusterResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data NATSClusterData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *NATSClusterResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// NATSClusterListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type NATSClusterListKeysResult struct {
	// The in-cluster HTTP monitoring endpoint, http://host:port — /varz and /jsz. Not secret, and returned here because a caller holding the client URL is the caller who needs it.
	MonitoringUrl string `json:"monitoringUrl"`
	// The client user's password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The in-cluster client URL, nats://host:port. ⚠ The external listener's address is not returned here — an address a client cannot reach from where it is running is worse than an absent one.
	Url string `json:"url"`
	// The client user.
	User string `json:"user"`
}

// RabbitMQClusterAdditional is the values /properties/plugins/additional accepts. ⚠ Closed: the write path refuses anything else.
type RabbitMQClusterAdditional string

const (
	RabbitMQClusterAdditionalRabbitmqFederation RabbitMQClusterAdditional = "rabbitmq_federation"
	RabbitMQClusterAdditionalRabbitmqMqtt       RabbitMQClusterAdditional = "rabbitmq_mqtt"
	RabbitMQClusterAdditionalRabbitmqShovel     RabbitMQClusterAdditional = "rabbitmq_shovel"
	RabbitMQClusterAdditionalRabbitmqStomp      RabbitMQClusterAdditional = "rabbitmq_stomp"
	RabbitMQClusterAdditionalRabbitmqStream     RabbitMQClusterAdditional = "rabbitmq_stream"
	RabbitMQClusterAdditionalRabbitmqWebMqtt    RabbitMQClusterAdditional = "rabbitmq_web_mqtt"
	RabbitMQClusterAdditionalRabbitmqWebStomp   RabbitMQClusterAdditional = "rabbitmq_web_stomp"
)

// RabbitMQClusterDefaultType is the values /properties/queues/defaultType accepts. ⚠ Closed: the write path refuses anything else.
type RabbitMQClusterDefaultType string

const (
	RabbitMQClusterDefaultTypeClassic RabbitMQClusterDefaultType = "classic"
	RabbitMQClusterDefaultTypeQuorum  RabbitMQClusterDefaultType = "quorum"
	RabbitMQClusterDefaultTypeStream  RabbitMQClusterDefaultType = "stream"
)

// RabbitMQClusterPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type RabbitMQClusterPreset string

const (
	RabbitMQClusterPresetC12xlarge RabbitMQClusterPreset = "c1.2xlarge"
	RabbitMQClusterPresetC14xlarge RabbitMQClusterPreset = "c1.4xlarge"
	RabbitMQClusterPresetC1Large   RabbitMQClusterPreset = "c1.large"
	RabbitMQClusterPresetC1Medium  RabbitMQClusterPreset = "c1.medium"
	RabbitMQClusterPresetC1Micro   RabbitMQClusterPreset = "c1.micro"
	RabbitMQClusterPresetC1Nano    RabbitMQClusterPreset = "c1.nano"
	RabbitMQClusterPresetC1Small   RabbitMQClusterPreset = "c1.small"
	RabbitMQClusterPresetC1Xlarge  RabbitMQClusterPreset = "c1.xlarge"
)

// RabbitMQClusterVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type RabbitMQClusterVersion string

const (
	RabbitMQClusterVersionN40 RabbitMQClusterVersion = "4.0"
	RabbitMQClusterVersionN41 RabbitMQClusterVersion = "4.1"
)

// RabbitMQClusterData is RabbitMQ cluster: the body a caller writes. A managed RabbitMQ cluster on the RabbitMQ Cluster Operator, with quorum queues as the default queue type and the management UI reachable only in-cluster.
type RabbitMQClusterData struct {
	// The region the cluster is billed in.
	Location string `json:"location"`
	// The cluster's own settings.
	Properties *RabbitMQClusterProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// RabbitMQClusterProperties is The cluster's own settings.
type RabbitMQClusterProperties struct {
	// The cluster whose namespace holds the RabbitmqCluster.
	ClusterID string `json:"clusterId"`
	// Broker limits a client can run into.
	Limits *RabbitMQClusterPropertiesLimits `json:"limits,omitempty"`
	// Number of RabbitMQ nodes. Use an odd number of at least three: a quorum queue is a Raft group, so a group of two tolerates no failures and an even count buys nothing over the odd count below it. One is offered for development only and replicates nothing.
	Nodes int64 `json:"nodes"`
	// Protocols and features beyond AMQP.
	Plugins *RabbitMQClusterPropertiesPlugins `json:"plugins,omitempty"`
	// How queues behave when a client does not say.
	Queues *RabbitMQClusterPropertiesQueues `json:"queues,omitempty"`
	// CPU and memory per node, either by preset or explicitly.
	Sizing *RabbitMQClusterPropertiesSizing `json:"sizing,omitempty"`
	// The message store, per node.
	Storage *RabbitMQClusterPropertiesStorage `json:"storage,omitempty"`
	// RabbitMQ version. Minor upgrades are applied automatically in the maintenance window; a major upgrade is an explicit update to this field. Only 4.x is offered: classic queue mirroring was removed in 4.0, so on every version here the replicated queue type is the quorum queue.
	Version RabbitMQClusterVersion `json:"version"`
}

// RabbitMQClusterPropertiesLimits is Broker limits a client can run into.
type RabbitMQClusterPropertiesLimits struct {
	// Largest message a client may publish, in bytes. Raising it costs memory on every node that holds a copy, which for a quorum queue is all of them.
	MaxMessageSize *int64 `json:"maxMessageSize,omitempty"`
}

// RabbitMQClusterPropertiesPlugins is Protocols and features beyond AMQP.
type RabbitMQClusterPropertiesPlugins struct {
	// Plugins to enable on top of the three the operator always enables — peer discovery, the management UI and the Prometheus endpoint. Each recognised plugin adds its own port to the cluster's in-cluster Service.
	Additional []RabbitMQClusterAdditional `json:"additional,omitempty"`
}

// RabbitMQClusterPropertiesQueues is How queues behave when a client does not say.
type RabbitMQClusterPropertiesQueues struct {
	// The queue type a client gets when it declares a queue without asking for one. Quorum by default — docs/plan/12: a quorum queue is replicated through Raft across the nodes above, and on 4.x a classic queue is not replicated at all, so leaving this unset would put a single-node queue on a cluster the tenant paid three nodes for. This is a node-wide fallback: a vhost created with its own default queue type overrides it.
	DefaultType *RabbitMQClusterDefaultType `json:"defaultType,omitempty"`
}

// RabbitMQClusterPropertiesSizing is CPU and memory per node, either by preset or explicitly.
type RabbitMQClusterPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Brokers use the c1 family, which is 1 vCPU to 2 GiB and guaranteed rather than burstable.
	Preset *RabbitMQClusterPreset `json:"preset,omitempty"`
}

// RabbitMQClusterPropertiesStorage is The message store, per node.
type RabbitMQClusterPropertiesStorage struct {
	// StorageClass name. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Message-store volume size per node, in Kubernetes quantity form. Grows online; never shrinks. A quorum queue keeps its whole Raft log on every member, so this is the same figure on every node rather than a share of one.
	Size string `json:"size"`
}

// RabbitMQClusterResource is one RabbitMQ cluster, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type RabbitMQClusterResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data RabbitMQClusterData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *RabbitMQClusterResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// RabbitMQClusterListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type RabbitMQClusterListKeysResult struct {
	// The in-cluster management UI and HTTP API, http://host:port. docs/plan/12 fronts this with the portal's authenticated proxy; it is never a public route.
	ManagementUrl string `json:"managementUrl"`
	// The generated user's password, read from the cluster's default-user Secret for this call only. ⚠ Neither this value nor the user name is chosen by the platform: the operator generates both at first boot and RabbitMQ's own guest/guest account is never created.
	Password string `json:"password"`
	// The in-cluster AMQP URL, amqp://host:port. ⚠ There is no external address to return — this type declares no external listener at all, for the reason its schema gives.
	Url string `json:"url"`
	// The generated broker user.
	User string `json:"user"`
}

// MonitorWorkspaceLogs is the values /properties/retention/logs accepts. ⚠ Closed: the write path refuses anything else.
type MonitorWorkspaceLogs string

const (
	MonitorWorkspaceLogsShort    MonitorWorkspaceLogs = "short"
	MonitorWorkspaceLogsStandard MonitorWorkspaceLogs = "standard"
	MonitorWorkspaceLogsExtended MonitorWorkspaceLogs = "extended"
)

// MonitorWorkspaceMetrics is the values /properties/retention/metrics accepts. ⚠ Closed: the write path refuses anything else.
type MonitorWorkspaceMetrics string

const (
	MonitorWorkspaceMetricsShort    MonitorWorkspaceMetrics = "short"
	MonitorWorkspaceMetricsStandard MonitorWorkspaceMetrics = "standard"
	MonitorWorkspaceMetricsExtended MonitorWorkspaceMetrics = "extended"
)

// MonitorWorkspaceTraces is the values /properties/retention/traces accepts. ⚠ Closed: the write path refuses anything else.
type MonitorWorkspaceTraces string

const (
	MonitorWorkspaceTracesShort    MonitorWorkspaceTraces = "short"
	MonitorWorkspaceTracesStandard MonitorWorkspaceTraces = "standard"
	MonitorWorkspaceTracesExtended MonitorWorkspaceTraces = "extended"
)

// MonitorWorkspaceData is Monitor workspace: the body a caller writes. A tenancy in the platform's metrics, logs and traces stores, with its own retention, quota, ingest key and read-only datasource endpoints.
type MonitorWorkspaceData struct {
	// The region the workspace is billed in, and the region whose telemetry stores hold its data.
	Location string `json:"location"`
	// The workspace's own settings.
	Properties *MonitorWorkspaceProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// MonitorWorkspaceProperties is The workspace's own settings.
type MonitorWorkspaceProperties struct {
	// The regional cluster whose telemetry data plane serves this workspace. ⚠ Nothing is deployed into it: a workspace is a tenancy in stores the platform already runs, and this is where its ingest routing is published.
	ClusterID string `json:"clusterId"`
	// Whether this workspace may be destroyed before its seven-day recovery window is out. Once true it stays true for the rest of the workspace's life, and a purge is refused while it is set.
	PurgeProtection bool `json:"purgeProtection"`
	// What the workspace may take in per day, and what happens when it takes in more.
	Quota *MonitorWorkspacePropertiesQuota `json:"quota,omitempty"`
	// How long each signal is kept. Priced per tier — docs/plan/16 § Cost and retention honesty.
	Retention *MonitorWorkspacePropertiesRetention `json:"retention,omitempty"`
}

// MonitorWorkspacePropertiesQuota is What the workspace may take in per day, and what happens when it takes in more.
type MonitorWorkspacePropertiesQuota struct {
	// Distinct values one metric label may take before the series carrying it are rejected. The rejection names the offending label — docs/plan/16 § Ingest — because a rejection nobody can diagnose is one the client just retries.
	CardinalityCap int64 `json:"cardinalityCap"`
	// Log lines accepted per day, in gibibytes.
	LogsGbPerDay int64 `json:"logsGbPerDay"`
	// Metric samples accepted per day, in gibibytes. This is the first factor of what the workspace draws against the subscription's storage quota; the second is the retention tier.
	MetricsGbPerDay int64 `json:"metricsGbPerDay"`
	// The percentage of data still accepted once a signal is over its daily allowance. ⚠ It cannot be zero: docs/plan/16 requires that going over quota samples at a visible rate rather than dropping silently, and zero is a silent drop spelled as a rate.
	OverQuotaSampleRate int64 `json:"overQuotaSampleRate"`
	// Active metric series the workspace may hold at once. One tenant putting a request id in a metric label is how a shared time-series database dies, so this is a ceiling rather than a guideline.
	SeriesCap int64 `json:"seriesCap"`
	// Spans accepted per day, in gibibytes.
	TracesGbPerDay int64 `json:"tracesGbPerDay"`
}

// MonitorWorkspacePropertiesRetention is How long each signal is kept. Priced per tier — docs/plan/16 § Cost and retention honesty.
type MonitorWorkspacePropertiesRetention struct {
	// Logs retention tier: short is 7 days, standard 30, extended 90. ⚠ Shortening this under an existing workspace destroys the lines already outside the new window, and the reconciler refuses the change rather than applying it.
	Logs MonitorWorkspaceLogs `json:"logs"`
	// Metrics retention tier: short is 15 days, standard 90, extended 400. ⚠ Shortening this under an existing workspace destroys the samples already outside the new window, and the reconciler refuses the change rather than applying it.
	Metrics MonitorWorkspaceMetrics `json:"metrics"`
	// Traces retention tier: short is 3 days, standard 14, extended 30. ⚠ Shortening this under an existing workspace destroys the spans already outside the new window, and the reconciler refuses the change rather than applying it.
	Traces MonitorWorkspaceTraces `json:"traces"`
}

// MonitorWorkspaceResource is one Monitor workspace, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type MonitorWorkspaceResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data MonitorWorkspaceData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *MonitorWorkspaceResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// MonitorWorkspaceListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type MonitorWorkspaceListKeysResult struct {
	// The VictoriaMetrics tenant this workspace's metrics live under. It appears in every metrics URL below and is returned rather than left to be guessed at.
	AccountID string `json:"accountId"`
	// The ClickHouse database this workspace's logs, traces and events live in. It is the one platform-chosen string that reaches the tenant's SQL.
	Database string `json:"database"`
	// The ingest key, read from the tenant's vault for this call only. It authenticates writes on every endpoint above.
	IngestKey string `json:"ingestKey"`
	// Where OTLP over gRPC is accepted, host:port.
	OtlpEndpoint string `json:"otlpEndpoint"`
	// The read-only PromQL/MetricsQL datasource URL, for the tenant's own Grafana or an external one.
	PromqlEndpoint string `json:"promqlEndpoint"`
	// Where Prometheus remote-write is accepted.
	RemoteWriteEndpoint string `json:"remoteWriteEndpoint"`
	// The read-only SQL datasource URL for logs, traces and events.
	SqlEndpoint string `json:"sqlEndpoint"`
}

// PublicIPAddressData is Public IP address: the body a caller writes. A public address allocated from the region's pool, which a load balancer or a gateway can later be given. On its own it carries no traffic.
type PublicIPAddressData struct {
	// The region the address is allocated in. ⚠ It must be a region whose operator has an external pool — nothing checks that, and an address in a region with none never becomes ready.
	Location string `json:"location"`
	// The address's own settings.
	Properties *PublicIPAddressProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// PublicIPAddressProperties is The address's own settings.
type PublicIPAddressProperties struct {
	// A particular address to ask for, rather than whichever one is free.
	Address *PublicIPAddressPropertiesAddress `json:"address,omitempty"`
	// The cluster whose external pool the address is allocated from. ⚠ An address is only reachable from the fabric that announces it, so a load balancer in another cluster cannot use it.
	ClusterID string `json:"clusterId"`
}

// PublicIPAddressPropertiesAddress is A particular address to ask for, rather than whichever one is free.
type PublicIPAddressPropertiesAddress struct {
	// The IPv4 address to reclaim, or empty to be given whichever one is free. ⚠ A bare address and not a prefix — 10.100.0.7, never 10.100.0.7/32. An address that is already taken is refused by the fabric rather than by the API.
	V4 *string `json:"v4,omitempty"`
	// The IPv6 address to reclaim, or empty to be given whichever one is free. ⚠ Lower case only — the fabric refuses an address with an upper-case letter in it. Whether an address has a v6 half at all is decided by the pool it comes from, not here.
	V6 *string `json:"v6,omitempty"`
}

// PublicIPAddressResource is one Public IP address, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type PublicIPAddressResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data PublicIPAddressData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *PublicIPAddressResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// PublicIPAddressShowAllocationResult is what showAllocation returns.
type PublicIPAddressShowAllocationResult struct {
	// The NAT rule currently using this address, or empty. ⚠ Empty means the address is allocated and carries no traffic, which in this api-version is every address — nothing can attach one yet.
	AttachedTo *string `json:"attachedTo,omitempty"`
	// The MAC address the fabric bound to it. ⚠ Reported because it is what an operator needs to find this address in an ARP table when it is unreachable.
	MacAddress *string `json:"macAddress,omitempty"`
	// Whether the fabric has finished announcing the address. ⚠ False with an address already reported is the ordinary state for a few seconds after create.
	Ready bool `json:"ready"`
	// When the platform read the object, RFC 3339. ⚠ The read time rather than the time the fabric wrote the figures, because the object carries no timestamp on them.
	SampledAt string `json:"sampledAt"`
	// The IPv4 address the fabric allocated, or empty when it has not allocated one yet.
	V4 string `json:"v4"`
	// The IPv6 address the fabric allocated, or empty for an IPv4-only pool.
	V6 *string `json:"v6,omitempty"`
}

// VirtualNetworkData is Virtual network: the body a caller writes. A private routing domain on Kube-OVN with its own address space. Network-layer tenant separation: a private routing domain with its own address space, on shared hardware.
type VirtualNetworkData struct {
	// The region the virtual network lives in. ⚠ It selects which reserved ranges the address space is checked against — a region's underlay is part of that list.
	Location string `json:"location"`
	// The virtual network's own settings.
	Properties *VirtualNetworkProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// VirtualNetworkProperties is The virtual network's own settings.
type VirtualNetworkProperties struct {
	// The address range the network plans for. ⚠ Declarative: it is checked against the region's reserved ranges and is not rendered into the fabric, because a Kube-OVN Vpc carries no CIDR — the subnets do.
	AddressSpace *VirtualNetworkPropertiesAddressSpace `json:"addressSpace,omitempty"`
	// The cluster whose fabric carries the network.
	ClusterID string `json:"clusterId"`
	// Whether the network's router is attached to the external network. Off by default: a network that reaches the outside without being asked is a network whose owner did not choose that. ⚠ Turning it on requires the cluster to have an external subnet configured; without one the Vpc is accepted and the attachment never completes.
	EnableExternal *bool `json:"enableExternal,omitempty"`
}

// VirtualNetworkPropertiesAddressSpace is The address range the network plans for. ⚠ Declarative: it is checked against the region's reserved ranges and is not rendered into the fabric, because a Kube-OVN Vpc carries no CIDR — the subnets do.
type VirtualNetworkPropertiesAddressSpace struct {
	// The IPv4 range the network plans for, in CIDR form. It may overlap another of your own virtual networks — that is what a VPC is for — and it may not overlap a range the platform reserves, which is refused with the conflicting range named.
	V4 string `json:"v4"`
	// The IPv6 range the network plans for, or empty for an IPv4-only network. ⚠ docs/plan/14 § IPv6 asks for dual-stack from day one rather than as a retrofit, which is why this is here at the first api-version even though a v4-only network is the ordinary case.
	V6 *string `json:"v6,omitempty"`
}

// VirtualNetworkResource is one Virtual network, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type VirtualNetworkResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data VirtualNetworkData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *VirtualNetworkResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// VirtualNetworkShowIsolationResult is what showIsolation returns.
type VirtualNetworkShowIsolationResult struct {
	// What tenant isolation this network provides, in one sentence. It is the same sentence for every virtual network on the platform, and it is deliberately narrower than 'isolated'.
	Claim string `json:"claim"`
	// What this network does NOT guarantee, one entry per limit, each naming the guarantee, why the substrate does not deliver it, and what to ask for instead. ⚠ Read this before deciding a virtual network satisfies a compliance requirement.
	Limits []string `json:"limits"`
	// The technology enforcing the separation, named so that a tenant's own security review has something to review.
	Substrate string `json:"substrate"`
}

// LoadBalancerPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type LoadBalancerPreset string

const (
	LoadBalancerPresetC1Large  LoadBalancerPreset = "c1.large"
	LoadBalancerPresetC1Medium LoadBalancerPreset = "c1.medium"
	LoadBalancerPresetC1Small  LoadBalancerPreset = "c1.small"
)

// LoadBalancerVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type LoadBalancerVersion string

const (
	LoadBalancerVersionN32 LoadBalancerVersion = "3.2"
	LoadBalancerVersionN34 LoadBalancerVersion = "3.4"
)

// LoadBalancerData is Load balancer: the body a caller writes. An L4 TCP proxy on an address inside a virtual network, spreading connections across a pool of workload addresses with health checks and a connection limit.
type LoadBalancerData struct {
	// The region the load balancer is billed in. ⚠ It must be the region its virtual network is in — nothing checks that, because the network's own region is not readable from here.
	Location string `json:"location"`
	// The load balancer's own settings.
	Properties *LoadBalancerProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// LoadBalancerProperties is The load balancer's own settings.
type LoadBalancerProperties struct {
	// Where the connections go.
	Backend *LoadBalancerPropertiesBackend `json:"backend,omitempty"`
	// The cluster the proxy runs in. ⚠ It must be the cluster the virtual network was created in: a proxy in another cluster has no route into this network at all.
	ClusterID string `json:"clusterId"`
	// The address and port workloads connect to.
	Frontend *LoadBalancerPropertiesFrontend `json:"frontend,omitempty"`
	// How a backend is decided to be up. ⚠ Checking cannot be turned off — a proxy that sends connections to a dead server is worse than no proxy.
	Health *LoadBalancerPropertiesHealth `json:"health,omitempty"`
	// What the proxy refuses rather than passes on.
	Limits *LoadBalancerPropertiesLimits `json:"limits,omitempty"`
	// CPU and memory for the proxy.
	Sizing *LoadBalancerPropertiesSizing `json:"sizing,omitempty"`
	// The subnet of this virtual network the proxy sits on. ⚠ The proxy gets an address from it, so the frontend address below must be inside its range. A name that is not a subnet of this network is refused by the fabric rather than by the API, and the proxy pod never schedules.
	Subnet string `json:"subnet"`
	// Which HAProxy line to run. 3.2 is the long-term-support line and is the default; 3.4 is the current one. ⚠ A change here restarts the proxy and drops open connections.
	Version *LoadBalancerVersion `json:"version,omitempty"`
}

// LoadBalancerPropertiesBackend is Where the connections go.
type LoadBalancerPropertiesBackend struct {
	// The workload addresses to balance across, comma separated — for example 10.20.1.11,10.20.1.12. ⚠ Addresses and not resource ids: there is no service discovery and no DNS inside a virtual network, so an address is what a tenant has for their own workloads. At most 32.
	Addresses string `json:"addresses"`
	// The TCP port every backend address is reached on. ⚠ One port for the whole pool: a pool whose members listen on different ports is two pools.
	Port int64 `json:"port"`
}

// LoadBalancerPropertiesFrontend is The address and port workloads connect to.
type LoadBalancerPropertiesFrontend struct {
	// The TCP port the proxy listens on. ⚠ There is no protocol setting: HAProxy does not proxy UDP in any version, so every rule here is TCP.
	Port int64 `json:"port"`
	// The IPv4 address the proxy answers on, inside the subnet's range. ⚠ Required, and it is the one thing about this resource a tenant must choose: there is no DNS inside a virtual network, so an address nobody picked is an address nothing can be pointed at. A bare address and never a prefix.
	V4 string `json:"v4"`
	// The IPv6 address the proxy also answers on, or empty. ⚠ Lower case only, for the fabric's reason. It must be inside the subnet's IPv6 range, which means the subnet has to have one.
	V6 *string `json:"v6,omitempty"`
}

// LoadBalancerPropertiesHealth is How a backend is decided to be up. ⚠ Checking cannot be turned off — a proxy that sends connections to a dead server is worse than no proxy.
type LoadBalancerPropertiesHealth struct {
	// How many successful probes put a backend back into the pool.
	HealthyAfter *int64 `json:"healthyAfter,omitempty"`
	// How often each backend is probed with a TCP connection.
	IntervalSeconds *int64 `json:"intervalSeconds,omitempty"`
	// How many failed probes take a backend out of the pool.
	UnhealthyAfter *int64 `json:"unhealthyAfter,omitempty"`
}

// LoadBalancerPropertiesLimits is What the proxy refuses rather than passes on.
type LoadBalancerPropertiesLimits struct {
	// How many connections the frontend accepts at once. ⚠ Further connections wait in the kernel's accept queue rather than being refused, so this is a back-pressure setting rather than a firewall. It is also applied per backend server.
	MaxConnections *int64 `json:"maxConnections,omitempty"`
}

// LoadBalancerPropertiesSizing is CPU and memory for the proxy.
type LoadBalancerPropertiesSizing struct {
	// How much the proxy pod gets. An L4 proxy is mostly kernel work, so the small row carries far more than its size suggests; the larger rows are for many long-lived connections.
	Preset *LoadBalancerPreset `json:"preset,omitempty"`
}

// LoadBalancerResource is one Load balancer, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type LoadBalancerResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data LoadBalancerData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *LoadBalancerResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// LoadBalancerShowBackendsResult is what showBackends returns.
type LoadBalancerShowBackendsResult struct {
	// The address and port the proxy answers on.
	Frontend string `json:"frontend"`
	// What the answer is and is not — in particular that the servers are what the platform configured rather than what the proxy currently believes is healthy.
	Note string `json:"note"`
	// How many proxy pods are running and passing their readiness probe. ⚠ 0 with servers listed is a load balancer that is configured and carrying no traffic, which is the state this action exists to make visible.
	ReadyReplicas int64 `json:"readyReplicas"`
	// When the platform read the Deployment, RFC 3339.
	SampledAt string `json:"sampledAt"`
	// How many servers the pool carries.
	ServerCount int64 `json:"serverCount"`
	// One line per backend server, in the order the proxy is given them.
	Servers []string `json:"servers"`
}

// SecurityGroupData is Security group: the body a caller writes. A deny-by-default set of allow rules that become OVN ACLs on the ports in a virtual network. A workload may carry several.
type SecurityGroupData struct {
	// The region the security group lives in. It must be the network's own region — nothing checks that.
	Location string `json:"location"`
	// The security group's own settings.
	Properties *SecurityGroupProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// SecurityGroupProperties is The security group's own settings.
type SecurityGroupProperties struct {
	// Whether workloads that carry this same security group may reach each other without a rule. Off by default: it is the one setting here that permits traffic nobody wrote a rule for.
	AllowSameGroupTraffic *bool `json:"allowSameGroupTraffic,omitempty"`
	// The cluster whose fabric carries the security group. Must be the cluster the network is in — nothing checks that.
	ClusterID string `json:"clusterId"`
	// What workloads carrying this group may reach. ⚠ Same default-deny: an empty section permits no outbound traffic at all, which for most workloads means no DNS and no package downloads.
	Egress *SecurityGroupPropertiesEgress `json:"egress,omitempty"`
	// What may reach workloads carrying this group. ⚠ Anything not allowed here is dropped — the fabric installs a default-deny for every port in a security group, so an empty section permits nothing.
	Ingress *SecurityGroupPropertiesIngress `json:"ingress,omitempty"`
}

// SecurityGroupPropertiesEgress is What workloads carrying this group may reach. ⚠ Same default-deny: an empty section permits no outbound traffic at all, which for most workloads means no DNS and no package downloads.
type SecurityGroupPropertiesEgress struct {
	// Whether outbound ICMP is allowed to the remotes above. Off by default.
	AllowIcmp *bool `json:"allowIcmp,omitempty"`
	// The IPv4 range outbound traffic may reach, or empty for no IPv4 rules at all.
	RemoteV4 *string `json:"remoteV4,omitempty"`
	// The IPv6 range outbound traffic may reach, or empty for no IPv6 rules at all.
	RemoteV6 *string `json:"remoteV6,omitempty"`
	// TCP ports outbound traffic may reach, in the same form as the inbound list. Empty means no outbound TCP.
	TcpPorts *string `json:"tcpPorts,omitempty"`
	// UDP ports outbound traffic may reach. Empty means no outbound UDP — which includes DNS on port 53.
	UdpPorts *string `json:"udpPorts,omitempty"`
}

// SecurityGroupPropertiesIngress is What may reach workloads carrying this group. ⚠ Anything not allowed here is dropped — the fabric installs a default-deny for every port in a security group, so an empty section permits nothing.
type SecurityGroupPropertiesIngress struct {
	// Whether inbound ICMP is allowed from the remotes above. Off by default. ⚠ With this off, a workload in this group does not answer ping and does not receive path-MTU messages.
	AllowIcmp *bool `json:"allowIcmp,omitempty"`
	// The IPv4 range inbound traffic may come from, in CIDR form, or empty for no IPv4 rules at all. Use 0.0.0.0/0 for the whole internet.
	RemoteV4 *string `json:"remoteV4,omitempty"`
	// The IPv6 range inbound traffic may come from, or empty for no IPv6 rules at all. ⚠ A group with only an IPv4 remote silently permits nothing over IPv6, which on a dual-stack subnet is not the same as permitting nothing.
	RemoteV6 *string `json:"remoteV6,omitempty"`
	// TCP ports inbound traffic may reach, as a comma-separated list of ports and ranges — for example 80,443,8000-8100. Empty means no TCP is allowed inbound. ⚠ There is no way to say 'every protocol'; 1-65535 says 'every TCP port'.
	TcpPorts *string `json:"tcpPorts,omitempty"`
	// UDP ports inbound traffic may reach, in the same form as tcpPorts. Empty means no UDP is allowed inbound.
	UdpPorts *string `json:"udpPorts,omitempty"`
}

// SecurityGroupResource is one Security group, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type SecurityGroupResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data SecurityGroupData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *SecurityGroupResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// SecurityGroupShowEffectiveRulesResult is what showEffectiveRules returns.
type SecurityGroupShowEffectiveRulesResult struct {
	// Whether workloads carrying this same group reach each other without a rule. ⚠ Returned because it is the one permission that is not in the list above.
	AllowSameGroupTraffic bool `json:"allowSameGroupTraffic"`
	// How many rules there are. ⚠ Worth reading next to the body: one port list of three entries with two remotes is six rules.
	Count int64 `json:"count"`
	// What happens to traffic no rule above matches. It is always 'drop' — the fabric installs a default-deny for every port in a security group and this resource cannot change that.
	DefaultAction string `json:"defaultAction"`
	// ⚠ What this answer is and is not. It is what the platform asks the fabric for, derived from the resource's stored body — not a reading of the ACLs OVN currently holds.
	Note string `json:"note"`
	// Every rule this security group's body becomes, in the order it is written to the fabric, one sentence each. An empty list means the group permits nothing, which is a valid and fully restrictive configuration.
	Rules []string `json:"rules"`
}

// SubnetData is Subnet: the body a caller writes. A range inside a virtual network that workloads are given addresses from, with optional outbound NAT and optional isolation from other subnets.
type SubnetData struct {
	// The region the subnet lives in. ⚠ It selects which reserved ranges the prefix is checked against, and it must be the network's own region — nothing checks that.
	Location string `json:"location"`
	// The subnet's own settings.
	Properties *SubnetProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// SubnetProperties is The subnet's own settings.
type SubnetProperties struct {
	// The range addresses are allocated from.
	AddressPrefix *SubnetPropertiesAddressPrefix `json:"addressPrefix,omitempty"`
	// The cluster whose fabric carries the subnet. Must be the cluster the network is in — nothing checks that, and a subnet placed elsewhere binds to a Vpc that does not exist there.
	ClusterID string `json:"clusterId"`
	// Whether the fabric answers DHCP in this subnet. Off by default: an address is assigned to a workload's port when the port is created, and DHCP is for guests that insist on asking — a virtual machine rather than a container.
	EnableDhcp *bool `json:"enableDhcp,omitempty"`
	// Whether workloads in this subnet reach the internet through source NAT. Off by default. ⚠ This is the opposite of Kube-OVN's own default for its cluster subnet, deliberately: a tenant subnet that silently egresses is a surprise, and docs/plan/12 § Cross-cutting decisions defaults external exposure to off. It also requires the network's enableExternal to be on; without it the flag is accepted and nothing egresses.
	NatOutgoing *bool `json:"natOutgoing,omitempty"`
	// Whether the subnet refuses traffic from other subnets. Off by default. ⚠ In this api-version it has no exception list, so on means no traffic from any other subnet in the network at all.
	Private *bool `json:"private,omitempty"`
}

// SubnetPropertiesAddressPrefix is The range addresses are allocated from.
type SubnetPropertiesAddressPrefix struct {
	// The IPv4 prefix, in CIDR form. ⚠ Host bits are cleared by the fabric, so 10.20.1.7/24 is stored as 10.20.1.0/24 and both name the same network. It may not overlap a range the platform reserves, which is refused with the conflicting range named.
	V4 string `json:"v4"`
	// The IPv6 prefix, or empty for an IPv4-only subnet. Setting both makes the subnet dual-stack — docs/plan/14 § IPv6.
	V6 *string `json:"v6,omitempty"`
}

// SubnetResource is one Subnet, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type SubnetResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data SubnetData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *SubnetResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// SubnetListAddressUsageResult is what listAddressUsage returns.
type SubnetListAddressUsageResult struct {
	// When the fabric last reported these figures, RFC 3339. ⚠ Returned because a count with no timestamp is a count a caller will read as live, and these come from the Subnet object's status rather than from a live query.
	SampledAt string `json:"sampledAt"`
	// IPv4 address usage.
	V4 *SubnetListAddressUsageResultV4 `json:"v4,omitempty"`
	// IPv6 address usage.
	V6 *SubnetListAddressUsageResultV6 `json:"v6,omitempty"`
}

// SubnetListAddressUsageResultV4 is IPv4 address usage.
type SubnetListAddressUsageResultV4 struct {
	// How many IPv4 addresses remain. ⚠ Zero here is the answer to 'why will nothing schedule in this subnet'.
	Available int64 `json:"available"`
	// How many IPv4 addresses the prefix contains that may be allocated, excluding the network address, the broadcast address and the gateway.
	Total int64 `json:"total"`
	// How many IPv4 addresses are currently allocated to ports.
	Used int64 `json:"used"`
}

// SubnetListAddressUsageResultV6 is IPv6 address usage.
type SubnetListAddressUsageResultV6 struct {
	// How many IPv6 addresses remain, as a decimal string, or empty for an IPv4-only subnet.
	Available *string `json:"available,omitempty"`
	// How many IPv6 addresses the prefix contains, as a decimal string, or empty for an IPv4-only subnet. ⚠ A string because a /64 does not fit in the signed 64-bit integer SchemaKind.WholeNumber validates through.
	Total *string `json:"total,omitempty"`
}

// WidgetTier is the values /properties/tier accepts. ⚠ Closed: the write path refuses anything else.
type WidgetTier string

const (
	WidgetTierFree     WidgetTier = "free"
	WidgetTierBasic    WidgetTier = "basic"
	WidgetTierStandard WidgetTier = "standard"
	WidgetTierPremium  WidgetTier = "premium"
)

// WidgetData is Widget: the body a caller writes. A ConfigMap with two fields in it.
type WidgetData struct {
	// The region the widget is billed in.
	Location string `json:"location"`
	// The widget's own settings.
	Properties *WidgetProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// WidgetProperties is The widget's own settings.
type WidgetProperties struct {
	// The ranges the widget claims to serve. Shape only; nothing enforces it.
	AllowedCidrs []string `json:"allowedCidrs,omitempty"`
	// The cluster whose namespace holds the widget's ConfigMap.
	ClusterID string `json:"clusterId"`
	// Whether the widget is switched on. Defaults to off when absent.
	Enabled *bool `json:"enabled,omitempty"`
	// What the ConfigMap's 'message' key says.
	Message string `json:"message"`
	// How many copies of the ConfigMap's message the widget claims to hold.
	Replicas *int64 `json:"replicas,omitempty"`
	// When the widget was retired, or null while it is live.
	RetiredOn *string `json:"retiredOn,omitempty"`
	// How much the widget's ConfigMap is allowed to say.
	Tier *WidgetTier `json:"tier,omitempty"`
}

// WidgetResource is one Widget, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type WidgetResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data WidgetData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *WidgetResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// WidgetPingContent is the parameters of ping.
type WidgetPingContent struct {
	// Text the response repeats back.
	Echo *string `json:"echo,omitempty"`
}

// WidgetPingResult is what ping returns.
type WidgetPingResult struct {
	// When the ping was served.
	At string `json:"at"`
	// What was echoed.
	Echo string `json:"echo"`
}

// OpenSearchServicePreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type OpenSearchServicePreset string

const (
	OpenSearchServicePresetM12xlarge OpenSearchServicePreset = "m1.2xlarge"
	OpenSearchServicePresetM14xlarge OpenSearchServicePreset = "m1.4xlarge"
	OpenSearchServicePresetM1Large   OpenSearchServicePreset = "m1.large"
	OpenSearchServicePresetM1Medium  OpenSearchServicePreset = "m1.medium"
	OpenSearchServicePresetM1Small   OpenSearchServicePreset = "m1.small"
	OpenSearchServicePresetM1Xlarge  OpenSearchServicePreset = "m1.xlarge"
)

// OpenSearchServiceVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type OpenSearchServiceVersion string

const (
	OpenSearchServiceVersionN2190 OpenSearchServiceVersion = "2.19.0"
	OpenSearchServiceVersionN310  OpenSearchServiceVersion = "3.1.0"
)

// OpenSearchServiceData is OpenSearch service: the body a caller writes. A managed OpenSearch cluster on the OpenSearch operator, with dedicated cluster-manager, data and optional coordinating node pools and operator-generated transport and HTTP TLS.
type OpenSearchServiceData struct {
	// The region the service is billed in.
	Location string `json:"location"`
	// The service's own settings.
	Properties *OpenSearchServiceProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// OpenSearchServiceProperties is The service's own settings.
type OpenSearchServiceProperties struct {
	// The cluster whose namespace holds the search service.
	ClusterID string `json:"clusterId"`
	// Number of coordinating-only nodes — nodes that hold no data and no cluster state and exist to fan a search out and merge the results. Zero is the default and is right until a query pattern makes one data node the bottleneck for every search. They are sized by the same preset as the data nodes.
	CoordinatingNodes int64 `json:"coordinatingNodes"`
	// Number of data nodes. This is the capacity axis: total raw capacity is this count times the disk size below, before replicas. Every data node also carries the ingest role, so an indexing pipeline needs no separate pool.
	DataNodes int64 `json:"dataNodes"`
	// Number of dedicated cluster-manager nodes. They hold the cluster state in a quorum, so three is the smallest count that survives losing one. One is offered for development and has no quorum at all. An even count is worse than the odd count below it and the API cannot say so — see the service's own documentation.
	MasterNodes int64 `json:"masterNodes"`
	// What the platform scrapes.
	Monitoring *OpenSearchServicePropertiesMonitoring `json:"monitoring,omitempty"`
	// CPU and memory per data node and per coordinating node, either by preset or explicitly. The cluster-manager nodes are sized by the platform and are not affected.
	Sizing *OpenSearchServicePropertiesSizing `json:"sizing,omitempty"`
	// The data volume, per data node.
	Storage *OpenSearchServicePropertiesStorage `json:"storage,omitempty"`
	// OpenSearch version. ⚠ OpenSearch is a fork of Elasticsearch 7.10 (ADR-011 — Elasticsearch is SSPL and is not available to us) and the two have diverged since, so an Elasticsearch 8 client is not promised anything here. Upgrades between the values below are online and in the maintenance window; a third value is a new api-version.
	Version OpenSearchServiceVersion `json:"version"`
}

// OpenSearchServicePropertiesMonitoring is What the platform scrapes.
type OpenSearchServicePropertiesMonitoring struct {
	// Whether the operator is asked for a ServiceMonitor. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". ⚠ The metrics themselves come from the prometheus-exporter plugin, which the operator installs into every node on the first reconcile after this is turned on — so turning it on restarts the pods and turning it off does not remove the plugin.
	Enabled *bool `json:"enabled,omitempty"`
}

// OpenSearchServicePropertiesSizing is CPU and memory per data node and per coordinating node, either by preset or explicitly. The cluster-manager nodes are sized by the platform and are not affected.
type OpenSearchServicePropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 8Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Search nodes use the m1 family, which is 1 vCPU to 8 GiB, because OpenSearch is bound by heap and by the filesystem cache rather than by CPU. The m1 rungs below m1.small are deliberately not offered: OpenSearch derives its JVM heap from the container limit and a node under 4 GiB fails a bootstrap check after passing its readiness probe.
	Preset *OpenSearchServicePreset `json:"preset,omitempty"`
}

// OpenSearchServicePropertiesStorage is The data volume, per data node.
type OpenSearchServicePropertiesStorage struct {
	// StorageClass name for every node pool. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Disk size per data node, in Kubernetes quantity form. Grows online; never shrinks. The cluster-manager and coordinating nodes get a fixed 10Gi that is not configurable and is counted against the storage quota anyway.
	Size string `json:"size"`
}

// OpenSearchServiceResource is one OpenSearch service, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type OpenSearchServiceResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data OpenSearchServiceData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *OpenSearchServiceResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// OpenSearchServiceListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type OpenSearchServiceListKeysResult struct {
	// The in-cluster REST endpoint, https://host:port. ⚠ No external address is returned, because there is none — see the service's own documentation on exposure.
	Endpoint string `json:"endpoint"`
	// The administrative password, read from the tenant's Vault for this call only.
	Password string `json:"password"`
	// The administrative user. Always "admin": the operator generates exactly one credential and does not name it.
	Username string `json:"username"`
}

// StorageAccountReplication is the values /properties/replication accepts. ⚠ Closed: the write path refuses anything else.
type StorageAccountReplication string

const (
	StorageAccountReplicationDifferentDataCenter StorageAccountReplication = "DifferentDataCenter"
	StorageAccountReplicationDifferentRack       StorageAccountReplication = "DifferentRack"
	StorageAccountReplicationNone                StorageAccountReplication = "None"
	StorageAccountReplicationSameRack            StorageAccountReplication = "SameRack"
)

// StorageAccountPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type StorageAccountPreset string

const (
	StorageAccountPresetS12xlarge StorageAccountPreset = "s1.2xlarge"
	StorageAccountPresetS14xlarge StorageAccountPreset = "s1.4xlarge"
	StorageAccountPresetS1Large   StorageAccountPreset = "s1.large"
	StorageAccountPresetS1Medium  StorageAccountPreset = "s1.medium"
	StorageAccountPresetS1Micro   StorageAccountPreset = "s1.micro"
	StorageAccountPresetS1Nano    StorageAccountPreset = "s1.nano"
	StorageAccountPresetS1Small   StorageAccountPreset = "s1.small"
	StorageAccountPresetS1Xlarge  StorageAccountPreset = "s1.xlarge"
)

// StorageAccountVersion is the values /properties/version accepts. ⚠ Closed: the write path refuses anything else.
type StorageAccountVersion string

const (
	StorageAccountVersionN440 StorageAccountVersion = "4.40"
	StorageAccountVersionN441 StorageAccountVersion = "4.41"
)

// StorageAccountData is Storage account: the body a caller writes. A managed S3-compatible object store on SeaweedFS, with replicated volume servers, a filer and a scalable S3 gateway.
type StorageAccountData struct {
	// The region the account is billed in.
	Location string `json:"location"`
	// The account's own settings.
	Properties *StorageAccountProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// StorageAccountProperties is The account's own settings.
type StorageAccountProperties struct {
	// The cluster whose namespace holds the object store.
	ClusterID string `json:"clusterId"`
	// The S3 gateway — docs/plan/15's ADR-008: "the API is S3".
	Gateway *StorageAccountPropertiesGateway `json:"gateway,omitempty"`
	// Number of master servers. The masters hold the volume topology in a Raft group, so three is the smallest count that survives losing one. One is offered for development and has no quorum at all.
	Masters int64 `json:"masters"`
	// What the platform scrapes.
	Monitoring *StorageAccountPropertiesMonitoring `json:"monitoring,omitempty"`
	// Where the second copy of every object is placed. None keeps one copy and is for scratch data only. A placement the cluster's topology cannot satisfy leaves volumes read-only rather than failing the create. Immutable, because SeaweedFS applies this to volumes as they are created and never rewrites the ones that already exist — a change would split the account into two durability promises with no way to tell which object got which.
	Replication StorageAccountReplication `json:"replication"`
	// CPU and memory per volume server, either by preset or explicitly. The masters, the filer and the S3 gateway are sized by the platform and are not affected.
	Sizing *StorageAccountPropertiesSizing `json:"sizing,omitempty"`
	// The data volume, per volume server.
	Storage *StorageAccountPropertiesStorage `json:"storage,omitempty"`
	// SeaweedFS version. ⚠ SeaweedFS ships a release roughly weekly and maintains no long-term branch, so docs/plan/12's "supported major versions" is a shape this project does not have; the two values here are the two most recent releases and a new api-version is what adds a third.
	Version StorageAccountVersion `json:"version"`
	// Number of volume servers. This is the capacity axis: total raw capacity is this count times the volume size below, before replication.
	VolumeServers int64 `json:"volumeServers"`
}

// StorageAccountPropertiesGateway is The S3 gateway — docs/plan/15's ADR-008: "the API is S3".
type StorageAccountPropertiesGateway struct {
	// Number of S3 gateway pods. The gateway is stateless, so this is a throughput and availability setting rather than a topology one.
	Replicas int64 `json:"replicas"`
}

// StorageAccountPropertiesMonitoring is What the platform scrapes.
type StorageAccountPropertiesMonitoring struct {
	// Whether the operator is asked for a ServiceMonitor per component. On by default — docs/plan/12: "a managed service the tenant cannot see the health of is a black box they will not trust with production". Turning it off removes the metrics port as well as the scrape, which is the operator's own behaviour rather than this provider's.
	Enabled *bool `json:"enabled,omitempty"`
}

// StorageAccountPropertiesSizing is CPU and memory per volume server, either by preset or explicitly. The masters, the filer and the S3 gateway are sized by the platform and are not affected.
type StorageAccountPropertiesSizing struct {
	// Explicit vCPU quantity in Kubernetes form, for example 500m or 2. Empty means take it from the preset.
	Cpu *string `json:"cpu,omitempty"`
	// Explicit memory quantity in Kubernetes form, for example 4Gi. Empty means take it from the preset.
	Memory *string `json:"memory,omitempty"`
	// A sizing preset from docs/plan/12. Volume servers use the s1 family, which is 1 vCPU to 4 GiB.
	Preset *StorageAccountPreset `json:"preset,omitempty"`
}

// StorageAccountPropertiesStorage is The data volume, per volume server.
type StorageAccountPropertiesStorage struct {
	// StorageClass name for the volume servers. Empty means the cluster default.
	Class *string `json:"class,omitempty"`
	// Data volume size per volume server, in Kubernetes quantity form. Grows online; never shrinks.
	Size string `json:"size"`
}

// StorageAccountResource is one Storage account, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type StorageAccountResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data StorageAccountData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *StorageAccountResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// StorageAccountListKeysResult is what listKeys returns. ⚠ Secret material — never log or persist this.
type StorageAccountListKeysResult struct {
	// The access key id. Not secret on its own; useless without the pair below.
	AccessKeyID string `json:"accessKeyId"`
	// The in-cluster S3 endpoint, http://host:port. ⚠ No external address is returned, because there is none — see the account's own documentation on exposure.
	Endpoint string `json:"endpoint"`
	// The region name an S3 client signs with. SeaweedFS does not route on it, and SigV4 refuses to sign without one, so it is returned rather than left for the caller to guess.
	Region string `json:"region"`
	// The secret access key, read from the tenant's Vault for this call only.
	SecretAccessKey string `json:"secretAccessKey"`
}

// BucketData is Bucket: the body a caller writes. An S3 bucket inside a managed object-storage account, with an optional size limit and optional object versioning.
type BucketData struct {
	// The region the bucket is billed in.
	Location string `json:"location"`
	// The bucket's own settings.
	Properties *BucketProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// BucketProperties is The bucket's own settings.
type BucketProperties struct {
	// The cluster whose namespace holds the bucket. Must be the cluster the account is in — nothing checks that, and a bucket placed elsewhere is applied into a namespace with no object store in it.
	ClusterID string `json:"clusterId"`
	// How much the bucket may hold.
	Quota *BucketPropertiesQuota `json:"quota,omitempty"`
	// Whether the bucket keeps previous versions of an overwritten object. Off by default. ⚠ docs/plan/15 § Object storage: SeaweedFS' S3 implementation is "good but not complete" and names object versioning as one of the partial areas, so the supported-operations table for the deployed version — not this property — is what says how completely it behaves.
	Versioning *bool `json:"versioning,omitempty"`
}

// BucketPropertiesQuota is How much the bucket may hold.
type BucketPropertiesQuota struct {
	// The bucket's size limit, in Kubernetes quantity form. Empty means no limit other than the account's own provisioned capacity. ⚠ This is a limit inside capacity the account already reserved; it does not add any, and it is not what the bucket is billed on.
	Size *string `json:"size,omitempty"`
}

// BucketResource is one Bucket, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type BucketResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data BucketData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *BucketResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// BucketStatsResult is what stats returns.
type BucketStatsResult struct {
	// How many objects the bucket holds, as of the last sample.
	ObjectCount int64 `json:"objectCount"`
	// When the two figures above were sampled, RFC 3339. ⚠ Returned because a sampled number with no timestamp is a number a caller will read as live.
	SampledAt string `json:"sampledAt"`
	// How many bytes the bucket holds before replication, as of the last sample. ⚠ Sampled rather than live — docs/plan/15 § Metering samples SeaweedFS volume stats hourly per bucket — so it is not a number to write an assertion against immediately after a PUT.
	SizeBytes int64 `json:"sizeBytes"`
}

// CloudTerminalVariant is the values /properties/image/variant accepts. ⚠ Closed: the write path refuses anything else.
type CloudTerminalVariant string

const (
	CloudTerminalVariantDefault CloudTerminalVariant = "default"
	CloudTerminalVariantMinimal CloudTerminalVariant = "minimal"
)

// CloudTerminalEgress is the values /properties/network/egress accepts. ⚠ Closed: the write path refuses anything else.
type CloudTerminalEgress string

const (
	CloudTerminalEgressInternet   CloudTerminalEgress = "Internet"
	CloudTerminalEgressTenantOnly CloudTerminalEgress = "TenantOnly"
)

// CloudTerminalPreset is the values /properties/sizing/preset accepts. ⚠ Closed: the write path refuses anything else.
type CloudTerminalPreset string

const (
	CloudTerminalPresetC1Large  CloudTerminalPreset = "c1.large"
	CloudTerminalPresetC1Medium CloudTerminalPreset = "c1.medium"
	CloudTerminalPresetC1Small  CloudTerminalPreset = "c1.small"
)

// CloudTerminalData is Cloud terminal: the body a caller writes. A browser shell running in this subscription's own cluster, holding a managed identity, with a persistent home directory and no stored credential.
type CloudTerminalData struct {
	// The region the console is billed in.
	Location string `json:"location"`
	// The console's own settings.
	Properties *CloudTerminalProperties `json:"properties,omitempty"`
	// Key/value tags, at most 50 pairs — docs/plan/06 § Tags, locks. Values are strings; the cap applies to the merged set, so a PATCH that adds one tag to a full bag is refused.
	Tags map[string]string `json:"tags,omitempty"`
}

// CloudTerminalProperties is The console's own settings.
type CloudTerminalProperties struct {
	// docs/plan/19 § Auditing. Who, when, from where, which subscription and for how long is always recorded and is not a setting.
	Audit *CloudTerminalPropertiesAudit `json:"audit,omitempty"`
	// The cluster whose namespace holds the shell and its home volume.
	ClusterID string `json:"clusterId"`
	// The persistent home directory. The only part of a console that survives an idle reclaim.
	Home *CloudTerminalPropertiesHome `json:"home,omitempty"`
	// Who the shell runs as.
	Identity *CloudTerminalPropertiesIdentity `json:"identity,omitempty"`
	// Which shell image to run.
	Image *CloudTerminalPropertiesImage `json:"image,omitempty"`
	// What the shell may reach. Nothing may ever reach the shell.
	Network *CloudTerminalPropertiesNetwork `json:"network,omitempty"`
	// When a shell stops. Both numbers are ceilings a tenant may lower and neither may be turned off.
	Session *CloudTerminalPropertiesSession `json:"session,omitempty"`
	// CPU and memory for the shell.
	Sizing *CloudTerminalPropertiesSizing `json:"sizing,omitempty"`
}

// CloudTerminalPropertiesAudit is docs/plan/19 § Auditing. Who, when, from where, which subscription and for how long is always recorded and is not a setting.
type CloudTerminalPropertiesAudit struct {
	// Whether everything typed into and printed by this shell is recorded. ⚠ Off by default and immutable afterwards: a shell contains secrets, a keystroke log is a liability, and a recording that could be switched off mid-life would be worth nothing to the compliance requirement it exists for.
	SessionRecording *bool `json:"sessionRecording,omitempty"`
}

// CloudTerminalPropertiesHome is The persistent home directory. The only part of a console that survives an idle reclaim.
type CloudTerminalPropertiesHome struct {
	// How long $HOME is kept after the console was last attached to. ⚠ Nothing sweeps on it yet — it is carried so that a console created today is swept correctly by whatever does.
	RetentionDays *int64 `json:"retentionDays,omitempty"`
	// The size of $HOME. It grows and never shrinks: a PersistentVolumeClaim refuses a decrease at the API.
	Size string `json:"size"`
}

// CloudTerminalPropertiesIdentity is Who the shell runs as.
type CloudTerminalPropertiesIdentity struct {
	// The managed identity every command in this shell acts as. It may not be changed: a console whose identity moved would have an audit trail describing a shell that no longer exists.
	PrincipalID string `json:"principalId"`
}

// CloudTerminalPropertiesImage is Which shell image to run.
type CloudTerminalPropertiesImage struct {
	// default carries every tool in docs/plan/19's table and takes about 40 seconds to pull cold; minimal carries shells, editors, cyc and kubectl and starts in about 8. The repository and the digest are the platform's.
	Variant *CloudTerminalVariant `json:"variant,omitempty"`
}

// CloudTerminalPropertiesNetwork is What the shell may reach. Nothing may ever reach the shell.
type CloudTerminalPropertiesNetwork struct {
	// Internet lets the shell reach public addresses as well as this subscription's own workloads and DNS — a shell that cannot git clone is not a shell. TenantOnly removes the public half and leaves the rest.
	Egress *CloudTerminalEgress `json:"egress,omitempty"`
}

// CloudTerminalPropertiesSession is When a shell stops. Both numbers are ceilings a tenant may lower and neither may be turned off.
type CloudTerminalPropertiesSession struct {
	// How long a shell may sit with nobody typing into it before it is reclaimed. The home volume survives; the pod does not. ⚠ There is no value meaning never — an idle terminal is a tenant paying for something they closed.
	IdleTimeoutMinutes *int64 `json:"idleTimeoutMinutes,omitempty"`
	// The longest a single shell may run, busy or not. Enforced by the kubelet through the pod's own deadline, so it holds even when nothing of this platform is running.
	MaxDurationHours *int64 `json:"maxDurationHours,omitempty"`
}

// CloudTerminalPropertiesSizing is CPU and memory for the shell.
type CloudTerminalPropertiesSizing struct {
	// How much a shell gets. The ladder stops at 2 vCPU and 4 GiB, which is docs/plan/19's ceiling for this row — a workload that needs more needs a cluster and a job rather than a terminal.
	Preset *CloudTerminalPreset `json:"preset,omitempty"`
}

// CloudTerminalResource is one Cloud terminal, as the API returns it: the Resource envelope, then the body. ⚠ Read, never written.
type CloudTerminalResource struct {
	Resource
	// The body, as the caller wrote it and the manager holds it.
	Data CloudTerminalData
}

// UnmarshalJSON reads the envelope and the body off one object.
func (r *CloudTerminalResource) UnmarshalJSON(data []byte) error {
	if err := json.Unmarshal(data, &r.Resource); err != nil {
		return err
	}
	return json.Unmarshal(data, &r.Data)
}

// CloudTerminalConnectResult is what connect returns.
type CloudTerminalConnectResult struct {
	// The SignalR hub path to open. docs/plan/10 § SignalR.
	Hub string `json:"hub"`
	// How long this shell may sit idle before it is reclaimed. Shown to the person, because a terminal that vanishes without warning reads as a bug.
	IdleTimeoutSeconds int64 `json:"idleTimeoutSeconds"`
	// How long this shell may run in total before the kubelet stops it.
	MaxDurationSeconds int64 `json:"maxDurationSeconds"`
	// Whether this session is being recorded. ⚠ docs/plan/19 § Auditing requires the portal to be loud about this, which is why it is on the connect response rather than only on the resource body: a panel that had to fetch the resource to find out would render one frame of a terminal that lies.
	Recording bool `json:"recording"`
	// The session to name on the terminal hub. ⚠ It is the shell pod's own UID, so it changes when a reclaimed console is re-created — which is exactly when a client's replay buffer has become meaningless.
	SessionID string `json:"sessionId"`
	// Starting while the shell is still being scheduled, Ready once it is running. A client opens the hub either way and sees output when there is some.
	State string `json:"state"`
}

// CloudTerminalTerminateResult is what terminate returns.
type CloudTerminalTerminateResult struct {
	// True when this call stopped a running shell, false when there was none. ⚠ Both are success: the caller's goal is that no shell is running.
	Terminated bool `json:"terminated"`
}

var _ = json.Marshal
