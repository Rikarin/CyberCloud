<Project Sdk="Microsoft.NET.Sdk">

    <!--
      docs/plan/03 § Providers — the third of the five projects: "ABP application services the gateway
      routes to".

      ⚠ IT HOLDS THE ABP MODULE AND NO APPLICATION SERVICE. Every verb this type has — PUT, GET,
      DELETE, and the two POST actions — is a generic resource-manager verb the gateway routes from
      the provider registry (ADR-012).
    -->

    <ItemGroup>
        <PackageReference Include="Volo.Abp.Ddd.Application"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="../CyberCloud.Providers.RecoveryServices.Contracts/CyberCloud.Providers.RecoveryServices.Contracts.csproj"/>
        <!--
          ⚠ THE IMPLEMENTATION ASSEMBLY, AND THIS LINE IS WHAT MAKES THE MODULE WORTH LOADING.
          docs/plan/03 § Providers: "each registers its resource types into CyberCloud.ResourceManager".
          Rule 5 keeps the gateway off the implementation assembly and it still holds: the gateway
          references THIS project, binds the module type, and binds nothing from the implementation.
        -->
        <ProjectReference Include="../CyberCloud.Providers.RecoveryServices/CyberCloud.Providers.RecoveryServices.csproj"/>
        <!--
          ⚠ The one CyberCloud.ResourceManager edge a provider family may carry — rule 8's second
          half lets a .Application name ResourceManagerSiloBuilderExtensions from it and nothing else.
        -->
        <ProjectReference Include="../../../CyberCloud.ResourceManager/CyberCloud.ResourceManager.csproj"/>
    </ItemGroup>

    <ItemGroup Label="CyberCloud.Analyzers — see CyberCloud.Core.csproj for why this is not a reference">
        <ProjectReference Include="../../../CyberCloud.Analyzers/CyberCloud.Analyzers.csproj"
                          OutputItemType="Analyzer"
                          ReferenceOutputAssembly="false"/>
    </ItemGroup>

</Project>
