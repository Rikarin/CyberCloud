<Project Sdk="Microsoft.NET.Sdk">

    <!--
      docs/plan/03 § Providers — the implementation assembly: "grains, reconcilers, the chart
      bindings". One reconciler, two action handlers, one chart binding; no grain, for the reason the
      .Contracts .csproj gives.

      ⚠ ITS ENTIRE REFERENCE SET IS ITS OWN .Contracts. Everything else — IResourceReconciler,
      ReconcileContext, IResourceView, KubeCommand — arrives through it. ⚠ AND NOT
      CyberCloud.Providers.DBforPostgreSQL, whose Cluster this reconciler schedules backups of: the
      reconciler reaches the server through ReconcileContext.View and knows its objects by the
      addresses the view returns, which is the one route src/Providers/README.md § Hard rule allows.
    -->

    <ItemGroup>
        <ProjectReference Include="../CyberCloud.Providers.RecoveryServices.Contracts/CyberCloud.Providers.RecoveryServices.Contracts.csproj"/>
    </ItemGroup>

    <ItemGroup Label="CyberCloud.Analyzers — see CyberCloud.Core.csproj for why this is not a reference">
        <ProjectReference Include="../../../CyberCloud.Analyzers/CyberCloud.Analyzers.csproj"
                          OutputItemType="Analyzer"
                          ReferenceOutputAssembly="false"/>
    </ItemGroup>

</Project>
